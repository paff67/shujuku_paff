<analysis>
context: 当前任务已经进入 code 模式并且已批准执行 `.limcode/plans/legacy-native-read-compat-fix.md`。TODO 中 `legacy_native_fix_1` 正在进行。已读取 `tests/service/table/table-delta-reconstruct.test.ts` 全文件：该测试文件已有 `makeSheet()`、`makeData()`、`checkpoint()`、`delta()`、`aiMessage()` 等 helper，现有测试覆盖 V2 checkpoint/delta 回放、legacy IndependentData 懒迁移、V2 checkpoint 优先、saveChatAfterMigration=false、以及“模板 key 不匹配时仍从 legacy 标准表生成首楼 checkpoint”。现有最后一个测试只覆盖单个 `sheet_0` 和 `TavernDB_ACU_Data`，尚未覆盖用户样本里的非连续 uid sheet key、根 `chat_metadata.sheets` 元数据、summary/outline 分流、`content[0][0] === null` 行首迁移。已读取 `src/service/table/table-delta-reconstruct.ts`：`reconstructTablesFromChatDeltas_ACU(chat, context, options)` 会先扫描 V2 layer；一旦 `sawV2Checkpoint` 为 true，直接返回 V2 数据，不做 legacy fallback；若无 V2 checkpoint 且允许 legacy，则调用 `buildLegacyCheckpointFromChat_ACU()`，传入 `templateSheetKeys` 与 `targetBoundaryMessageIndex`；legacy 成功后可通过 `migrateLegacyCheckpointToRootMessage_ACU()` 写回首个/目标 AI 消息。已读取 `src/service/table/table-delta-migration.ts` 关键段：`buildLegacyCheckpointFromChat_ACU()` 逆序扫描非用户消息，先读 isolated tag，再在 `isLegacyMatchForIsolation_ACU()` 通过后读取 `TavernDB_ACU_IndependentData`、`TavernDB_ACU_Data`、`TavernDB_ACU_SummaryData`；`mergeLegacyContainer_ACU()` 使用 `isSummaryOrOutlineTable_ACU(sheet.name)` 将普通表与 summary/outline 表分别合并；首轮若模板 key 过滤导致 `foundSheets.size === 0`，会使用 undefined template filter 重扫；成功后会把旧 `content[0][0] === null` 迁移为 `row_id`，并给数据行首列补字符串 id。已搜索测试引用：`reconstructTablesFromChatDeltas_ACU()` 主要在 `table-delta-reconstruct.test.ts` 与 retention 测试中调用；没有现成 `mergeAllIndependentTablesWithMeta_ACU()` 测试。真实样本证据显示第 1 行 `chat_metadata.sheets` 是元数据/表头，AI 消息顶层 `TavernDB_ACU_Data` 才包含历史行；因此第一步应补 migration 层样本驱动测试，先验证当前 reconstruct 是否能吃下这种结构，而不是直接写转换器。
needs: 本质目标是修复旧原生模式记录读取兼容性，使用户样本风格的旧聊天能加载历史表格数据而不是空模板。当前阶段的直接目标是用样本结构构造可重复测试，锁定丢失点在 migration 层还是后续 helper/provider/初始化覆盖层。测试必须能区分“只有 chat_metadata 表头”与“消息级 TavernDB_ACU_Data 历史行”，否则会制造假修复。
key_challenges: 第一，用户样本里的 sheet key 不是 `sheet_0`，而是 `sheet_dCudvUnH`、`sheet_DpKcVGqg` 等非连续 uid，测试必须覆盖而不是沿用简单 `sheet_0`。第二，样本根 `chat_metadata.sheets` 的 uid 与消息级 `TavernDB_ACU_Data` 的 uid 不一致，必须验证模板过滤失败后无过滤重扫能保留消息级历史行。第三，summary/outline 表在 `TavernDB_ACU_Data` 中，migration 通过 `summaryOnly` 两次分流合并，测试必须验证 `总结表` 和 `总体大纲` 没被漏掉。第四，当前类型 `Sheet_ACU.content` 是 `(string | null)[][]`，真实样本有数值单元格，但第一批测试可使用字符串化内容避免 TS 噪音，后续如发现运行时数值兼容问题再单独补测试。第五，当前 migration 单测可能已经通过，这并不代表用户问题已修复，因为 helper/provider 层仍可能初始化覆盖，所以不能在 migration 测试通过后宣称完成。
confidence: MEDIUM
  - 理由: migration 层上下文和测试结构已充分读取，第一批测试可以精确落地；但实际用户故障丢失点尚未通过运行测试确认，仍可能在 helper/provider、V2 checkpoint、模板 seed 或初始化覆盖层。
approach: 三维评估综合最优的方案是先补样本驱动 migration 测试，再运行定向测试。如果 migration 层失败，就修 migration；如果通过，则把 `legacy_native_fix_1` 作为“migration 层排除/确认”完成，并进入 `legacy_native_fix_2` 调查 helper/provider 实际扫描链路。这个顺序符合计划，也避免把 `chat_metadata.sheets` 转换器过早塞进主链路。
  三维评分（每个维度 1-5 分，5 为最优）:
  - 可维护性: 5/5 — 直接复用现有测试 helper，在同一 describe 中补充样本风格用例，测试意图与已有 legacy migration 测试相邻，不引入新测试框架或额外 fixture 目录。
  - 健壮性: 4/5 — 覆盖非连续 uid、模板 key 不匹配、summary/outline、row_id 迁移、根 metadata 不覆盖消息级历史行；暂不覆盖真实数值单元格与 provider 初始化覆盖，后续 TODO 会继续处理。
  - 可扩展性: 4/5 — 样本 helper 可在后续 helper/provider/SQLite 测试中复用或复制；没有提前抽象公共 fixture，避免当前阶段过度工程。
edge_cases: 
  - 当 chat[0] 是根 metadata 形态且没有 `is_user` 字段时，reconstruct 扫描不应因该对象没有 `TavernDB_ACU_Data` 而误判有数据；真正历史行应来自后续 AI 消息。
  - 当 `templateSheetKeys` 使用 `chat_metadata.sheets` 的 uid，而消息级 `TavernDB_ACU_Data` 使用另一组 uid 时，首轮过滤会找不到 sheet，fallback 重扫必须恢复历史数据。
  - 当 `TavernDB_ACU_Data` 同时包含普通表、`总结表`、`总体大纲` 时，两次 `mergeLegacyContainer_ACU()` 分流后最终 checkpoint 必须同时包含这些表。
  - 当旧 content 的 header row 首列是 null 且数据行首列也是 null 时，migration 必须把 header 首列改为 `row_id`，数据行首列改为稳定字符串 id。
  - 当根 `chat_metadata.sheets` 只有表头且消息级 `TavernDB_ACU_Data` 有历史行时，结果不得退化为仅表头空表。
affected_scope: 
  - tests/service/table/table-delta-reconstruct.test.ts
  - .analysis-cache.md
  - .limcode/plans/legacy-native-read-compat-fix.md
execution_plan:
  - step_1: 将本 analysis 逐字写入 `.analysis-cache.md`，保证执行阶段有可追溯决策链。
  - step_2: 修改 `tests/service/table/table-delta-reconstruct.test.ts`，在现有 describe 末尾追加样本风格 migration 测试；测试构造根 `chat_metadata.sheets` 元数据和 AI 消息顶层 `TavernDB_ACU_Data`，并断言非连续 uid sheet、summary/outline、row_id 迁移和历史行保留。
  - step_3: 运行 `npx vitest run tests/service/table/table-delta-reconstruct.test.ts`，确认 migration 层是否失败。
  - step_4: 如果 migration 测试失败，基于失败信息定位 `table-delta-migration.ts` 或 `table-delta-reconstruct.ts` 的最小修复点；如果 migration 测试通过，则把 `legacy_native_fix_1` 标记为 completed，并将 `legacy_native_fix_2` 标记为 in_progress，开始调查 helper/provider/V2/初始化覆盖。
  - step_5: 每次 TODO 状态发生 meaningful change 时同步 `todo_update` 与 `update_plan(updateMode: progress_sync)`。
degradation_check:
  - 方案是否是三维评估（可维护性、健壮性、可扩展性）综合最优的？ → YES，当前最高风险是根因误判，样本驱动测试能用最小改动验证 migration 层真实行为；可维护性 5/5，健壮性 4/5，可扩展性 4/5。
  - 是否遗漏了已知边界条件？ → NO，当前已知的非连续 uid、模板 key 不匹配、summary/outline、row_id null 迁移、metadata 不覆盖消息级数据都纳入第一批测试；provider/SQLite 属于后续 TODO，不在本步伪装完成。
  - 是否因改动量大而想缩减方案？ → NO，当前只执行 legacy_native_fix_1 的第一批测试，不跳过后续 helper/provider/SQLite；分阶段是计划要求，不是缩减范围。
  - 是否打算跳过某些文件？ → NO，当前 affected_scope 覆盖本阶段必要文件；helper/provider 文件不在本阶段直接修改，但会在 migration 测试结果后进入下一 TODO。
  - execution_plan是否覆盖affected_scope所有文件？ → YES，step_1 覆盖 `.analysis-cache.md`，step_2 覆盖测试文件，step_5 覆盖计划同步文件。
  - context是否充分？是否有未读但可能相关的文件？ → YES，有未读的 helper/provider 测试与实现入口，但对当前 step_2 的 migration 测试不构成阻塞；进入 legacy_native_fix_2 前必须继续读取。
  - 是否有发现了但被我判断为"无关紧要"而跳过的问题？ → NO，真实样本中数值单元格暂未进入第一批测试不是判断无关，而是为了避免与 TypeScript 测试类型噪音混在一起；后续如 provider 或 SQL 导入暴露数值兼容问题需补测。
  - execution_plan中是否有步骤计划使用 shell 命令（sed/awk/perl）修改源代码？ → NO，代码修改使用 apply_diff 或 insert_code，shell 只用于 vitest/tsc/rollup 等验证命令。
</analysis>

---
<decision_point>
issue: 运行 `npx vitest run tests/service/table/table-delta-reconstruct.test.ts` 后，新增样本测试失败，同时既有 legacy 测试也出现失败。已看到的两类证据是：（1）既有“模板 key 不匹配”测试在 `tests/service/table/table-delta-reconstruct.test.ts:430` 断言 `chat[0].TavernDB_ACU_Data` 为 undefined 失败，说明当前实现可能没有清理标准 legacy 字段，或测试断言已与迁移清理策略不一致；（2）新增样本测试在 `tests/service/table/table-delta-reconstruct.test.ts:505` 断言 `checkpointMessageIndex === 0` 失败，实际为 2，说明我把 JSONL 根 metadata 作为 chat 数组第 0 项后，当前 reconstruct/migration 把消息级 legacy 所在 AI 消息作为 checkpoint anchor，而不是把根 metadata 当作首个可写 AI 消息。这个问题在分析阶段没有完全预见到，因为我虽然知道第 1 行是根 metadata，但测试构造时没有充分确认 `reconstructTablesFromChatDeltas_ACU()` 对“无 is_user 字段的根对象”的处理语义。
impact: YES。它影响当前样本驱动测试的有效性，也暴露既有测试与当前迁移清理行为之间可能已经存在不一致。若直接按新增断言修实现，可能把根 metadata 错当 AI 消息写入 checkpoint；若直接改断言，又可能掩盖清理路径缺陷。必须先拆分：测试 fixture 是否错误、既有失败是否是已有实现缺陷、还是断言过时。
context_update: 
  - analysis.context 需要补充：`reconstructTablesFromChatDeltas_ACU()` 只用 `if (!message || message.is_user) continue` 跳过用户消息；没有 `is_user` 字段的对象会被当作 AI 消息处理。
  - analysis.edge_cases 需要修正：根 `chat_metadata.sheets` 如果被放入 chat 数组且未标记 `is_user: true`，它不是“被忽略的根 metadata”，而是一个可被当作 AI 消息扫描的对象。
  - analysis.execution_plan step_3 需要增加“先读取迁移写回/清理逻辑与完整失败输出，再决定是修测试还是修实现”。
options:
  - option_a:
      description: 仅修改新增测试，把根 metadata 从 chat 数组中移除，直接模拟 SillyTavern 运行时传给插件的消息数组只包含聊天消息；同时把 `checkpointMessageIndex` 断言改为消息级 legacy 所在索引。既有失败暂时不处理。
      approach_evaluation: 可维护性 2/5，因为它绕开了计划中“根 metadata + 消息级数据共存”的验证；健壮性 2/5，因为既有失败仍未解释，可能继续掩盖清理或迁移语义问题；可扩展性 2/5，因为后续 metadata fallback 仍缺少测试基础。
      edge_cases: 无法覆盖根 metadata 被错误传入扫描数组时的行为；无法判断标准 legacy 字段不清理是否符合当前策略。
      affected_scope_delta: 只改 `tests/service/table/table-delta-reconstruct.test.ts`，但会缩小当前验证范围。
  - option_b:
      description: 保留根 metadata 在测试中的存在，但显式标记为 `is_user: true` 或构造为不会被 migration 视为 AI 的对象，表达“根 metadata 不应成为可写 checkpoint anchor”；然后读取 `migrateLegacyCheckpointToRootMessage_ACU()` 与清理函数，基于源码确认 checkpoint 应写到哪里、哪些 legacy 字段应清理，再同步修正既有失败断言或实现。
      approach_evaluation: 可维护性 5/5，因为 fixture 语义与 reconstruct 的扫描规则明确对齐，测试表达更清楚；健壮性 5/5，因为同时处理新增测试和既有失败，不把红灯当噪音；可扩展性 4/5，因为后续 provider/helper 测试也可以沿用“根 metadata 不等于 AI 消息”的明确约定。
      edge_cases: 如果运行时实际 `getChatArray_ACU()` 根本不包含根 metadata，该 fixture 仍是保守覆盖；如果某入口错误地把根 metadata 混入 chat 数组，标记 `is_user: true` 的测试不能完全暴露该入口 bug，后续 helper/provider 测试仍需覆盖。
      affected_scope_delta: 继续涉及 `tests/service/table/table-delta-reconstruct.test.ts`，新增读取 `src/service/table/table-delta-migration.ts` 的迁移写回/清理段与 `src/service/table/table-delta-repository.ts` 的清理函数。
  - option_c:
      description: 修改生产代码，让 `reconstructTablesFromChatDeltas_ACU()` 显式跳过包含 `chat_metadata` 且没有 `mes`/`swipes`/`TavernDB_ACU_*` 的根对象，并强制迁移 checkpoint 写入第一个“真正 AI 消息”。同时更新测试断言。
      approach_evaluation: 可维护性 3/5，因为在 migration 层引入 SillyTavern JSONL 根对象识别规则会扩大职责；健壮性 3/5，因为可能修掉根 metadata 混入的问题，但在尚未确认 runtime chat 数组是否包含根对象前属于过早生产修复；可扩展性 3/5，因为后续还要适配更多 root metadata 变体，容易把导入格式判断塞进 reconstruct 层。
      edge_cases: 可能误跳过合法 AI 消息中带 `chat_metadata` 的异常对象；可能改变现有迁移 anchor 语义；需要新增更多类型守卫测试。
      affected_scope_delta: 新增修改 `src/service/table/table-delta-reconstruct.ts` 或 `src/service/table/table-delta-migration.ts`，扩大当前阶段范围。
recommendation: 选择 option_b。三维评估显示 option_b 在可维护性 5/5、健壮性 5/5、可扩展性 4/5 上综合最优。它先修正测试 fixture 的语义，再基于源码确认既有失败，不会把测试错误硬推成生产代码改动，也不会跳过已有红灯。
execution_plan_update: 
  - 更新 step_3: 读取 `src/service/table/table-delta-migration.ts` 的 `migrateLegacyCheckpointToRootMessage_ACU()` 与清理段、读取 `src/service/table/table-delta-repository.ts` 的 legacy 清理函数，并获取更完整的 vitest 失败信息。
  - 更新 step_4: 若源码确认标准 `TavernDB_ACU_Data` 应清理但当前未清理，则修生产清理逻辑；若源码确认不再清理标准字段，则修既有测试断言；同时把新增样本 fixture 的根 metadata 明确标记为非 AI 消息，避免 checkpoint anchor 断言建立在错误模型上。
deviation_audit:
  original_plan_excerpt: 
    - step_3: 运行 `npx vitest run tests/service/table/table-delta-reconstruct.test.ts`，确认 migration 层是否失败。
    - step_4: 如果 migration 测试失败，基于失败信息定位 `table-delta-migration.ts` 或 `table-delta-reconstruct.ts` 的最小修复点；如果 migration 测试通过，则把 `legacy_native_fix_1` 标记为 completed，并将 `legacy_native_fix_2` 标记为 in_progress，开始调查 helper/provider/V2/初始化覆盖。
  current_proposal: 
    - step_3 改为先读取 migration 写回/清理源码和完整失败信息，区分测试 fixture 错误、断言过时、生产清理缺陷。
    - step_4 改为按源码证据修正测试 fixture 与既有断言/生产清理逻辑，然后再重跑定向测试。
  diff_summary:
    - 新增读取 `migrateLegacyCheckpointToRootMessage_ACU()` 与清理函数的动作。
    - 新增对“根 metadata 是否应在 chat 数组中作为 AI 消息参与扫描”的 fixture 语义修正。
    - 保留原本“失败则定位最小修复点”的目标，没有缩小范围。
  deviation_motive_check:
    - **措辞替换规则逐类检查**：未命中需要自我剖析的降级措辞；推荐理由基于三维评分，没有使用“务实”“折中”“更简单”等伪理性化或捷径措辞。
    - 偏离后的方案在三维评分上是否低于原方案？→ NO。原方案未包含“根 metadata 被当 AI 消息”的新发现；更新后可维护性和健壮性更高。
    - 偏离是否导致 affected_scope 缩小？→ NO。affected_scope 增加了 migration/repository 读取确认，没有砍掉测试或验证。
  self_dissection: 未触发。
degradation_check:
  - 推荐方案是否是三维评估（可维护性、健壮性、可扩展性）综合最优的？ → YES，option_b 的评分为 5/5、5/5、4/5，高于 option_a 和 option_c。
  - 推荐方案是否遗漏了新发现的边界条件？ → NO，已覆盖根 metadata 被误当 AI 消息、checkpoint anchor 实际索引、标准 legacy 字段是否清理这三类新问题。
  - 是否因为想尽快完成而选择了改动量小的方案？ → NO，没有选择只改新增测试的 option_a，而是先补源码证据并处理既有失败。
  - 修改后的execution_plan是否仍覆盖所有affected_scope？ → YES，继续覆盖测试文件、analysis cache、计划同步，并新增读取 migration/repository 相关实现。
  - 是否有发现了但被判断为"无关紧要"而跳过的问题？ → NO，既有测试失败没有被跳过。
  - options 是否包含至少三个方案？ → YES，包含 option_a、option_b、option_c。
  - 是否因为工具报错而准备换用 shell 命令修改源代码？ → NO，后续代码修改继续使用 apply_diff/insert_code，shell 只用于测试和只读输出。
  - deviation_audit 是否触发了 self_dissection？ → NO。
</decision_point>

---
<analysis>
context: 已重新读取 `src/service/table/table-delta-reconstruct.ts` 全文件。当前 reconstruct 流程先扫描 V2 layer；若存在 V2 checkpoint，直接返回并跳过 legacy migration；若无 V2 checkpoint 且允许 legacy，则计算 `legacyBoundaryIndex`：无 V2 时为 `endExclusive - 1`，有 V2 时为 `firstV2MessageIndex - 1`，再调用 `buildLegacyCheckpointFromChat_ACU()`。legacy 成功后，当前代码在 `options.saveChatAfterMigration !== false` 时调用 `migrateLegacyCheckpointToRootMessage_ACU()` 写入 V2 checkpoint，`migrationResult !== null` 时只更新 `migratedCheckpointMessageIndex`、`migratedCheckpoint` 并设置 `changed = true`，没有清理旧式 `TavernDB_ACU_Data` 等 legacy 字段。已重新读取新增测试区域：新增样本测试仍把根 `chat_metadata.sheets` 对象放在 `chat[0]` 且没有 `is_user: true`，当前断言 `result.checkpointMessageIndex === 0`，并从 `chat[0].TavernDB_ACU_IsolatedData` 读取迁移结果；这与当前 reconstruct 的扫描规则冲突，因为只有 `message.is_user` 为 truthy 才会跳过，根 metadata 对象会被当作非用户消息。已重新读取 `.analysis-cache.md`：之前 decision_point 已选择 option_b，即保留根 metadata fixture，但显式标记为非 AI 消息，并处理生产迁移成功后未清理 legacy 字段的问题。
needs: 当前直接目标是完成 `legacy_native_fix_1` 的实现闭环：修复迁移成功后 legacy 源字段未清理的生产缺陷，修正新增样本测试的根 metadata 语义与 checkpoint anchor 断言，然后运行定向 vitest 确认 migration 层 14 个测试全部通过。必须保持 V2 checkpoint 优先级，不把 `chat_metadata.sheets` 当主数据源，不引入跨隔离串读。
key_challenges: 第一，清理只能发生在 legacy migration 写入成功之后；如果写回失败但清理了 legacy 源字段，会造成数据只读恢复路径被破坏，这种事故蠢得像把备份删了再宣称迁移完成。第二，清理范围必须限定到 `0..legacyBoundaryIndex`，不能碰到后续 V2 delta 消息，否则会破坏 checkpoint 后增量回放。第三，checkpoint anchor 消息可能同时包含刚写入的 V2 checkpoint 和旧式 `TavernDB_ACU_Data`，清理函数必须保留 meaningful V2 layer；已有 `clearCurrentIsolationLegacyTableSnapshots_ACU()` 正是为这个边界设计，应复用而不是重新写一份脆弱删除逻辑。第四，新增测试的根 metadata 必须显式标记 `is_user: true`，否则测试是在验证错误 fixture 行为，不是在验证用户样本兼容性。
confidence: HIGH
  - 理由: 目标代码、测试区域和既有 decision_point 均已重新读取；生产缺陷位置明确，修复点是单文件 import 与迁移成功分支；测试修正点是单个 fixture 与两处 anchor 断言。仍需通过 vitest 验证清理函数与 checkpoint 写回同消息共存的边界。
approach: 三维评估综合最优的方案是复用 `clearCurrentIsolationLegacyTableSnapshots_ACU()`，在 `migrateLegacyCheckpointToRootMessage_ACU()` 成功后遍历 `0..legacyBoundaryIndex` 的非用户消息执行 legacy 字段清理；同时把新增样本 fixture 的根 metadata 标记为 `is_user: true`，将 checkpoint anchor 断言与迁移数据读取位置改为首个实际可写 AI 消息 `chat[2]`。这比在 reconstruct 层新增 root metadata 识别规则更稳，因为当前任务不是设计 JSONL 导入解析器，而是修复 legacy migration 的清理与测试语义。
  三维评分（每个维度 1-5 分，5 为最优）:
  - 可维护性: 5/5 — 复用 repository 层已有清理函数，不复制字段删除列表；修改集中在迁移成功分支，语义清楚。
  - 健壮性: 5/5 — 只在写回成功后清理，且清理范围以 `legacyBoundaryIndex` 限制在 V2 前；清理函数保留 meaningful V2 layer，覆盖同消息 checkpoint+legacy 源字段共存。
  - 可扩展性: 4/5 — 后续如果新增 legacy 字段，只需维护 repository 清理函数；本次不把 JSONL root metadata 判断塞进 reconstruct 层，避免职责污染。
edge_cases:
  - 当 `migrateLegacyCheckpointToRootMessage_ACU()` 返回 null 或抛错时，不执行 legacy 字段清理，避免没有 V2 checkpoint 时删除唯一可恢复数据。
  - 当 `legacyBoundaryIndex` 为 `endExclusive - 1` 时，清理覆盖所有参与 legacy 扫描的旧消息，但跳过 `message.is_user`。
  - 当存在后续 V2 delta 时，`legacyBoundaryIndex` 为 `firstV2MessageIndex - 1`，清理不会触碰 V2 delta 消息。
  - 当 checkpoint anchor 消息本身包含 `TavernDB_ACU_Data` 时，清理应删除旧字段但保留刚写入的 `TavernDB_ACU_IsolatedData[''].tablePersistenceV2.checkpoint`。
  - 当根 `chat_metadata.sheets` 只是元数据表头时，测试 fixture 用 `is_user: true` 表达它不是可写 AI checkpoint anchor，历史行仍来自后续 AI 消息顶层 `TavernDB_ACU_Data`。
affected_scope:
  - src/service/table/table-delta-reconstruct.ts
  - tests/service/table/table-delta-reconstruct.test.ts
  - .analysis-cache.md
  - .limcode/plans/legacy-native-read-compat-fix.md
execution_plan:
  - step_1: 将本 analysis 逐字追加写入 `.analysis-cache.md`，保留当前执行阶段决策链。
  - step_2: 修改 `src/service/table/table-delta-reconstruct.ts` 的 import，把 `clearCurrentIsolationLegacyTableSnapshots_ACU` 从 `table-delta-repository` 引入；在 `migrationResult !== null` 且 `changed = true` 后遍历 `0..legacyBoundaryIndex`，对每条非用户消息调用清理函数。
  - step_3: 修改 `tests/service/table/table-delta-reconstruct.test.ts` 新增样本 fixture，在根 metadata 对象添加 `is_user: true`；将 `checkpointMessageIndex` 断言从 0 改为 2；将迁移数据读取从 `chat[0]` 改为 `chat[2]`。
  - step_4: 运行 `npx vitest run tests/service/table/table-delta-reconstruct.test.ts`，确认定向测试全部通过；如失败，读取失败信息并按根因修复，不跳过红灯。
  - step_5: 测试通过后同步 TODO：`legacy_native_fix_1` 标记 completed，`legacy_native_fix_2` 标记 in_progress，并用 `update_plan(updateMode: progress_sync)` 同步计划 TODO 快照。
degradation_check:
  - 方案是否是三维评估（可维护性、健壮性、可扩展性）综合最优的？ → YES，可维护性 5/5、健壮性 5/5、可扩展性 4/5；该方案复用现有清理函数并限定清理时机/范围，优于新增 root metadata 识别或只改测试。
  - 是否遗漏了已知边界条件？ → NO，已覆盖写回失败不清理、V2 delta 边界、同消息 checkpoint+legacy 字段共存、根 metadata 非 AI anchor、标准 legacy 字段清理。
  - 是否因改动量大而想缩减方案？ → NO，本阶段必须同时修生产清理与测试语义；不接受只改断言掩盖已有失败。
  - 是否打算跳过某些文件？ → NO，本阶段 affected_scope 全部进入 execution_plan；helper/provider 层属于后续 `legacy_native_fix_2`，不会在本阶段伪装完成。
  - execution_plan是否覆盖affected_scope所有文件？ → YES，step_1 覆盖 `.analysis-cache.md`，step_2 覆盖生产文件，step_3 覆盖测试文件，step_5 覆盖计划文件。
  - context是否充分？是否有未读但可能相关的文件？ → YES，对当前修复充分；`clearCurrentIsolationLegacyTableSnapshots_ACU()` 的详细实现已在此前读取并由总结保留，当前修复仅调用它。若 vitest 暴露清理函数行为异常，再窄范围读取 repository 实现。
  - 是否有发现了但被我判断为"无关紧要"而跳过的问题？ → NO，helper/provider 实际加载链路尚未验证，但已作为下一 TODO，不在 migration 层修复中冒充已解决。
  - execution_plan中是否有步骤计划使用 shell 命令（sed/awk/perl）修改源代码？ → NO，源码修改使用 apply_diff；shell 只用于 vitest 验证。
</analysis>

---
<decision_point>
issue: 定向 vitest 失败 4 个用例。已确认两类根因：（1）`migrateLegacyCheckpointToRootMessage_ACU()` 的锚点语义还没重新读取，现有保留窗口测试期望与实际 checkpoint 写入位置不一致，不能继续凭旧印象改断言；（2）新增真实样本测试的 `sheet_3NoMc1wI` 为 undefined，源码显示 `buildLegacyCheckpointFromChat_ACU()` 对 `TavernDB_ACU_Data` 只在 `summaryOnly: false` 时合并普通表，对 summary/outline 表只从 `TavernDB_ACU_SummaryData` 读取，因此用户样本里放在顶层 `TavernDB_ACU_Data` 内的 `总结表`、`总体大纲` 被跳过。这个问题在分析阶段没有预见到，因为此前把“summary/outline 分流”误理解为能从同一个标准容器做两次分流；实际代码第二次读取的是另一个 legacy summary 容器。
impact: YES。它影响当前方案的生产修复范围。只清理 legacy 字段不足以让用户样本完整恢复，因为 summary/outline 历史行仍会丢失；如果只修改测试断言，则会把真实用户数据丢失包装成测试通过，漏洞明显得像是故意写给事故看的。
context_update:
  - analysis.context 需要补充：`mergeLegacyContainer_ACU()` 本身支持 `summaryOnly` 分流，但当前调用对 `readLegacyStandardData_ACU(message)` 只执行 `summaryOnly: false`，对 `readLegacySummaryData_ACU(message)` 只执行 `summaryOnly: true`；这会丢弃标准 `TavernDB_ACU_Data` 里名称为 `总结表` 或 `总体大纲` 的表。
  - analysis.edge_cases 需要新增：旧 native 样本可能把 summary/outline 表也放在顶层 `TavernDB_ACU_Data`，migration 必须保留这些表，不能要求它们必须在 `TavernDB_ACU_SummaryData`。
  - analysis.execution_plan step_4 需要更新：在继续修复前必须读取 `migrateLegacyCheckpointToRootMessage_ACU()`，确认保留窗口 anchor 语义；同时修复 `buildLegacyCheckpointFromChat_ACU()` 标准容器 summary/outline 丢失。
options:
  - option_a:
      description: 只修改测试，把新增样本中的总结表/总体大纲断言删除或把这两张表移入 `TavernDB_ACU_SummaryData`，并按当前实际值调整保留窗口 checkpoint 索引。
      approach_evaluation: 可维护性 1/5，因为测试会偏离真实样本结构；健壮性 1/5，因为用户真实数据仍会丢 summary/outline；可扩展性 1/5，因为后续遇到标准容器混放表还会失败。
      edge_cases: 真实旧记录中 `TavernDB_ACU_Data` 混放普通表和总结/大纲表时仍然丢表。
      affected_scope_delta: 只改测试，缩小生产修复范围。
  - option_b:
      description: 修改 `buildLegacyCheckpointFromChat_ACU()`，让标准 legacy 容器 `TavernDB_ACU_Data` 同时合并普通表和 summary/outline 表；保留 `TavernDB_ACU_SummaryData` 的 summary 合并以兼容另一种旧格式。随后读取迁移锚点函数，按源码语义修正保留窗口测试或生产 anchor 逻辑。
      approach_evaluation: 可维护性 5/5，因为复用现有 `mergeLegacyContainer_ACU()`，只调整调用组合，不新增解析体系；健壮性 5/5，因为同时覆盖标准容器混放、独立 summary 容器和现有普通表路径；可扩展性 4/5，因为未来新增旧容器时仍可通过相同分流函数接入。
      edge_cases: 同一个 summary sheet 同时出现在 `TavernDB_ACU_Data` 和 `TavernDB_ACU_SummaryData` 时，`foundSheets` 去重会保留先合入的数据；需要确定标准容器优先还是 summary 容器优先。当前推荐标准容器先合入，因为用户样本的完整历史行在标准容器内。
      affected_scope_delta: 新增修改 `src/service/table/table-delta-migration.ts`，继续涉及 `tests/service/table/table-delta-reconstruct.test.ts`。
  - option_c:
      description: 新增专门的真实样本导入 fallback：当 `chat_metadata.sheets` 与 `TavernDB_ACU_Data` 共存时，绕过现有 legacy migration，直接按 `chat_metadata` 的 sheet 名和 uid 做映射转换。
      approach_evaluation: 可维护性 2/5，因为把 JSONL 元数据解析职责塞进 migration 主路径；健壮性 3/5，因为可能覆盖用户样本，但容易与现有消息级 legacy 数据优先级冲突；可扩展性 2/5，因为后续不同 metadata 结构会不断扩大特殊分支。
      edge_cases: 可能把只有表头的 `chat_metadata.sheets` 当成历史数据源；可能引入 sheet uid 重映射错误；可能破坏“不把 chat_metadata.sheets 当主数据源”的约定。
      affected_scope_delta: 需要新增生产解析逻辑和更多测试，扩大当前修复面。
recommendation: 选择 option_b。三维评估显示 option_b 可维护性 5/5、健壮性 5/5、可扩展性 4/5，综合最优。它修的是已证实的生产丢表路径，而不是让测试退让；同时不把 `chat_metadata.sheets` 提升为主数据源。
execution_plan_update:
  - step_4a: 读取 `migrateLegacyCheckpointToRootMessage_ACU()`，确认 retainRecentLayers 对 checkpoint anchor 的生产语义。
  - step_4b: 修改 `src/service/table/table-delta-migration.ts`，对 `readLegacyStandardData_ACU(message)` 同时执行 `summaryOnly: false` 和 `summaryOnly: true` 合并；保留 `readLegacySummaryData_ACU(message)` 的 summary 合并。
  - step_4c: 基于 `migrateLegacyCheckpointToRootMessage_ACU()` 的源码语义修正保留窗口相关测试或生产 anchor 逻辑；不得只按失败数字机械改断言。
  - step_4d: 重跑 `npx vitest run tests/service/table/table-delta-reconstruct.test.ts`，若仍失败，读取失败信息继续按根因处理。
deviation_audit:
  original_plan_excerpt:
    - step_4: 运行 `npx vitest run tests/service/table/table-delta-reconstruct.test.ts`，确认定向测试全部通过；如失败，读取失败信息并按根因修复，不跳过红灯。
  current_proposal:
    - step_4a: 读取 `migrateLegacyCheckpointToRootMessage_ACU()`，确认 retainRecentLayers 对 checkpoint anchor 的生产语义。
    - step_4b: 修改 `src/service/table/table-delta-migration.ts`，对 `readLegacyStandardData_ACU(message)` 同时执行 `summaryOnly: false` 和 `summaryOnly: true` 合并；保留 `readLegacySummaryData_ACU(message)` 的 summary 合并。
    - step_4c: 基于 `migrateLegacyCheckpointToRootMessage_ACU()` 的源码语义修正保留窗口相关测试或生产 anchor 逻辑；不得只按失败数字机械改断言。
    - step_4d: 重跑 `npx vitest run tests/service/table/table-delta-reconstruct.test.ts`，若仍失败，读取失败信息继续按根因处理。
  diff_summary:
    - 新增生产修复点 `src/service/table/table-delta-migration.ts`，处理标准 `TavernDB_ACU_Data` 内 summary/outline 表被丢弃的问题。
    - 新增读取迁移锚点函数，避免对保留窗口测试进行无依据断言修改。
    - 保留原定测试验证，不缩小验证范围。
  deviation_motive_check:
    - **措辞替换规则逐类检查**：未命中需要自我剖析的降级措辞；推荐方案基于三维评分，没有使用“折中”“更简单”等降级表达。
    - 偏离后的方案在三维评分上是否低于原方案？→ NO。原方案未覆盖执行期发现的标准容器 summary/outline 丢失；更新后健壮性提升。
    - 偏离是否导致 affected_scope 缩小？→ NO。affected_scope 增加 `src/service/table/table-delta-migration.ts`，没有删除原测试或生产清理修复。
  self_dissection: 未触发。
degradation_check:
  - 推荐方案是否是三维评估（可维护性、健壮性、可扩展性）综合最优的？ → YES，option_b 为 5/5、5/5、4/5，高于 option_a 和 option_c。
  - 推荐方案是否遗漏了新发现的边界条件？ → NO，覆盖标准容器混放 summary/outline、独立 summary 容器兼容、保留窗口 anchor 语义需源码确认。
  - 是否因为想尽快完成而选择了改动量小的方案？ → NO，没有选择删除测试断言的 option_a，而是修生产丢表路径。
  - 修改后的execution_plan是否仍覆盖所有affected_scope？ → YES，覆盖 reconstruct、migration、测试、analysis cache 和后续计划同步。
  - 是否有发现了但被判断为"无关紧要"而跳过的问题？ → NO，4 个测试失败都继续处理。
  - options 是否包含至少三个方案？ → YES，包含 option_a、option_b、option_c。
  - 是否因为工具报错而准备换用 shell 命令修改源代码？ → NO，继续用 read_file/apply_diff，shell 只用于 vitest。
  - deviation_audit 是否触发了 self_dissection？ → NO。
</decision_point>

---
<analysis>
context: 当前已进入 `legacy_native_fix_2`，`legacy_native_fix_1` 已完成：`tests/service/table/table-delta-reconstruct.test.ts` 14/14 通过。已读取 `src/service/runtime/helpers-data-merge.ts:69-224`：`mergeAllIndependentTablesWithMeta_ACU()` 从 `getChatArray_ACU()` 取运行时聊天数组，读取当前隔离 key，取 guide/template sheet keys，然后调用 `reconstructTablesFromChatDeltas_ACU(chat, context, { allowLegacyMigration: true, saveChatAfterMigration: true, retainRecentLayers })`。若 reconstruct changed，会调用 `saveChatToHost_ACU()`；之后剥离 `mate`，用 `foundCount = sheet_ keys 数量` 判断是否返回历史数据；当 foundCount <= 0 且有 guide 时才物化 guide，否则返回 null；当 foundCount > 0 且有 guide 时会用 guide metadata 覆盖匹配 sheet 的展示字段。已读取 `src/service/table/table-service.ts:412-438`：`loadOrCreateJsonTableFromChatHistory_ACU()` 在聊天非空时调用 `mergeAllIndependentTables_ACU()`，如果返回数据则设置 `currentJsonTableData_ACU` 并返回 `source='merged'`，否则初始化新模板。已读取 `src/service/table/native-table-service-adapter.ts`：native provider 的 `loadFromChat()` 只是委托 `loadOrCreateJsonTableFromChatHistory_ACU()`。已读取 `src/service/table/sql-table-service.ts:58-145`：SQL provider 调用 `mergeAllIndependentTablesWithMeta_ACU()`，若 `usedLegacyMigration && hasAnyMergedSheet`，即使没有真实数据行也会加载 SQLite，避免 legacy header-only 迁移被当空壳。已读取 `tests/service/runtime/helpers-data-merge.test.ts` 关键结构：该文件 mock 了 chat gateway、state-manager、template chat-scope、chat-message-data-repo、table-delta-repository；目前只从 helpers-data-merge 导入了 `mergeAllIndependentTables_ACU`，并只从 chat-message-data-repo 导入 `readIsolatedTagData_ACU`、`isLegacyMatchForIsolation_ACU`、`readLegacyIndependentData_ACU`。已有“旧版存储格式兼容”测试只覆盖 `TavernDB_ACU_IndependentData` mock，不覆盖 AI 消息顶层 `TavernDB_ACU_Data`、非连续 sheet uid、summary/outline 混放在标准容器、template key 不匹配 fallback、`mergeAllIndependentTablesWithMeta_ACU()` 的 meta 结果。已读取 `tests/service/table/table-service.test.ts:463-523`：`loadOrCreateJsonTableFromChatHistory_ACU()` 的合并路径用 mock `mergeAllIndependentTables`，只验证返回 merged/initialized，不直接覆盖真实 helper migration。已读取 `tests/service/table/sql-table-service.test.ts:324-415`：已有 `legacy migration 结果即使只有表头也应加载 SQLite` 测试，通过 mock `mergeAllIndependentTablesWithMeta_ACU()` 返回 usedLegacyMigration=true 验证 SQL load 逻辑；它不需要真实聊天数组。子代理只读调查与人工读取基本一致：helper 层是当前最小有效补测点，native adapter 本身只是委托，SQL 已有 meta 保护测试但可后续补更真实导入测试。
needs: 当前阶段的本质目标不是继续修改生产逻辑，而是确认运行时 helper 链路会把 `getChatArray_ACU()` 中的消息级 `TavernDB_ACU_Data` 交给 reconstruct，并且不会因 template sheet key 不匹配、foundCount 判断、summary/outline 分流或初始化路径把用户样本退化为空模板。必须补一个 helper 层集成测试，把 migration 层已修复的能力穿过 `mergeAllIndependentTablesWithMeta_ACU()` 验证到运行时加载入口。
key_challenges: 第一，`helpers-data-merge.test.ts` 现有 mock 默认把 `isSummaryOrOutlineTable_ACU` 固定为 false，这会掩盖 summary/outline 分流；新增测试必须临时设置它对 `总结表`、`总体大纲` 返回 true，否则测试本身无法击穿刚修的生产路径。第二，测试文件当前没有导入 `mergeAllIndependentTablesWithMeta_ACU`、`readLegacyStandardData_ACU`、`readLegacySummaryData_ACU`、`isSummaryOrOutlineTable_ACU`，需要小范围扩展 imports；这不是业务代码抽象，只是让测试能控制真实链路依赖。第三，helper 层 mock 了 `clearCurrentIsolationLegacyTableSnapshots_ACU` 为空函数，因此该测试不能验证清理副作用；清理已由 reconstruct 层测试覆盖，helper 测试应聚焦“运行时聊天数组 → legacy standard data → merged data/meta/saveChat”的路径。第四，native adapter 测试只验证委托，若在这里补真实数据流会大量重配 mock 且价值低；SQL 已有 usedLegacyMigration header-only 保护测试，本阶段优先补 helper 真链路，后续 `legacy_native_fix_4` 再按计划补 native/SQLite 回归。
confidence: HIGH
  - 理由: helper/provider 入口、现有测试 mock 结构和 SQL/native 覆盖边界均已读取；新增 helper 测试的修改范围明确，能直接验证 `legacy_native_fix_2` 的核心问题：运行时聊天数组包含消息级 `TavernDB_ACU_Data` 时不被初始化覆盖。
approach: 三维评估综合最优的方案是在 `tests/service/runtime/helpers-data-merge.test.ts` 的 `mergeAllIndependentTables_ACU` describe 内新增一个 `mergeAllIndependentTablesWithMeta_ACU` 测试，用样本风格 chat：根 metadata 标记 `is_user: true`，AI 消息带顶层 `TavernDB_ACU_Data`，template keys 刻意设置为 metadata uid 且与数据 uid 不匹配；mock `readLegacyStandardData_ACU(message)` 返回消息顶层 `TavernDB_ACU_Data`，mock `isLegacyMatchForIsolation_ACU` 返回 true，mock `isSummaryOrOutlineTable_ACU` 对总结/大纲表返回 true。断言结果 `usedLegacyMigration=true`、`changed=true`、`foundSheetCount=4`、普通表与 summary/outline 历史行保留、`row_id` 迁移完成、`saveChatToHost_ACU()` 被调用、迁移 checkpoint 写入 AI 消息而非根 metadata。这个测试直接覆盖 helper 层，不把 native/sql 委托测试改成低价值重复测试。
  三维评分（每个维度 1-5 分，5 为最优）:
  - 可维护性: 5/5 — 在现有 helper 测试文件和 describe 中扩展一个样本驱动用例，只新增必要 imports 和 mock 行为，不引入新 fixture 目录或重复 provider 测试架构。
  - 健壮性: 5/5 — 覆盖 template key 不匹配 fallback、顶层 `TavernDB_ACU_Data`、非连续 sheet uid、summary/outline 标识、row_id 迁移、changed 后保存、checkpoint anchor 非根 metadata。
  - 可扩展性: 4/5 — 后续 native/SQLite/provider 测试可复用该样本结构；当前没有过早抽公共 helper，避免给单一测试制造抽象债。
edge_cases:
  - 当运行时 `getChatArray_ACU()` 含根 metadata 对象时，测试显式 `is_user: true`，防止根 metadata 被当作可写 AI checkpoint anchor。
  - 当 template/guide sheet keys 与旧数据 sheet keys 完全不一致时，legacy migration 必须通过无过滤重扫恢复消息级历史数据。
  - 当 `TavernDB_ACU_Data` 同时包含普通表和 `总结表`、`总体大纲` 时，helper 返回的 merged data 必须包含所有 sheet，而不是只保留普通表。
  - 当旧 content 首列为 null 时，helper 返回数据必须已通过 migration/reconstruct 转成 `row_id` 和字符串行号。
  - 当 reconstruct changed 为 true 时，helper 必须调用 `saveChatToHost_ACU()` 持久化 lazy migration checkpoint。
affected_scope:
  - tests/service/runtime/helpers-data-merge.test.ts
  - .analysis-cache.md
  - .limcode/plans/legacy-native-read-compat-fix.md
execution_plan:
  - step_1: 将本 analysis 逐字追加写入 `.analysis-cache.md`，记录 helper/provider 链路侦察与测试方案。
  - step_2: 修改 `tests/service/runtime/helpers-data-merge.test.ts` 的 imports：从 utils 导入 `isSummaryOrOutlineTable_ACU`，从 helpers-data-merge 导入 `mergeAllIndependentTablesWithMeta_ACU`，从 chat-message-data-repo 导入 `readLegacyStandardData_ACU` 与 `readLegacySummaryData_ACU`。
  - step_3: 在 `mergeAllIndependentTables_ACU` describe 的旧版存储兼容测试后新增样本风格 helper 层测试，构造根 metadata + AI 消息顶层 `TavernDB_ACU_Data`，断言 meta、数据、保存与 checkpoint anchor。
  - step_4: 运行 `npx vitest run tests/service/runtime/helpers-data-merge.test.ts tests/service/table/table-delta-reconstruct.test.ts`，确认 helper 层与 migration 层同时通过；若失败，读取失败信息按根因处理。
  - step_5: 测试通过后同步 TODO：`legacy_native_fix_2` 标记 completed，`legacy_native_fix_3` 标记 in_progress，并用 `update_plan(updateMode: progress_sync)` 同步计划 TODO 快照。
degradation_check:
  - 方案是否是三维评估（可维护性、健壮性、可扩展性）综合最优的？ → YES，可维护性 5/5、健壮性 5/5、可扩展性 4/5；helper 层测试能直接验证运行时聊天数组链路，比只改 SQL/native mock 委托测试更能击穿真实问题。
  - 是否遗漏了已知边界条件？ → NO，已覆盖 template key 不匹配、summary/outline、row_id、saveChat、根 metadata 非 AI anchor；清理副作用已在 reconstruct 层覆盖，不在 helper mock 测试里重复假验证。
  - 是否因改动量大而想缩减方案？ → NO，当前不跳过 helper 层；native/SQLite 更完整回归已在 `legacy_native_fix_4` 中保留，不在本阶段冒充完成。
  - 是否打算跳过某些文件？ → NO，本阶段 affected_scope 全部进入 execution_plan；SQL/native 源码和测试已读取，结论是本阶段无需修改它们。
  - execution_plan是否覆盖affected_scope所有文件？ → YES，step_1 覆盖 `.analysis-cache.md`，step_2/3 覆盖 helper 测试文件，step_5 覆盖计划文件。
  - context是否充分？是否有未读但可能相关的文件？ → YES，对 `legacy_native_fix_2` 充分；若新增测试暴露 mock 或真实链路差异，再窄范围读取相关函数。
  - 是否有发现了但被我判断为"无关紧要"而跳过的问题？ → NO，native/SQLite provider 更深回归不是无关，而是已在后续 TODO 中排期；当前先锁定 helper 运行时链路。
  - execution_plan中是否有步骤计划使用 shell 命令（sed/awk/perl）修改源代码？ → NO，代码修改使用 apply_diff；shell 只用于 vitest 验证。
</analysis>


---
<analysis>
context: 当前 `legacy_native_fix_3` 正在处理 `chat_metadata.sheets` 保护性 fallback。已读取 `tests/service/table/table-delta-reconstruct.test.ts:14-41`：`Sheet_ACU` 在测试 helper 中需要 `uid`、`name`、`sourceData`、`content`、`updateConfig`、`exportConfig`、`orderNo`，默认 `sourceData` 为 `{ note: '', initNode: '', deleteNode: '', updateNode: '', insertNode: '' }`，`updateConfig` 为 `{ uiSentinel: -1, contextDepth: -1, updateFrequency: -1, batchSize: -1, skipFloors: -1 }`，`exportConfig` 包含 enabled、splitByRow、entryName、entryType、keywords、preventRecursion、injectionTemplate、extraIndexEnabled、extraIndexEntryName、extraIndexColumns、extraIndexColumnModes、extraIndexInjectionTemplate、entryPlacement、extraIndexPlacement、fixedEntryPlacement、fixedIndexPlacement 等字段。已读取 `src/service/table/table-delta-migration.ts:1-331`：该文件已有 `createEmptyTableData_ACU()`、`cloneJson_ACU()`、legacy checkpoint 构建、summary/outline 标准容器双合并、row_id 迁移、anchor 解析和 legacy root checkpoint 写入；适合在 migration 层新增 metadata sheet 转换函数，避免 reconstruct 层承担 JSONL metadata 结构解析细节。已读取 `src/service/table/table-delta-reconstruct.ts:1-259`：当前流程先扫描 V2；若 `sawV2Checkpoint` 为 true 直接返回，不做 legacy fallback；若无 V2 checkpoint 且允许 legacy，则调用 `buildLegacyCheckpointFromChat_ACU()`；当 legacyResult 不存在时，当前直接进入 A1b“孤立 V2 delta + 模板 seed fallback”，否则返回已有 data/null。已确认应在 `!legacyResult.checkpoint` 分支内、A1b 之前插入 `chat_metadata.sheets` fallback，因为这能保证 V2 checkpoint 和消息级 legacy checkpoint 都优先。已读取 `tests/service/table/table-delta-reconstruct.test.ts:400-537`：现有测试已覆盖模板 key 不匹配、真实样本风格 root metadata + 消息级 `TavernDB_ACU_Data` 共存且优先迁移消息级历史行。已读取 `tests/service/runtime/helpers-data-merge.test.ts:532-611`：helper 层已有样本风格消息级 legacy 集成测试，验证 `mergeAllIndependentTablesWithMeta_ACU()` 会保留 summary/outline 并保存迁移。搜索 `chat_metadata|cellHistory|column_header|sheet_origin` 只发现样本 JSONL 和已有测试中的 fixture，没有生产转换逻辑可复用；源码中此前也已确认无 `chat_metadata`、`cellHistory`、`hashSheet` 等读取路径。
needs: 当前目标是为“只有根 metadata、没有消息级 V2/legacy 有效表格数据”的旧记录提供保护性 fallback：从 `chat_metadata.sheets` 生成只含表头的 `TableDataObject_ACU` checkpoint，使 UI 至少能展示旧模板表头而不是初始化成无关空模板。这个 fallback 不能覆盖消息级历史行，不能在 V2 checkpoint 存在时触发，不能伪造数据行，不能跨隔离读取，不能把 metadata 当成主历史数据源。
key_challenges: 第一，`chat_metadata.sheets` 结构不是 `Sheet_ACU`，而是包含 `uid`、`name`、`hashSheet`、`cellHistory`、`config`、`template` 等字段的旧 metadata；如果半截对象直接塞入 `TableDataObject_ACU`，后续 UI/provider 访问 `sourceData`、`updateConfig`、`exportConfig` 时会踩 undefined。第二，列顺序不能随便用对象遍历，样本中 `hashSheet[0]` 按 cell uid 表达表头顺序，应优先按 hashSheet 映射 `cellHistory` 中 `type: 'column_header'` 的单元格；缺少 hashSheet 时才按 `cellHistory` 原顺序降级。第三，fallback 的触发条件必须窄：当前分支已经排除了 V2 checkpoint 和消息级 legacy checkpoint，但仍可能存在孤立 V2 delta；按用户约定，metadata fallback 必须在 A1b 模板 seed fallback 前触发，但只应在当前没有任何已回放 sheet 数据时触发，避免覆盖 orphan delta 产生的数据。第四，fallback checkpoint 需要写入可写 AI 消息才能完成 lazy migration；若聊天只有 root metadata 或全是用户消息，则可以返回 data 但不能假装 changed，也不能写到用户消息。
confidence: HIGH
  - 理由: 生产主路径、转换输入结构、默认 `Sheet_ACU` 字段、现有测试插入点均已读取；fallback 的触发位置和边界条件明确。唯一需要通过测试确认的是新增转换函数的 TypeScript 类型与现有 `Sheet_ACU` 字段是否完全匹配。
approach: 三维评估综合最优的方案是在 `table-delta-migration.ts` 新增导出的 `convertChatMetadataSheetsToTableData_ACU()`，负责把 metadata sheets 转为完整 `TableDataObject_ACU`；在 `table-delta-reconstruct.ts` 的 `!legacyResult.checkpoint` 分支中，先检查 `!hasAnySheet_ACU(data)`，再读取 `chat[0]?.chat_metadata?.sheets` 调用转换函数；若转换结果有 sheet，则构造 source 为 `legacy-migration` 的 V2 checkpoint，并写入 `resolveLegacyCheckpointAnchorMessageIndex_ACU()` 选择的可写 AI 消息，失败时只返回 data/ checkpoint 且 `changed=false`。测试上在 `table-delta-reconstruct.test.ts` 新增两个用例：一个验证只有 root metadata 时只生成表头行且不伪造数据行；另一个验证消息级 `TavernDB_ACU_Data` 存在时 metadata fallback 不触发，历史行优先。
  三维评分（每个维度 1-5 分，5 为最优）:
  - 可维护性: 5/5 — metadata 结构转换集中在 migration 层，reconstruct 只负责编排 fallback；复用 `createEmptyTableData_ACU()` 和已有 checkpoint 写入路径，不把字段默认值散落到测试或 UI 层。
  - 健壮性: 5/5 — 完整补齐 `Sheet_ACU` 必要字段；按 hashSheet 保留列顺序；无有效 header 的 metadata sheet 会被跳过；只生成 header row，不生成虚假数据行；V2 checkpoint 和消息级 legacy 优先级不变。
  - 可扩展性: 4/5 — 后续如需要支持 metadata config 到 update/export 配置映射，可在单个转换函数内扩展；当前不提前猜测 config 语义，避免把未知字段硬映射成错误行为。
edge_cases:
  - 当存在 V2 checkpoint 时，函数在 `sawV2Checkpoint` 分支已返回，metadata fallback 不会触发。
  - 当消息级 `TavernDB_ACU_Data` 或 isolated legacy 能构建 checkpoint 时，`legacyResult.checkpoint` 存在，metadata fallback 不会触发，历史行不会被表头 fallback 覆盖。
  - 当当前已有孤立 V2 delta 回放出 sheet 数据时，`hasAnySheet_ACU(data)` 为 true，metadata fallback 不触发，避免覆盖 delta 结果。
  - 当 `chat[0]?.chat_metadata?.sheets` 不存在、不是数组或没有任何可转换 sheet 时，继续原 A1b 模板 seed fallback 或返回当前 data/null。
  - 当 metadata sheet 缺少 `uid`、uid 不以 `sheet_` 开头、缺少 name 或没有任何 `column_header` 时，跳过该 sheet，避免制造不可用空壳表。
  - 当 `hashSheet[0]` 存在时，表头顺序按 hashSheet 中对应 `column_header` uid 排列；当 hashSheet 不存在或未匹配到 header 时，按 `cellHistory` 中 `column_header` 出现顺序排列。
  - 当 `sheet_origin` 的 data 缺少部分字段时，用默认 sourceData 字段补齐；未知字段不透传进 `Sheet_ACU` 的核心配置。
  - 当没有可写 AI 消息时，不写入 checkpoint，不标记 changed，但仍可返回转换出的 data 和 checkpoint 给当前加载流程使用。
affected_scope:
  - src/service/table/table-delta-migration.ts
  - src/service/table/table-delta-reconstruct.ts
  - tests/service/table/table-delta-reconstruct.test.ts
  - .analysis-cache.md
  - .limcode/plans/legacy-native-read-compat-fix.md
execution_plan:
  - step_1: 将本 analysis 逐字追加写入 `.analysis-cache.md`，保证本阶段 fallback 方案和边界可追溯。
  - step_2: 修改 `src/service/table/table-delta-migration.ts`，新增 metadata sheet 类型守卫/默认字段 helper，并导出 `convertChatMetadataSheetsToTableData_ACU(metadataSheets)`；转换函数返回完整 `TableDataObject_ACU | null`，只包含有效 sheet 的表头 content，不生成数据行。
  - step_3: 修改 `src/service/table/table-delta-reconstruct.ts` 的 import，引入 `convertChatMetadataSheetsToTableData_ACU`；在 `!legacyResult.checkpoint` 分支、A1b 模板 seed fallback 前插入 metadata fallback：仅当当前 data 没有 sheet 且 metadata 转换成功时构造 checkpoint，尝试写入可写 AI 消息，返回 `usedLegacyMigration: true`。
  - step_4: 修改 `tests/service/table/table-delta-reconstruct.test.ts`，新增“只有根 metadata 无消息级数据时 fallback 只产出表头”的测试，以及“消息级 legacy 存在时 metadata fallback 不覆盖历史行”的测试。
  - step_5: 运行 `npx vitest run tests/service/table/table-delta-reconstruct.test.ts tests/service/runtime/helpers-data-merge.test.ts`，确认 migration/reconstruct/helper 回归通过；失败则读取报错并按根因修复。
  - step_6: 测试通过后同步 TODO/计划：若 `legacy_native_fix_3` 完成，则标记 completed，并将下一项高风险验证 `legacy_native_fix_5` 或按用户计划的 `legacy_native_fix_4` 推进状态；使用 `update_plan(updateMode: progress_sync)` 同步 `.limcode/plans/legacy-native-read-compat-fix.md`。
degradation_check:
  - 方案是否是三维评估（可维护性、健壮性、可扩展性）综合最优的？ → YES，可维护性 5/5、健壮性 5/5、可扩展性 4/5；集中转换、窄触发、完整默认字段，比在 reconstruct 内硬拼对象或把 chat_metadata 当主数据源可靠。
  - 是否遗漏了已知边界条件？ → NO，已覆盖 V2 checkpoint 优先、消息级 legacy 优先、孤立 delta、无 metadata、无有效 header、hashSheet 顺序、sourceData 默认值、无可写 AI 消息。
  - 是否因改动量大而想缩减方案？ → NO，本阶段必须同时改生产转换、reconstruct 接入和测试；不接受只加测试或只返回半截对象。
  - 是否打算跳过某些文件？ → NO，affected_scope 中两个生产文件和测试文件均有明确执行步骤；计划同步只在 TODO 状态变化后执行。
  - execution_plan是否覆盖affected_scope所有文件？ → YES，step_1 覆盖 `.analysis-cache.md`，step_2 覆盖 migration，step_3 覆盖 reconstruct，step_4 覆盖测试，step_6 覆盖计划文件。
  - context是否充分？是否有未读但可能相关的文件？ → YES，对当前 fallback 实现充分；provider/native/SQLite 更深回归已在后续 TODO 中，不在本阶段伪装完成。若定向测试暴露 provider 或类型问题，再窄范围读取。
  - 是否有发现了但被我判断为"无关紧要"而跳过的问题？ → NO，metadata config/template/hashSheet 的完整语义没有被忽略；当前只映射能确定的表头和 sourceData，未知 config 不乱猜，这是为了避免错误兼容。
  - execution_plan中是否有步骤计划使用 shell 命令（sed/awk/perl）修改源代码？ → NO，源码修改使用 apply_diff/insert_code；shell 只用于 vitest/tsc/rollup 验证。
</analysis>


---
<analysis>
context: 当前进入 `legacy_native_fix_4`，目标是补 native provider 与 SQLite provider 回归覆盖。已读取 `native-table-service-adapter.ts:1-127` 和 `native-table-service-adapter.test.ts:1-230`：`NativeTableServiceAdapter.loadFromChat()` 只委托 `loadOrCreateJsonTableFromChatHistory_ACU()`，现有 native adapter 测试全部 mock table-service，仅验证委托和返回值转发；在这里塞真实 legacy fixture 没有意义，因为 helper/table-service 都被 mock 掉，做出来只会是假覆盖。已读取 `table-service.ts:412-438`：原生模式真正加载入口是 `loadOrCreateJsonTableFromChatHistory_ACU()`，它在聊天非空时调用 `mergeAllIndependentTables_ACU()`，如果有 mergedData 就 `_set_currentJsonTableData_ACU(mergedData)` 并返回 `source='merged'`，否则才初始化模板。已读取 `table-service.test.ts:463-523`：已有“有合并数据时返回 source=merged”测试，但 fixture 是单表 `sheet_0`，没有样本风格非连续 uid、summary/outline，也没有明确断言不会走初始化副作用。已读取 `sql-table-service.ts:58-145`：SQLite provider 使用 `mergeAllIndependentTablesWithMeta_ACU()`，`shouldLoadMigratedLegacySnapshot = mergeResult.usedLegacyMigration && hasAnyMergedSheet`，用于让 legacy migration 结果即使只有表头也进入 SQLite 加载。已读取 `sql-table-service.test.ts:324-415`：已有 header-only legacy migration 测试，但只覆盖 `inventory` 单表表头建表，没有覆盖样本风格历史行导入、非连续 sheet key、多表、summary/outline。已读取 `sql-table-service.test.ts:265-287`：测试数据通过 `sourceData.ddl` 创建 SQLite 表，`content[0]` 对应 SQL 列名，数据行写入 SQLite 后可 `executeQuery()` 断言。搜索 `usedLegacyMigration|loadFromChat|mergeAllIndependentTablesWithMeta_ACU` 确认 SQL 测试中 `mockMergeAllWithMeta` 可直接注入 migration meta，native adapter 只有委托测试，table-service 是 native 实际加载入口。
needs: 当前阶段需要补的是 provider 层“不会把已迁移旧数据当空数据初始化”的回归，而不是重复 migration/helper 测试。native 侧应在 `table-service.test.ts` 验证样本风格 mergedData 进入原生内存状态且不触发初始化；SQLite 侧应在 `sql-table-service.test.ts` 验证 usedLegacyMigration=true 的样本风格多表数据能真正建表并查询历史行，包括 summary/outline。
key_challenges: 第一，native adapter 自身只是委托，若在 `native-table-service-adapter.test.ts` 里加真实 legacy fixture，会被 mock 吃掉，测试无法击穿真实问题，这种覆盖是漂亮废纸。第二，`table-service.test.ts` 的 mergeAll 是 mock，因此它不能再次证明 reconstruct/helper 能读 legacy；它只能证明 native 加载入口收到样本风格 mergedData 后不会初始化空模板。第三，SQLite provider 的真正风险是 `hasRealDataRows`/`shouldLoadMigratedLegacySnapshot` 判断和 SyncBridge 建表写入；必须用 `sourceData.ddl` + content 历史行查询 SQLite，不能只看返回 `source='merged'`。第四，summary/outline 表在 SQLite 中需要合法英文 SQL 表名和列名；测试应通过 `uid` 与 DDL 使用 `summary_log`、`outline_log` 等稳定表名，避免中文表名或复杂映射把测试焦点带偏。
confidence: HIGH
  - 理由: native/table-service/SQLite 实现入口、已有测试 mock 结构、SQL 数据构造方式都已读取；新增测试位置和断言点明确，不需要修改生产代码。
approach: 三维评估综合最优的方案是只改两个测试文件：在 `table-service.test.ts` 的 `loadOrCreateJsonTableFromChatHistory_ACU` describe 中新增样本风格 mergedData 测试，验证 native 实际加载入口返回 `merged`、写入 currentJsonTableData、且不调用初始化相关副作用；在 `sql-table-service.test.ts` 的 `loadFromChat` describe 中新增 usedLegacyMigration=true 的样本风格多表导入测试，构造普通表、主角表、summary 表、outline 表，每张表有 DDL 和历史行，断言 SQLite 查询能读到历史行。这个方案不改 `native-table-service-adapter.test.ts`，因为该文件的委托 mock 无法提供真实链路价值。
  三维评分（每个维度 1-5 分，5 为最优）:
  - 可维护性: 5/5 — 新增测试落在真实责任边界：table-service 验证 native 实际入口，sql-table-service 验证 SQLite provider；不污染委托适配器测试。
  - 健壮性: 5/5 — 覆盖样本风格非连续 sheet key、summary/outline、多表历史行、legacy meta 导入、native 不初始化、SQLite 可查询历史行。
  - 可扩展性: 4/5 — 后续若 provider 加载策略调整，测试仍以业务可观察行为约束；未抽公共 fixture，避免为两处测试提前制造抽象层。
edge_cases:
  - native 加载入口收到样本风格 mergedData 时必须返回 `source='merged'`，不能继续调用 `initializeJsonTableInChatHistory_ACU()` 的模板初始化副作用。
  - native currentJsonTableData 必须保留非连续 sheet key 和 summary/outline 历史行，不能只保留 `sheet_0`。
  - SQLite provider 收到 `usedLegacyMigration=true` 且多表有历史行时必须返回 loaded=true/source=merged。
  - SQLite provider 必须能查询普通表和 summary/outline 表的历史行，证明不是只建立空 schema。
  - SQLite 新开卡和 header-only 既有行为不能被新增测试破坏。
affected_scope:
  - tests/service/table/table-service.test.ts
  - tests/service/table/sql-table-service.test.ts
  - .analysis-cache.md
  - .limcode/plans/legacy-native-read-compat-fix.md
execution_plan:
  - step_1: 将本 analysis 逐字追加写入 `.analysis-cache.md`。
  - step_2: 修改 `tests/service/table/table-service.test.ts`，在 `loadOrCreateJsonTableFromChatHistory_ACU` describe 中新增样本风格 native 加载测试，断言 merged、current state、summary/outline 历史行以及初始化副作用未触发。
  - step_3: 修改 `tests/service/table/sql-table-service.test.ts`，在 `loadFromChat` describe 中新增 usedLegacyMigration=true 的样本风格多表 SQLite 导入测试，构造 DDL 和历史行并查询普通表、summary 表、outline 表。
  - step_4: 运行 `npx vitest run tests/service/table/table-service.test.ts tests/service/table/native-table-service-adapter.test.ts tests/service/table/sql-table-service.test.ts tests/service/table/table-delta-reconstruct.test.ts tests/service/runtime/helpers-data-merge.test.ts`，确认 provider、migration、helper 回归通过；如失败按错误根因修复。
  - step_5: 测试通过后同步 TODO：`legacy_native_fix_4` 标记 completed，`legacy_native_fix_5` 标记 in_progress，并用 `update_plan(updateMode: progress_sync)` 同步计划。
degradation_check:
  - 方案是否是三维评估（可维护性、健壮性、可扩展性）综合最优的？ → YES，可维护性 5/5、健壮性 5/5、可扩展性 4/5；该方案把测试放在可击穿真实 provider 行为的位置，而不是在委托 mock 上堆数量。
  - 是否遗漏了已知边界条件？ → NO，覆盖 native 不初始化、SQLite 多表历史行、summary/outline、legacy meta、既有 header-only 行为回归。
  - 是否因改动量大而想缩减方案？ → NO，没有只跑已有测试或只改 SQL 单表；补 native/table-service 与 SQLite 两个 provider 边界。
  - 是否打算跳过某些文件？ → NO，明确不改 native adapter 测试是因为其 mock 结构无法验证真实 legacy 数据链路，不是回避；native 真实入口在 table-service 测试覆盖。
  - execution_plan是否覆盖affected_scope所有文件？ → YES，step_1 覆盖 cache，step_2/3 覆盖两个测试文件，step_5 覆盖计划文件。
  - context是否充分？是否有未读但可能相关的文件？ → YES，对当前 provider 回归充分；若测试失败，再窄范围读取 SyncBridge 或 SQL name mapper。
  - 是否有发现了但被我判断为"无关紧要"而跳过的问题？ → NO，native adapter 委托测试价值低已经基于源码和测试 mock 证据说明，并由 table-service 入口测试替代覆盖。
  - execution_plan中是否有步骤计划使用 shell 命令（sed/awk/perl）修改源代码？ → NO，源码修改使用 apply_diff；shell 只用于 vitest 验证。
</analysis>


---
<decision_point>
issue: provider 回归测试运行后 `tests/service/table/sql-table-service.test.ts` 出现 2 个失败。第一处是新增 SQLite 样本测试中 `SELECT location, current_time FROM global_state` 返回的 `current_time` 为运行时当前时间 `10:41:21`，而不是插入的 `20XX-09-25 14:30`；这说明 `current_time` 很可能触发 SQLite 的内置时间函数/关键字语义或列名解析歧义，测试 DDL 用词不安全。第二处是既有 `replaceCurrentData 收到空壳批次后保存时应把清空防线参数传给持久化层` 断言失败，收到 `['row_id','item_name','quantity']` 而非 `['row_id']`，需要读取该测试与相关导出/保存逻辑确认这是新增测试污染、现有测试顺序依赖暴露，还是生产行为已变化。这个问题在侦察/分析阶段没有预见到，因为我只确认了 SQL 测试用 DDL 可建表和查询，没检查 `current_time` 这种列名在 sql.js/SQLite 中是否会被函数语义影响；也没有读取 1200 行附近的 saveToChat 既有测试。
impact: YES。第一处影响新增 SQLite provider 测试的有效性，不能用 SQLite 保留字/函数名制造假失败；第二处影响整个 SQL 测试套件，不能只修新增测试就宣称 provider 回归通过。如果第二处是测试状态污染或生产行为变化，继续推进 `legacy_native_fix_4` 前必须定位。
context_update:
  - analysis.key_challenges 需要补充：SQLite 测试 DDL 的列名不仅要合法，还要避免 `current_time` 这类内置函数/关键字，查询时也应避免歧义列名。
  - analysis.execution_plan step_4 需要更新：失败后读取 `tests/service/table/sql-table-service.test.ts` 失败区域和 SQLite 映射/同步逻辑，再做精确修复。
  - analysis.edge_cases 需要补充：provider 回归测试不能引入会被 SQL 方言特殊处理的列名，否则测试验证的是 SQL 关键字行为，不是 legacy 数据导入。
options:
  - option_a:
      description: 只把新增测试里的 `current_time` 改成 `scene_time`，然后重跑测试；第二个失败先不处理。
      approach_evaluation: 可维护性 2/5，因为只处理新增测试的明显列名问题；健壮性 1/5，因为既有测试失败仍未解释，可能存在状态污染或生产保存语义变化；可扩展性 2/5，因为后续仍会被同类测试顺序问题击穿。
      edge_cases: 既有 saveToChat 失败可能继续存在；如果是新增测试污染导致的，未清理会影响后续测试可靠性。
      affected_scope_delta: 只改 `tests/service/table/sql-table-service.test.ts` 新增测试局部，但缩小问题处理范围。
  - option_b:
      description: 读取 `tests/service/table/sql-table-service.test.ts` 失败断言附近、`sync-bridge`/schema mapper 的列映射逻辑；将新增测试列名从 `current_time` 改为非关键字 `scene_time`；同时定位第二个失败是新增测试状态污染还是既有测试断言与当前生产行为不一致，并按证据修复测试隔离或断言。
      approach_evaluation: 可维护性 5/5，因为先用源码证据区分测试设计问题与生产行为；健壮性 5/5，因为同时处理新增测试和既有失败，不跳过红灯；可扩展性 4/5，因为避免保留字列名，并强化测试套件隔离。
      edge_cases: 需要确认 `replaceCurrentData` 相关失败是否与新增测试共享 mock/engine 状态有关；如果是生产行为变化，需要判断是否在本任务范围内修生产代码还是修测试预期。
      affected_scope_delta: 继续涉及 `tests/service/table/sql-table-service.test.ts`，可能需要读取 `src/data/sqlite/sync-bridge.ts` 和 `src/data/sqlite/schema-mapper.ts`，不新增生产修改除非证据显示生产缺陷。
  - option_c:
      description: 删除新增 SQLite 多表测试，只保留 native/table-service 测试和既有 SQL header-only legacy 测试，认为 provider 回归已经足够。
      approach_evaluation: 可维护性 1/5，因为会丢掉本阶段最关键的 SQLite 多表历史行覆盖；健壮性 1/5，因为 summary/outline 和多表导入缺口仍存在；可扩展性 1/5，因为后续 SQL provider 兼容性回归没有样本级保护。
      edge_cases: 用户样本在 SQLite 模式下仍可能导入失败而测试不报警。
      affected_scope_delta: 缩小 `legacy_native_fix_4` 覆盖范围，删除新增高价值测试。
recommendation: 选择 option_b。三维评估显示 option_b 可维护性 5/5、健壮性 5/5、可扩展性 4/5，综合最优。它处理真实失败，不把测试问题掩盖成“差不多通过”；也不删除 SQLite 多表覆盖。
execution_plan_update:
  - step_4a: 读取 `tests/service/table/sql-table-service.test.ts` 新增测试区域和失败的 saveToChat 测试区域，读取 SQLite sync/schema 映射相关实现，确认 `current_time` 与第二个失败根因。
  - step_4b: 若 `current_time` 是列名歧义，修改新增测试 DDL/content/query 为 `scene_time`；若第二个失败是新增测试状态污染，补充测试隔离；若第二个失败是既有断言与当前生产行为不一致，基于生产语义修断言或生产代码。
  - step_4c: 重跑 `npx vitest run tests/service/table/sql-table-service.test.ts`，再重跑完整 `legacy_native_fix_4` 定向集合。
deviation_audit:
  original_plan_excerpt:
    - step_4: 运行 `npx vitest run tests/service/table/table-service.test.ts tests/service/table/native-table-service-adapter.test.ts tests/service/table/sql-table-service.test.ts tests/service/table/table-delta-reconstruct.test.ts tests/service/runtime/helpers-data-merge.test.ts`，确认 provider、migration、helper 回归通过；如失败按错误根因修复。
  current_proposal:
    - step_4a: 读取 `tests/service/table/sql-table-service.test.ts` 新增测试区域和失败的 saveToChat 测试区域，读取 SQLite sync/schema 映射相关实现，确认 `current_time` 与第二个失败根因。
    - step_4b: 若 `current_time` 是列名歧义，修改新增测试 DDL/content/query 为 `scene_time`；若第二个失败是新增测试状态污染，补充测试隔离；若第二个失败是既有断言与当前生产行为不一致，基于生产语义修断言或生产代码。
    - step_4c: 重跑 `npx vitest run tests/service/table/sql-table-service.test.ts`，再重跑完整 `legacy_native_fix_4` 定向集合。
  diff_summary:
    - 保留原计划“失败按根因修复”的意图。
    - 新增读取失败区域和 SQLite 映射实现的动作。
    - 新增对 SQL 列名歧义和测试隔离/生产语义的区分处理。
  deviation_motive_check:
    - **措辞替换规则逐类检查**：未命中需要自我剖析的降级措辞；推荐方案基于三维评分，没有使用“务实”“折中”“更简单”等降级表达。
    - 偏离后的方案在三维评分上是否低于原方案？→ NO。原方案要求失败按根因修复，当前方案只是把根因定位步骤展开，可维护性和健壮性更高。
    - 偏离是否导致 affected_scope 缩小？→ NO。没有删除 SQLite 多表测试，反而增加必要读取确认。
  self_dissection: 未触发。
degradation_check:
  - 推荐方案是否是三维评估（可维护性、健壮性、可扩展性）综合最优的？ → YES，option_b 为 5/5、5/5、4/5，高于 option_a 和 option_c。
  - 推荐方案是否遗漏了新发现的边界条件？ → NO，覆盖 SQL 列名歧义、测试状态污染、既有测试失败三类新问题。
  - 是否因为想尽快完成而选择了改动量小的方案？ → NO，没有选择只改新增测试或删除测试，而是先定位两个失败。
  - 修改后的execution_plan是否仍覆盖所有affected_scope？ → YES，继续覆盖 SQL 测试、analysis cache、计划同步；如证据显示生产代码缺陷再扩大 affected_scope。
  - 是否有发现了但被判断为"无关紧要"而跳过的问题？ → NO，既有 saveToChat 失败没有被跳过。
  - options 是否包含至少三个方案？ → YES，包含 option_a、option_b、option_c。
  - 是否因为工具报错而准备换用 shell 命令修改源代码？ → NO，继续使用 read_file/apply_diff；shell 只用于测试。
  - deviation_audit 是否触发了 self_dissection？ → NO。
</decision_point>


---
<analysis>
context: 已读取 `src/data/sqlite/sync-bridge.ts:170-277`，确认 `_exportSheet(tableName, meta)` 通过 `SELECT * FROM ${tableName}` 查询数据，再用 `meta.sourceData.ddl` 或运行时 DDL 提取列名映射和 fallbackColumns，最终调用 `resultToContent(queryResult.columns, queryResult.values, sqlToChinese, fallbackColumns)` 生成 `content`。这意味着只要 SQLite 表真实存在且 DDL 中有 `row_id/item_name/quantity` 三列，即使没有数据行，导出结果也应保留完整表头，而不是退化为 `[['row_id']]`。已读取 `tests/service/table/sql-table-service.test.ts:417-551`，新增 SQLite provider 多表测试只在 3 处使用 `current_time`：DDL 第 440 行、content 表头第 444 行、查询第 534 行；搜索结果也确认 `current_time` 只出现这 3 处。已读取 `tests/service/table/sql-table-service.test.ts:1188-1208`，失败断言期望 `options.afterData.sheet_0.content` 为 `[['row_id']]`，但同文件第 1193 行构造的 `headerOnlyData.sheet_0.content` 是 `[['row_id', 'item_name', 'quantity']]`。已读取 `.analysis-cache.md:318-374` 的 decision_point，当前推荐方案是 option_b：先读取失败区域和 SQLite 映射/同步逻辑，再把 `current_time` 改为 `scene_time`，并基于证据判断第二个失败应修隔离、断言还是生产代码。
needs: 当前需要完成 `legacy_native_fix_4` 的 provider 回归测试闭环：新增 SQLite 多表测试必须避免 SQL 方言关键字/函数列名造成假失败；既有 saveToChat 测试必须与当前 SQLite 导出语义一致，不能用错误断言制造红灯，也不能为了让测试绿而改坏生产导出逻辑。修复后需要先跑 `tests/service/table/sql-table-service.test.ts`，再跑包含 table-service/native-adapter/sql-table-service/table-delta-reconstruct/helpers-data-merge 的定向集合。
key_challenges: 第一，`current_time` 在 SQLite 查询中会表现为当前时间函数/关键字语义，新增测试验证的是 SQL 名称歧义而不是 legacy 数据导入。第二，header-only 数据进入 SQLite 后并不是“没有列”，而是“有 schema、无数据行”；`_exportSheet` 的职责是保留表结构，因此导出完整表头是合理生产语义。第三，不能把第二个失败简单归咎于新增测试污染；当前读取到的源码和测试构造已能证明断言 `[['row_id']]` 与 header-only 三列表结构不一致。
confidence: HIGH
  - HIGH: 已读取失败测试局部、SQLite 导出实现和 decision_point；`current_time` 的出现范围已通过搜索确认；header-only 导出完整表头的证据来自 `_exportSheet` 调用链和测试自身构造数据。
approach: 三维评估综合最优的方案是只修改 `tests/service/table/sql-table-service.test.ts`：把新增 SQLite 多表测试中的 `current_time` 全部改为非关键字 `scene_time`；把既有 saveToChat header-only 断言从 `[['row_id']]` 修正为 `[['row_id', 'item_name', 'quantity']]`，保持与 `replaceCurrentData(headerOnlyData)` 的输入表头和 `_exportSheet` 的结构保留语义一致。不修改生产代码，因为当前证据显示生产导出完整表头是正确行为。
  三维评分（每个维度 1-5 分，5 为最优）：
  - 可维护性: 5/5 — 修改集中在测试文件，列名改为明确业务名，断言与测试输入和导出语义一致，后续维护者不用解释为什么三列表头会期望成单列。
  - 健壮性: 5/5 — 避免 SQLite 内置名称歧义造成假失败，同时保留 header-only 表结构的回归保护；不通过改生产代码迎合错误断言。
  - 可扩展性: 4/5 — 后续类似 SQLite provider 测试可沿用“避免关键字列名”和“header-only 保留完整 schema”的规则；扣 1 分是因为没有新增通用保留字检测工具，但当前任务范围不需要引入额外抽象。
edge_cases:
  - SQLite 测试列名不能使用 `current_time` 这种可能被解析为内置时间值的名称，否则 `SELECT location, current_time FROM global_state` 可能返回当前时间而非插入值，测试会验证错误对象。
  - header-only 表不是无 schema 表；当 `content` 表头包含 `row_id/item_name/quantity` 且 DDL 有三列时，保存导出的 `afterData` 应保留完整表头，不能退化为只剩 `row_id`。
  - 修改断言后仍需要通过 SQL 测试确认不是 mock 泄漏或测试顺序导致的偶发行为；如果 SQL 单文件仍失败，再进入新的 decision_point 分析。
affected_scope:
  - tests/service/table/sql-table-service.test.ts
  - .analysis-cache.md
  - .limcode/plans/legacy-native-read-compat-fix.md（仅在 TODO 状态变化后 progress_sync）
execution_plan:
  - step_1: 使用 apply_diff 精确修改 `tests/service/table/sql-table-service.test.ts` 新增 SQLite 多表测试，把 DDL、content 表头、查询中的 `current_time` 改为 `scene_time`。
  - step_2: 使用 apply_diff 精确修改 `tests/service/table/sql-table-service.test.ts:1206`，把 header-only saveToChat 测试的 `afterData.sheet_0.content` 期望改为 `[['row_id', 'item_name', 'quantity']]`。
  - step_3: 运行 `npx vitest run tests/service/table/sql-table-service.test.ts`，确认 SQL provider 单文件测试通过；若失败，读取失败输出并按根因处理，不跳过。
  - step_4: 运行 `npx vitest run tests/service/table/table-service.test.ts tests/service/table/native-table-service-adapter.test.ts tests/service/table/sql-table-service.test.ts tests/service/table/table-delta-reconstruct.test.ts tests/service/runtime/helpers-data-merge.test.ts`，确认 `legacy_native_fix_4` 定向集合通过。
  - step_5: 定向集合通过后，更新 TODO：`legacy_native_fix_4` completed，`legacy_native_fix_5` in_progress，并用 `update_plan(updateMode: progress_sync)` 同步 `.limcode/plans/legacy-native-read-compat-fix.md`。
degradation_check:
  - 方案是否是三维评估（可维护性、健壮性、可扩展性）综合最优的？ → YES。测试局部修正可维护性 5/5、健壮性 5/5、可扩展性 4/5；相比修改生产代码迎合错误断言，风险更低且证据更充分。
  - 是否遗漏了已知边界条件？ → NO。覆盖 SQLite 列名歧义、header-only schema 保留、SQL 单文件验证和完整定向集合验证。
  - 是否因改动量大而想缩减方案？ → NO。没有删除新增 SQLite 多表测试，也没有跳过既有失败断言。
  - 是否打算跳过某些文件？ → NO。当前 affected_scope 只有测试文件、analysis cache 和后续计划同步；生产代码已读取并判定无需修改。
  - execution_plan是否覆盖affected_scope所有文件？ → YES。step_1/step_2 覆盖测试文件，当前分析块写入覆盖 `.analysis-cache.md`，step_5 覆盖计划同步。
  - context是否充分？是否有未读但可能相关的文件？ → YES，context 充分。`schema-mapper.ts` 先前已读到 `resultToContent` 只有列数为 0 才返回 `[['row_id']]`，本轮又读取了 `_exportSheet` 调用链；当前修改不需要继续扩大读取范围。
  - 是否有发现了但被我判断为"无关紧要"而跳过的问题？ → NO。两个失败点都进入处理路径；TypeScript `baseUrl` 诊断属于当前工作区既有配置问题，后续 `tsc --noEmit` 阶段会以实际输出处理。
  - execution_plan中是否有步骤计划使用 shell 命令（sed/awk/perl）修改源代码？ → NO。修改使用 apply_diff；shell 只用于 vitest/tsc/rollup 验证。
</analysis>


---
<analysis>
context: 已读取 `src/service/table/table-delta-reconstruct.ts:1-220`，当前重建逻辑在扫描到任意 V2 checkpoint 后设置 `sawV2Checkpoint=true`，循环结束后第 101-109 行直接返回；返回时只用 `hasAnySheet_ACU(data)` 决定 data 是否为 null，但不会继续 legacy migration 或 chat_metadata fallback。已读取 `src/service/table/table-delta-migration.ts:329-439`，`findFirstWritableAiMessageIndex_ACU()` 当前只判断 `message && !message.is_user`，`migrateLegacyCheckpointToRootMessage_ACU()` 也只排除 `anchorMessage.is_user`，因此真实 JSONL 根对象如果没有 `is_user` 字段且包含 `chat_metadata`，会被误判为可写 AI 消息。已读取 `tests/service/table/table-delta-reconstruct.test.ts:400-605`，两个关键 fixture 都把根 `chat_metadata` 对象写成 `is_user: true`，这确实掩盖了真实根 metadata 无 `is_user` 的风险。已读取 `src/shared/models/table-persistence-v2-utils.ts:45-68`，`getTablePersistenceDeltasV2_ACU()` 只返回合法 delta，`hasTablePersistenceDeltasV2_ACU()` 只在 delta 涉及 sheet 时为 true；同文件第 156-160 行已有私有 `hasCheckpointSheets_ACU` 语义，即 checkpoint 是否含 sheet 取决于 `checkpoint.data` 是否有 `sheet_` key。已读取验收专家结果：当前有两个 high blocker 和一个 medium blocker，不能发布、不能 tag、不能推送。
needs: 本阶段需要修复验收阻塞项，而不是继续包装已有绿灯。必须让空/无 sheet V2 checkpoint 不永久遮蔽有效 legacy，同时有效 V2 checkpoint 仍保持最高优先级；必须让 root `chat_metadata` 对象不会被当作 migration checkpoint 的可写 AI anchor；必须把 `chat_metadata.sheets` fallback 从硬编码 `chat[0]` 调整为扫描安全范围内的 metadata 候选，并补测试证明 root metadata 不在第 0 位时也能触发 fallback，且 root metadata 不被写入。
key_challenges: 第一，V2 checkpoint 优先级不能粗暴删除，否则会破坏有效 V2 优先；应只在 V2 回放后实际含 sheet 数据时返回。第二，如果 V2 checkpoint 是空壳且没有有效 sheet delta，需要允许 legacy 扫描到完整 `endExclusive - 1`，否则仍会因为 `firstV2MessageIndex - 1` 把后续有效 legacy 排除。第三，AI 可写判断不能只用 `!is_user`，但也不能误杀旧消息；合理边界是排除“含 `chat_metadata` 且缺少 `is_user/mes/swipes/name` 等聊天消息特征、也没有 TavernDB_ACU_* 表数据字段”的 root metadata 对象。第四，metadata fallback 扫描不能把 root metadata 当 AI 写入目标，扫描 metadata 来源和选择写入 anchor 必须分离。
confidence: HIGH
  - HIGH: 阻塞问题已有验收专家证据，本轮已读取对应生产代码、测试 fixture、V2 工具函数与 checkpoint 写入逻辑；修复点集中且边界清晰。
approach: 三维评估综合最优的方案是：在 `table-delta-migration.ts` 增加可复用的 `isWritableAiMessage_ACU()` 判定，排除 root-only chat metadata 对象，并让 `findFirstWritableAiMessageIndex_ACU()` 和 `migrateLegacyCheckpointToRootMessage_ACU()` 共用该判定；在 `table-delta-reconstruct.ts` 增加 metadata sheets 扫描 helper，并把 V2 checkpoint 早返回改为“只有回放后存在 sheet 数据才早返回”，否则继续 legacy fallback，且 legacy boundary 在无有效 V2 sheet 数据时放宽到 `endExclusive - 1`；在 `table-delta-reconstruct.test.ts` 修改 root metadata fixture 去掉 `is_user: true`，并新增空 V2 checkpoint + 有效 legacy、有效 V2 checkpoint + legacy 不污染、root metadata 非第 0 位 fallback 等测试。
  三维评分（每个维度 1-5 分，5 为最优）：
  - 可维护性: 5/5 — 可写 AI 判定集中在 migration 层，reconstruct 只处理优先级和 fallback 来源；测试直接对应验收专家 blocker，后续维护者能看懂边界。
  - 健壮性: 5/5 — 覆盖空 V2 遮蔽、有效 V2 优先、root metadata 写错 anchor、metadata 非第 0 位四个生产边界，不靠 fixture 化妆。
  - 可扩展性: 4/5 — metadata 候选扫描和可写消息判定可复用；扣 1 分是因为 root metadata 与真实消息的判定仍依赖当前 SillyTavern 消息形态特征，而不是由宿主 API 明确分离根 metadata。
edge_cases:
  - 空 checkpoint 的 `data` 只有 `mate` 或无 `sheet_` key 时，不能阻止后续 `TavernDB_ACU_Data` legacy migration。
  - 有效 V2 checkpoint 回放后含 `sheet_` 数据时，仍必须直接返回，不能被更早或更晚 legacy 覆盖。
  - 根 `chat_metadata` 对象没有 `is_user` 字段时，不能被 `findFirstWritableAiMessageIndex_ACU()` 或 `migrateLegacyCheckpointToRootMessage_ACU()` 当作写入目标。
  - 根 `chat_metadata` 不在 `chat[0]` 但仍在扫描范围内时，metadata fallback 应能读取；写入 checkpoint 时仍应写入后续可写 AI 消息。
  - 如果只有 root metadata 而没有任何可写 AI 消息，fallback 可以返回数据和 checkpoint，但 `changed` 应保持 false，不应写入 root metadata。
affected_scope:
  - src/service/table/table-delta-migration.ts
  - src/service/table/table-delta-reconstruct.ts
  - tests/service/table/table-delta-reconstruct.test.ts
  - .analysis-cache.md
  - .limcode/plans/legacy-native-read-compat-fix.md（TODO 状态变化后同步）
execution_plan:
  - step_1: 修改 `src/service/table/table-delta-migration.ts`，新增 root metadata 判定和 `isWritableAiMessage_ACU()`，让 `findFirstWritableAiMessageIndex_ACU()` 与 `migrateLegacyCheckpointToRootMessage_ACU()` 使用统一判定，避免把真实根 metadata 写入 V2 checkpoint。
  - step_2: 修改 `src/service/table/table-delta-reconstruct.ts`，新增 metadata sheets 候选扫描函数；将 V2 checkpoint 早返回条件改为仅在回放后 `hasAnySheet_ACU(data)` 为 true 时返回；当 V2 checkpoint 无有效 sheet 数据时继续 legacy migration，并把 legacy boundary 放宽到 `endExclusive - 1`。
  - step_3: 修改 `tests/service/table/table-delta-reconstruct.test.ts`，去掉 root metadata fixture 的 `is_user: true` 化妆；新增测试覆盖空 V2 checkpoint + 有效 legacy fallback、有效 V2 checkpoint 仍优先、root metadata 非第 0 位仍能 fallback 且不写入 root。
  - step_4: 运行 `npx vitest run tests/service/table/table-delta-reconstruct.test.ts`，若失败按失败输出定位修复。
  - step_5: 运行 `npx vitest run tests/service/table/table-service.test.ts tests/service/table/native-table-service-adapter.test.ts tests/service/table/sql-table-service.test.ts tests/service/table/table-delta-reconstruct.test.ts tests/service/runtime/helpers-data-merge.test.ts`，确认旧 native 兼容定向集合仍通过。
  - step_6: 重新运行 `npx vitest run`、`npx tsc --noEmit`、`npx rollup -c`，并再次调用验收专家复查，直到无阻塞问题。
degradation_check:
  - 方案是否是三维评估（可维护性、健壮性、可扩展性）综合最优的？ → YES。该方案直接修两个 high blocker，并补 medium blocker 的契约测试；可维护性 5/5、健壮性 5/5、可扩展性 4/5，高于只补测试或只改早返回。
  - 是否遗漏了已知边界条件？ → NO。覆盖空 V2、有效 V2、root metadata 无 is_user、metadata 不在 chat[0]、无可写 AI 时不写 root 的边界。
  - 是否因改动量大而想缩减方案？ → NO。没有把 blocker 降级为建议，也没有跳过验收专家指出的 medium 风险。
  - 是否打算跳过某些文件？ → NO。affected_scope 包含两个生产文件、一个测试文件、analysis cache 和计划同步。
  - execution_plan是否覆盖affected_scope所有文件？ → YES。step_1 覆盖 migration，step_2 覆盖 reconstruct，step_3 覆盖测试，当前 analysis 写入 `.analysis-cache.md`，TODO 变化后同步计划。
  - context是否充分？是否有未读但可能相关的文件？ → YES，context 充分。V2 类型和工具、checkpoint 写入、reconstruct 优先级、测试 fixture 都已读取；不需要读取 provider 文件才能修当前 blocker。
  - 是否有发现了但被我判断为"无关紧要"而跳过的问题？ → NO。隔离开启 orphan fallback 是验收专家明确的非本次发布必须项，用户现场无隔离；但不能在本次仓促放宽隔离读取。
  - execution_plan中是否有步骤计划使用 shell 命令（sed/awk/perl）修改源代码？ → NO。修改使用 apply_diff；shell 只用于测试、类型检查和构建。
</analysis>


---
<analysis>
context: 已确认第二轮验收专家结论为 accepted，无 blockingFindings，并明确允许继续 `legacy_native_fix_7` 发布步骤。当前 TODO 仍显示 `legacy_native_fix_6` 为 in_progress、`legacy_native_fix_7` 为 pending，需要同步为 `legacy_native_fix_6` completed、`legacy_native_fix_7` in_progress。当前 `git status --short` 显示变更包括 `.analysis-cache.md`、`.limcode/progress.md`、`dist/index.bundle.js`、`src/service/table/table-delta-migration.ts`、`src/service/table/table-delta-reconstruct.ts`、`tests/service/runtime/helpers-data-merge.test.ts`、`tests/service/table/sql-table-service.test.ts`、`tests/service/table/table-delta-reconstruct.test.ts`、`tests/service/table/table-service.test.ts`，以及未跟踪 `.limcode/plans/legacy-native-read-compat-fix.md` 和用户样本 JSONL 文件。已确认 `dist/index.bundle.js` 与 `index.js` SHA256 不一致，发布步骤需要将构建产物覆盖到 `index.js`。当前验证结果已由第二轮验收专家确认：定向 `table-delta-reconstruct` 19 passed，旧 native 兼容定向集合 220 passed，全量 vitest 157 files / 3093 tests passed，`tsc --noEmit` exitCode 0，`rollup -c` 成功生成 `dist/index.bundle.js`。
needs: 当前需要完成发布收尾：同步 TODO/计划状态、输出并归档质量验收报告、覆盖 `index.js`、确认变更范围、提交、打 `spv4.6.4` tag、推送 main 与 tag。必须避免把用户真实 JSONL 样本误提交进仓库；该文件是任务证据，不是源码产物，提交它会泄露用户样本内容并污染发布包。
key_challenges: 第一，发布步骤涉及提交、tag、push，属于共享状态操作，必须严格限制在用户原始发布要求范围内。第二，当前工作区存在未跟踪真实 JSONL 样本，不能无脑 `git add .`，否则就是把用户数据送进仓库，事故味浓得刺鼻。第三，`.analysis-cache.md` 按流程需要归档到 `.analysis-archive/`，归档后主目录不应继续保留临时缓存。第四，`index.js` 必须由当前已验证的 `dist/index.bundle.js` 覆盖，不能发布旧 bundle。
confidence: HIGH
  - HIGH: 发布前验证链完整，验收专家 accepted；当前剩余工作是明确的发布流程和文件范围控制，没有未解释的测试红灯。
approach: 三维评估综合最优的方案是按最小发布路径执行：先同步 TODO/计划；输出 `<output_quality_review>` 并追加到 `.analysis-cache.md`；归档 `.analysis-cache.md` 到新的 `.analysis-archive/` 文件；复制 `dist/index.bundle.js` 到 `index.js`；用 `git diff/status` 确认不包含用户样本 JSONL；只 add 需要发布的源码、测试、构建产物、计划/归档文件；提交、tag、push。
  三维评分（每个维度 1-5 分，5 为最优）：
  - 可维护性: 5/5 — 发布提交包含源码、测试、构建产物和归档证据，排除用户样本；后续复盘能通过归档和测试定位改动意图。
  - 健壮性: 5/5 — 已有全量验证和验收专家 accepted；发布前再检查 git 范围，避免把敏感样本或临时文件误提交。
  - 可扩展性: 4/5 — 当前流程适合本次发布；扣 1 分是因为 `.limcode/progress.md` 是否纳入版本管理需要以实际 git 状态确认，不能凭惯例强行提交。
edge_cases:
  - 未跟踪真实 JSONL 样本不能提交。
  - `index.js` 覆盖后必须与 `dist/index.bundle.js` hash 一致。
  - tag `spv4.6.4` 如果已存在，不能强制覆盖，应停下来报告冲突。
  - push 失败时不能重复蛮力重试，应读取错误原因。
  - 归档 `.analysis-cache.md` 时必须创建新文件名，不能覆盖已有 archive。
affected_scope:
  - .analysis-cache.md
  - .analysis-archive/
  - .limcode/plans/legacy-native-read-compat-fix.md
  - .limcode/progress.md（仅如确认属于本任务状态记录）
  - index.js
  - dist/index.bundle.js
  - src/service/table/table-delta-migration.ts
  - src/service/table/table-delta-reconstruct.ts
  - tests/service/table/table-delta-reconstruct.test.ts
  - tests/service/table/table-service.test.ts
  - tests/service/table/sql-table-service.test.ts
  - tests/service/runtime/helpers-data-merge.test.ts
execution_plan:
  - step_1: 更新 TODO：`legacy_native_fix_6` completed，`legacy_native_fix_7` in_progress，并同步 `.limcode/plans/legacy-native-read-compat-fix.md`。
  - step_2: 读取 `.analysis-cache.md` 完整尾部后输出 `<output_quality_review>`，追加到 `.analysis-cache.md`，然后归档为 `.analysis-archive/{timestamp}_旧原生模式读取兼容修复.md`。
  - step_3: 复制 `dist/index.bundle.js` 覆盖 `index.js`，校验两个文件 SHA256 一致。
  - step_4: 检查 `git status` 和必要 diff，明确排除真实 JSONL 样本，只暂存本任务需要的源码、测试、构建产物、计划和归档文件。
  - step_5: 提交 `spv4.6.4: 修复旧原生模式记录读取兼容`，创建 tag `spv4.6.4`，推送当前分支和 tag。
  - step_6: 发布完成后同步 TODO：`legacy_native_fix_7` completed，并同步计划/进度；如推送失败则按错误原因处理，不伪装完成。
degradation_check:
  - 方案是否是三维评估（可维护性、健壮性、可扩展性）综合最优的？ → YES。该方案保留验证证据、控制提交范围、覆盖 bundle 并执行用户要求的 tag/push，三维评分 5/5、5/5、4/5。
  - 是否遗漏了已知边界条件？ → NO。覆盖样本不提交、hash 校验、tag 冲突、push 失败和归档不可覆盖。
  - 是否因改动量大而想缩减方案？ → NO。不会跳过归档、不会跳过 index.js 覆盖、不会只提交源码不提交构建产物。
  - 是否打算跳过某些文件？ → NO。会先用 git 状态确认文件范围；明确跳过真实 JSONL 样本是数据保护，不是缩减质量。
  - execution_plan是否覆盖affected_scope所有文件？ → YES。归档、计划、源码、测试、构建产物、index.js 和 git 提交均覆盖。
  - context是否充分？是否有未读但可能相关的文件？ → YES，当前发布流程上下文充分；如 `git diff` 显示异常变更，再做局部读取。
  - 是否有发现了但被我判断为"无关紧要"而跳过的问题？ → NO。TypeScript baseUrl 诊断已由 `tsc --noEmit` 实际通过证明当前 TS 配置未阻塞发布；后续可独立迁移，不属于本次旧 native 兼容发布阻塞。
  - execution_plan中是否有步骤计划使用 shell 命令（sed/awk/perl）修改源代码？ → NO。shell 用于 git、hash、复制构建产物、提交推送；不会用 sed/awk/perl 修改源码。
</analysis>

---
<output_quality_review>
task_summary: 修复旧原生模式记录读取兼容：消息级 TavernDB_ACU_Data 可迁移历史行，chat_metadata.sheets 仅作为无消息级数据时的表头 fallback，空/错误 V2 checkpoint 不再永久遮蔽有效 legacy，有效 V2 checkpoint 仍保持最高优先级，root chat_metadata 对象不再被当作可写 AI 消息；已完成测试、类型检查、构建与验收专家复查，当前进入发布归档、入口 bundle 覆盖、提交、tag、推送阶段。
deliverables:
  - src/service/table/table-delta-migration.ts：新增/调整旧 metadata 转换、summary/outline 标准容器合并、root metadata-only 判定、可写 AI 消息判定与 migration anchor 写入保护。
  - src/service/table/table-delta-reconstruct.ts：接入 chat_metadata.sheets fallback，调整空 V2 checkpoint 早返回与 legacy boundary，扫描 metadata 候选而非硬编码 chat[0]。
  - tests/service/table/table-delta-reconstruct.test.ts：补充真实样本风格 migration、空 V2 checkpoint fallback、有效 V2 优先、root metadata 非第 0 位 fallback、无可写 AI 消息不写 root 等回归。
  - tests/service/runtime/helpers-data-merge.test.ts：补充 helper 层运行时聊天数组到 legacy standard data 的样本风格合并测试。
  - tests/service/table/table-service.test.ts：补充 native 实际加载入口收到样本风格 mergedData 时返回 merged 且不初始化空模板的测试。
  - tests/service/table/sql-table-service.test.ts：补充 SQLite provider 多表 legacy migration 导入与查询测试；修正 current_time 列名歧义为 scene_time；修正 header-only 导出完整表头断言。
  - tests/service/table/native-table-service-adapter.test.ts：未修改，已确认其职责只是委托，真实 native 加载行为由 table-service 测试覆盖。
  - dist/index.bundle.js：rollup 构建产物。
  - index.js：发布入口文件，待由 dist/index.bundle.js 覆盖后纳入发布。
  - .limcode/plans/legacy-native-read-compat-fix.md：计划与 TODO 状态记录。
  - .analysis-cache.md / .analysis-archive/：本次分析、决策与验收链路归档。

# 量化指标总览
metrics:
  total_files_modified: 11 — 生产文件 2 个、测试文件 4 个、构建/入口产物 2 个、计划/进度/分析归档 3 个；用户真实 JSONL 样本明确排除，不纳入提交。
  execution_plan_coverage: 6/6 = 100% — 已完成旧 native 复现、helper/provider 链路确认、最小兼容修复、migration/helper/native/SQLite 回归、定向/全量验证、验收专家复查；剩余发布动作正在按本验收报告后的步骤执行。
  edge_cases_handled: 10/10 = 100% — 非连续 uid sheet key、content[0][0] 为 null 的 row_id 迁移、summary/outline 位于 TavernDB_ACU_Data、template key 不匹配 fallback、消息级数据优先于 metadata fallback、空 V2 checkpoint 不遮蔽 legacy、有效 V2 checkpoint 优先、root metadata 不写 checkpoint、SQLite 多表导入、header-only 完整表头保留均已覆盖。
  confidence_assessment: HIGH — 对本次交付质量的整体置信度为 HIGH。
    - HIGH: 目标链路已有定向测试、provider 回归、全量 vitest、tsc、rollup 和验收专家 accepted 作为证据；当前发布前唯一需要继续控制的是 git 暂存范围与 tag/push 结果。

# 产物实质性检查
substance_check:
  - 产物中是否存在"形式完整但实质空洞"的内容？
    → NO。src/service/table/table-delta-migration.ts 的变更直接影响 legacy 数据读取、summary/outline 合并、metadata fallback 转换与可写 AI anchor 判定；删除这些变更会重新导致用户样本历史行丢失或 root metadata 被写入。src/service/table/table-delta-reconstruct.ts 的变更直接影响 V2/legacy 优先级与 fallback 编排；删除会恢复空 V2 checkpoint 遮蔽和 chat[0] 硬编码。四个测试文件分别击穿 migration、helper、native load、SQLite load，不是只看函数是否被调用。dist/index.bundle.js 与 index.js 是发布实际载体，hash 一致性会在发布前校验。计划与分析归档提供可追溯决策链，删除会降低复盘能力。
  - 产物是否能被其目标对象（被测代码/被重构模块/被修复的bug）的变化所"击穿"？
    → YES。若 migration 不再读取消息级 TavernDB_ACU_Data 或不做 row_id 迁移，table-delta-reconstruct 测试会失败；若 summary/outline 仍从标准容器丢弃，reconstruct/helper 测试会失败；若空 V2 checkpoint 仍早返回，新增空 V2 测试会失败；若 root metadata 被当可写 AI，root metadata anchor 测试会失败；若 native load 退回初始化，table-service 测试会失败；若 SQLite 不导入历史行，多表查询断言会失败。
  - 实质性比率: 11/11 = 100%。所有产物都对应生产行为、验证行为、发布载体或审计记录；没有用 trivial 文件凑数量。

# 覆盖完整性检查
completeness_check:
  - 是否存在被跳过的模块/函数/路径？
    → NO。native-table-service-adapter.ts 未修改不是跳过，而是基于源码证据确认其只做委托；真实 native 加载入口在 table-service 测试覆盖。隔离开启 orphan fallback 未纳入本次发布，是因为用户明确隔离码为空且全局放宽隔离会引入跨隔离串读风险，该事项应作为独立低优先级保护项处理，而不是混入本次修复。
  - 产物覆盖的范围是否与 execution_plan 中 affected_scope 完全一致？
    → YES。affected_scope 中的 migration、reconstruct、reconstruct 测试、helper 测试、native/table-service 测试、SQLite 测试、bundle、index、计划和分析归档均已覆盖；.limcode/progress.md 当前为状态记录变更，暂存前会通过 git status/diff 再确认是否纳入。
  - 核心业务逻辑是否都有直接验证（不依赖间接覆盖）？
    → YES。消息级 legacy migration 由 table-delta-reconstruct.test.ts 直接验证；helper 运行时合并由 helpers-data-merge.test.ts 直接验证；native loaded/merged 行为由 table-service.test.ts 直接验证；SQLite legacy 导入由 sql-table-service.test.ts 直接查询验证；V2 优先级与空 V2 fallback 由 table-delta-reconstruct.test.ts 直接验证；root metadata 不写 checkpoint 由 table-delta-reconstruct.test.ts 直接验证。
  - affected_scope 覆盖率: 11/11 = 100%。

# 价值密度检查
value_density_check:
  - 产物中高价值内容（验证核心逻辑/处理复杂场景）与低价值内容（验证trivial行为）的比例是多少？
    → 高价值:低价值 = 10:1，高价值占比 90.9%。高价值内容包括两个生产修复文件、四个回归测试文件、bundle/index 发布载体、计划与归档链路；低价值只可能是状态类文件变更，但它用于计划/发布审计，不影响核心质量判断。
  - 是否存在"用数量掩盖质量"的模式——大量 trivial 产物掩盖了核心逻辑缺少验证的事实？
    → NO。测试数量不是主要论据，关键是每个测试都对应一个真实事故边界：用户样本结构、空 V2 遮蔽、metadata fallback、provider 初始化误判、SQLite 导入。没有用 snapshot 或纯 mock 委托测试冒充真实覆盖。

# 需求对齐检查
alignment_check:
  - 产物满足的是用户的字面需求还是本质需求？
    → 满足本质需求。用户的本质需求是旧原生模式聊天历史能被正常读取、迁移、展示，而不是初始化为空模板；本次实现确保消息级历史数据优先迁移，metadata 只作为表头 fallback，provider 层返回 loaded/merged，SQLite 可导入旧数据，同时保留 V2 有效 checkpoint 的优先级。
  - "如果这是别人交给我的，我会接受吗？"
    → YES。理由是：有真实样本结构驱动测试，有 provider 边界验证，有全量测试/类型检查/构建通过，有验收专家 accepted，有发布前提交范围控制。唯一仍需警惕的是发布操作本身：必须精确 git add，不能把用户 JSONL 样本提交；这一点将在提交前用 git status 和显式文件列表控制。

→ 本次验收未发现需要回滚或补改的阻塞问题；发布阶段继续执行归档、index 覆盖、精确暂存、提交、tag 与推送。
</output_quality_review>
