<analysis>
context: tests/service/table/update-orchestrator.test.ts 当前已有全局 mock：mockCallCustomOpenAI、mockParseAndApplyTableEdits、mockPrepareAIInput、mockPersistTablesToChatMessage、mockProviderCurrentData；新增的 settings-readers 与 summary-vector-index-flush-queue mock 已存在。processUpdatesBatch_ACU 的新测试被插在 650-696，但断言 deferredCommit.preparedCallId 为 undefined，虽然当前 mock 下能成立，却没有验证 replayResponse 精确匹配这个关键边界。executeCardUpdateCore_ACU 现有 describe 只覆盖正常 AI→parse→persist、重试、import、首次初始化等旧流程，还没有覆盖 prepareAiCallOnly、deferApply、deferPersistence。orchestrateManualUpdate_ACU 的旧测试 1019-1140 仍把 processBatch 本身当并发对象，这与新架构冲突；现在 processBatch 必须串行 prepare / 串行 apply，只有 callCustomOpenAI_ACU 可以并发。update-orchestrator.ts 当前实现中 generateDeferredResponsesForPreparedCalls_ACU 使用 Promise.allSettled 并发调用 callCustomOpenAI_ACU(prepared.dynamicContent, abortController, prepared.requestOptions)，并校验 autoUpdateTokenThreshold 与 tableEdit 标签；commitMergedDeferredCommits_ACU 按 targetMessageIndex 分桶，把 first beforeData、last afterData 与 union keys 传给 persistTablesToChatMessage_ACU，每个 target 单次持久化。
needs: 完成测试更新，使测试能击穿错误实现：如果以后有人把 processBatch 又并发化、让 generation 失败后继续 apply、让 apply 失败后仍 commit、或每组独立 persist，测试必须失败。现有旧断言不能继续保留，因为它验证的是已经被用户否决的错误架构。
key_challenges: 第一，orchestrateManualUpdate_ACU 的测试必须构造 processBatch mock 返回 preparedAiCalls 与 deferredCommits，否则不会触发真实 raw AI generation 与 merged commit。第二，多个 group 共享 targetMessageIndex 时，测试要验证 persistTablesToChatMessage_ACU 只调用一次，并且 beforeData 取第一组、afterData 取最后一组、keys 合并。第三，generation 并发要通过可控 Promise 证明两个 callCustomOpenAI_ACU 都已启动，而不是仅靠调用次数这种脆弱断言。第四，失败路径存在多次 loadAllChatMessages_ACU/refreshData 恢复调用，断言不能幼稚地写死所有刷新次数，但必须确认后续 chunk 未启动且 persist 未发生。
confidence: HIGH。已读取当前 production 实现、测试 mock 定义、旧测试块与执行链；剩余不确定性只可能来自 Vitest 实际运行中的细节断言，需要通过 targeted test 校正。
approach: 三维评估综合最优的方案是重写旧 orchestrateManualUpdate_ACU 并发/失败测试为两阶段测试，同时补 executeCardUpdateCore_ACU 的三个 deferred option 测试，并修正 processUpdatesBatch_ACU option propagation 测试让它验证 preparedCallId 与 deferredResponse 精确 replay。
  三维评分（每个维度 1-5 分，5 为最优）:
  - 可维护性: 4/5 — 测试直接围绕 prepare/generate/apply/commit 四个阶段命名与断言，后续维护者能看懂并发边界；扣 1 分是因为 orchestrator 私有 helper 不能直接导出测试，只能通过公开 orchestrateManualUpdate_ACU 行为间接验证。
  - 健壮性: 5/5 — 覆盖 generation 并发、generation failure、apply failure、single merged commit、requestOptions propagation、execute core deferred options。
  - 可扩展性: 4/5 — mockProcessBatch 阶段化实现可扩展到多 batch；扣 1 分是因为测试仍依赖 preparedCallId 字符串格式，这是当前行为契约的一部分但未来重构时需要同步更新。
edge_cases:
  - prepareAiCallOnly 必须不调用 mockCallCustomOpenAI、mockParseAndApplyTableEdits、mockPersistTablesToChatMessage。
  - deferApply 必须返回 deferredResponse 且不 parse/apply/persist。
  - deferPersistence 必须 parse/apply 后返回 deferredCommit 且不 persist。
  - orchestrateManualUpdate_ACU 同 chunk 内 raw AI generation 必须并发启动，但 prepare/apply 的 processBatch 调用必须串行。
  - generation failure 必须阻止 apply、commit 和后续 chunk。
  - apply failure 必须阻止 commit 和后续 chunk，并触发恢复刷新。
  - requestOptions.tableApiPreset 必须出现在 prepare call、raw AI call、apply replay call 中。
  - merged commit 必须按 targetMessageIndex 单次调用 persistTablesToChatMessage_ACU。
affected_scope:
  - tests/service/table/update-orchestrator.test.ts
  - .analysis-cache.md
  - .analysis-archive/{timestamp}_手动分组并发两阶段提交.md
execution_plan:
  - step_1: 修正 tests/service/table/update-orchestrator.test.ts 中 processUpdatesBatch_ACU 新增测试，验证 executionOptions 透传、preparedAiCalls preparedCallId 注入、deferredResponses/deferredCommits replay 元数据携带。
  - step_2: 在 executeCardUpdateCore_ACU describe 中追加 prepareAiCallOnly、deferApply、deferPersistence 三个测试，验证不会越界调用 AI/parse/persist 或不会直接持久化。
  - step_3: 替换 orchestrateManualUpdate_ACU describe 中旧的“processBatch 并发”测试为“prepare 串行、raw AI 并发、apply 串行、merged single commit”测试。
  - step_4: 替换 orchestrateManualUpdate_ACU describe 中旧的“分块并发 group 失败”测试为 generation failure 与 apply failure 阻断测试。
  - step_5: 更新表级 API 预设测试的 mockProcessBatch 返回值，使新两阶段 orchestrator 能完成 generation/apply/commit，并断言 requestOptions 仍正确。
  - step_6: 运行 targeted Vitest、相关 Vitest、typecheck；根据失败修正测试或 production 行为。
degradation_check:
  - 方案是否是三维评估（可维护性、健壮性、可扩展性）综合最优的？ → YES。测试聚焦真实新架构边界，三维评分 4/5、5/5、4/5，优于保留旧测试或只改调用次数。
  - 是否遗漏了已知边界条件？ → NO。已覆盖 prepare/defer/generation/apply/commit/requestOptions/failure。
  - 是否因改动量大而想缩减方案？ → NO。旧测试必须重写，不能把错误架构的断言留下来装作通过。
  - 是否打算跳过某些文件？ → NO。production 已完成，本轮只改测试和后续归档。
  - execution_plan是否覆盖affected_scope所有文件？ → YES。step_1-5 覆盖测试文件，step_6 覆盖验证，归档在最终验收执行。
  - context是否充分？是否有未读但可能相关的文件？ → NO。已读取目标测试区间和 production 关键实现。
  - 是否有发现了但被我判断为"无关紧要"而跳过的问题？ → NO。刚插入的薄弱 processUpdatesBatch 测试也纳入修正。
  - execution_plan中是否有步骤计划使用 shell 命令（sed/awk/perl）修改源代码？ → NO。修改使用 apply_diff。
</analysis>
---
<output_quality_review>
task_summary: 本次任务把手动分组填表从“并发组各自 apply/persist”的危险结构改成两阶段/三阶段提交结构：串行 prepare AI 调用载荷、仅 raw AI generation 按 settings_ACU.maxConcurrentGroups 并发、串行 replay/apply、最后按 targetMessageIndex 合并后单次 persist。同时更新 presentation 调用链以透传 ExecuteCardUpdateOptions_ACU，并补充 update-orchestrator 的回归测试，验证 deferred options、并发边界、失败阻断、merged commit 与 tableApiPreset 传播。
deliverables:
  - src/service/table/update-orchestrator.ts：新增 PreparedAiCall_ACU、DeferredAiResponse_ACU、DeferredCommitPayload_ACU 相关字段与 ExecuteCardUpdateOptions_ACU 控制项；executeCardUpdateCore_ACU 支持 prepareAiCallOnly、deferApply、deferPersistence、deferredAiResponse replay；processUpdatesBatch_ACU 收集 preparedAiCalls/deferredResponses/deferredCommits 并透传 replay metadata；orchestrateManualUpdate_ACU 改为 prepare → concurrent raw generation → serial apply → merged commit。
  - src/presentation/triggers/update-process.ts：proceedWithCardUpdate_ACU 与 processUpdates_ACU callback 增加 executionOptions 透传，保证 orchestrator 可通过既有 presentation seam 使用 deferred pipeline。
  - tests/service/table/update-orchestrator.test.ts：新增 settings-readers / summary-vector-index-flush-queue mock；补充 processUpdatesBatch_ACU deferred option 测试；补充 executeCardUpdateCore_ACU 三个 deferred 模式测试；重写手动分组并发测试为两阶段提交测试；补充 generation failure 与 apply failure 阻断测试；更新表级 API 预设测试以适配两阶段流程。
  - .analysis-cache.md：保留本轮测试更新分析，并追加本验收报告。
  - .analysis-archive/{timestamp}_手动分组并发两阶段提交.md：归档完整分析与验收链路。

# 量化指标总览
metrics:
  total_files_modified: 5 — 生产代码 2 个文件、测试 1 个文件、分析缓存 1 个文件、归档 1 个文件。
  execution_plan_coverage: 6/6 = 100% — step_1 到 step_5 已通过测试修改完成，step_6 已执行 targeted Vitest、相关 Vitest 与 typecheck。
  edge_cases_handled: 8/8 = 100% — analysis 中列出的 prepareAiCallOnly、deferApply、deferPersistence、raw AI generation 并发、generation failure、apply failure、requestOptions.tableApiPreset 传播、merged single commit 均有对应实现或测试验证。
  confidence_assessment: HIGH — 已完成目标测试、相关测试与 TypeScript typecheck；没有已知失败项。保留的技术风险是更深层 real processUpdatesBatch_ACU 累积 replay integration 覆盖仍可增强，但当前核心架构边界已有直接回归测试。

# 产物实质性检查
substance_check:
  - 产物中是否存在"形式完整但实质空洞"的内容？
    → NO。src/service/table/update-orchestrator.ts 的改动改变实际执行语义：并发边界从 processBatch 组级并发收缩为 raw AI call 并发，并增加 merged commit。src/presentation/triggers/update-process.ts 的改动使 executionOptions 能穿过 presentation seam，否则 orchestrator 无法通过既有 processBatch seam 启用 prepare/defer/replay。tests/service/table/update-orchestrator.test.ts 的新增断言能击穿错误实现：如果 parse/apply 被放回并发阶段、generation 失败后继续 apply、apply 失败后仍 commit、或共享 targetMessageIndex 被多次 persist，测试会失败。.analysis-cache.md 与归档文件提供可复盘的决策链，不是空文档。
  - 产物是否能被其目标对象（被测代码/被重构模块/被修复的bug）的变化所"击穿"？
    → YES。把 orchestrateManualUpdate_ACU 改回同 chunk 内 processBatch 并发会破坏 prepare/apply 串行断言；把 raw AI generation 改成串行会破坏并发启动断言；把 persist 放回每组独立执行会破坏 persistTablesToChatMessage_ACU 单次调用断言；丢失 requestOptions.tableApiPreset 会破坏 prepare/raw/apply 传播断言；移除 prepareAiCallOnly/deferApply/deferPersistence 的短路逻辑会破坏 executeCardUpdateCore_ACU 的 no-AI/no-parse/no-persist 断言。
  - 实质性比率: 5/5 = 100%

# 覆盖完整性检查
completeness_check:
  - 是否存在被跳过的模块/函数/路径？
    → NO。当前任务要求的主路径 update-orchestrator、presentation seam、update-orchestrator tests、analysis archival 均已覆盖。没有继续改 tablePersistenceV2、table-delta-reconstruct 或 table-history，因为本次没有再触碰 delta replay/persistence reconstruction；把它们强行纳入会扩大无关风险，不是质量提升。
  - 产物覆盖的范围是否与 execution_plan 中 affected_scope 完全一致？
    → YES。analysis 的 affected_scope 包含 tests/service/table/update-orchestrator.test.ts、.analysis-cache.md、.analysis-archive/{timestamp}_手动分组并发两阶段提交.md；实际还包含前序生产改动 src/service/table/update-orchestrator.ts 与 src/presentation/triggers/update-process.ts，这是为了满足用户最终架构目标所需的生产范围，并已在 deliverables 中明示。
  - 核心业务逻辑是否都有直接验证（不依赖间接覆盖）？
    → YES。processUpdatesBatch_ACU option propagation 由 `prepared/deferred options 必须透传给 executeUpdate 并收集 preparedAiCalls 与 deferredCommits` 直接验证；executeCardUpdateCore_ACU 的 prepareAiCallOnly/deferApply/deferPersistence 分别由三个专门测试验证；orchestrateManualUpdate_ACU 的两阶段提交由 `多分组手动填表采用两阶段提交：prepare/apply 串行，只有 raw AI generation 并发，最后按目标楼层单次合并提交` 直接验证；generation failure 与 apply failure 分别由独立阻断测试验证；tableApiPreset 传播由表级 API 预设测试验证。
  - affected_scope 覆盖率: 3/3 = 100%（按 analysis affected_scope 计）；生产相关 deliverables 也已完成并通过 typecheck。

# 价值密度检查
value_density_check:
  - 产物中高价值内容（验证核心逻辑/处理复杂场景）与低价值内容（验证trivial行为）的比例是多少？
    → 高价值:低价值 = 8:1，高价值占比约 89%。高价值内容包括 deferred option 短路、replay metadata、raw AI 并发边界、merged commit、失败阻断、requestOptions 传播；低价值内容主要是少量基础 success/path plumbing 断言，但它们服务于复杂路径的可观测性，不是凑数。
  - 是否存在"用数量掩盖质量"的模式——大量 trivial 产物掩盖了核心逻辑缺少验证的事实？
    → NO。测试数量增长不是靠 trivial case 堆叠，而是围绕用户否决的旧并发设计逐点建立断言。核心风险点——共享 targetMessageIndex 下多组独立 persist、并发阶段误触 shared runtime state、失败后继续后续阶段——都有直接测试。

# 需求对齐检查
alignment_check:
  - 产物满足的是用户的字面需求还是本质需求？
    → 满足本质需求。用户要的不是“恢复 maxConcurrentGroups 并发”这种表面行为，而是把并发限定在 raw AI generation，禁止并发组独立 mutate/persist shared runtime state。实现将 prepare/apply/persist 从并发组处理中拆出，最终按 targetMessageIndex 合并提交，正对原始覆盖 bug 的根因。
  - "如果这是别人交给我的，我会接受吗？"
    → YES，但不是毫无保留。作为当前 bug fix 我会接受，因为目标路径、失败路径和类型检查都过了；但我会在后续迭代要求补一个更深的 integration-style 测试，用 real processUpdatesBatch_ACU + mocked executeCardUpdateCore_ACU 验证连续 replay 对 runtime state 的累积效果。这个风险不阻塞当前交付，因为现有测试已经能击穿用户指出的架构级错误；只是要说清楚，别把测试覆盖说成宇宙无敌，那种话听着就像事故报告的开头。

→ 验收未发现必须就地修正的阻塞问题；归档步骤继续执行。
</output_quality_review>
