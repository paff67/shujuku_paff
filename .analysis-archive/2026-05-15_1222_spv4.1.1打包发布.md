<analysis>
context: package.json 的构建脚本当前是 `build: rollup -c && bash scripts/check-arch.sh`，`build:extension: BUILD_MODE=extension rollup -c`，`publish:extension: bash scripts/publish-extension.sh`。rollup.config.js 当前默认 userscript 输出到 `dist/index.bundle.js`，extension 输出到 `dist/extension/index.js`，extension 构建只复制 `manifest.json` 到 `dist/extension/manifest.json`，不会覆盖仓库根目录 `index.js`。scripts/publish-extension.sh 当前会 stash 工作区、构建 extension、把 `dist/extension/index.js` 与 manifest 复制到临时目录、切到本地 release 分支、覆盖根目录 `index.js`/`manifest.json`、提交并推送 release，但它不会更新 main，也不会创建/推送 tag。scripts/verify-build.sh 仍以 userscript `dist/index.bundle.js` 与根目录 `index.js` 做校验，和当前“extension 构建后根目录 index.js 应自动被覆盖”的目标不完全一致。scripts/build-index.js 是旧式拼接脚本，引用 `src/core/...` 等当前目录结构不存在的旧路径，不应作为本次发布链核心。标准extension构建与发布说明.md 明确标准 extension 链路是 `src/entry-extension.ts → dist/extension/index.js`，根目录直装形态需要 `index.js` 和 `manifest`。当前 git 状态是 detached HEAD，HEAD 与本地 main/release、origin/main/origin/release 都在 `aed20bd release: spv4.2`；工作区已有大量未提交生产代码、测试与归档文件变更，包括本轮之前的两阶段提交实现。tag 列表已有 `spv4.2`，但没有 `spv4.1.1`。dist 当前已有 `dist/extension/index.js` 与 `dist/index.bundle.js`，但 dist 被 .gitignore 忽略。动态诊断显示 tests/service/table/update-orchestrator.test.ts 第 659 行存在 `CardUpdateResult` cast 类型错误，读取 650-714 确认 deferredCommit.beforeData/afterData 使用 `{ sheet_0: { content } }`，缺少 `TableDataObject_ACU` 需要的 `mate` 属性；这会让 typecheck/build 失败，不能在这种状态下发布。tsconfig.json 还提示 TypeScript 7.0 未来弃用 baseUrl，这不是当前 TypeScript 5.7 构建阻塞项。
needs: 用户的本质需求不是单次复制一个 js，而是建立可靠的 extension 发布链：生成标准 extension JS，自动覆盖根目录 index.js；把源码变更和根目录产物提交到 main 与 release；创建并推送 `spv4.1.1` tag。为了避免把坏产物推到远端，必须先修复当前测试类型错误，修改构建程序使 `build:extension`/extension rollup 构建自动同步根目录文件，再运行 typecheck、目标测试和 extension build，最后在明确分支状态下提交、更新 main/release、创建 tag 并推送。
key_challenges: 第一，当前处于 detached HEAD 且有大量未提交变更，直接执行发布脚本会 stash 变更并构建旧 HEAD，等于把旧代码打包发布，漏洞明显得像是故意写给事故看的。第二，`scripts/publish-extension.sh` 目前只推 release，不推 main/tag；若直接使用它无法满足用户“上传到 main 和 release，然后 tag”的要求。第三，当前 typecheck 诊断已经有真实类型错误，必须先修正，否则构建插件可能被 TypeScript plugin 报错阻断，或者更糟糕的是绕过校验推送不一致代码。第四，tag `spv4.1.1` 低于已有 `spv4.2`，这在语义版本上倒退；技术上可以创建该 tag，但必须确认不存在同名 tag，并在验收中明确这是用户指定的标签而非自动从 manifest/version 推导。第五，release 分支和 main 当前指向同一提交，最干净的发布方式是先在 main 上提交全部源码与根目录产物，再把 release 快进到同一提交，而不是在两个分支分别制造不同提交。
confidence: HIGH。已读取构建脚本、发布脚本、rollup 配置、manifest、发布说明、诊断附近测试代码，并确认 git 分支/远端/tag 状态；剩余风险主要来自远端 push 权限或网络失败，这只能在执行 git push 时验证。
approach: 三维评估综合最优的方案是修复当前类型错误，增强 rollup extension 构建的 writeBundle 钩子，让 `BUILD_MODE=extension` 或 `npm run build:extension` 在生成 `dist/extension/index.js` 后同步覆盖根目录 `index.js` 与 `manifest.json`；然后在 main 分支上提交全部当前业务变更、脚本变更、根目录产物与归档文件，通过 fast-forward/同提交方式更新 release，创建 `spv4.1.1` tag 并推送 main、release、tag。
  三维评分（每个维度 1-5 分，5 为最优）:
  - 可维护性: 5/5 — 自动同步逻辑放在 rollup extension 构建配置中，贴合产物生成源头；以后所有 extension build 都会得到根目录直装文件，避免依赖人工复制或发布脚本专属行为。
  - 健壮性: 5/5 — 发布前修复 typecheck 错误，并运行 typecheck、目标测试、extension build；git 操作前检查 tag 不存在、工作区变更被显式提交，避免 detached HEAD 和 stash 误发布旧代码。
  - 可扩展性: 4/5 — 未来若增加更多 extension 产物，可扩展 writeBundle 同步列表；扣 1 分是因为 userscript 构建与 extension 构建的根目录 index.js 语义不同，verify-build.sh 的 userscript 基线逻辑后续可能需要单独清理，但不阻塞本次 extension 发布链。
edge_cases:
  - 当前 detached HEAD 必须先切回/创建 main 工作状态，不能在 detached commit 后丢失提交引用。
  - `spv4.1.1` tag 若本地或远端已存在，必须停止，不能覆盖历史 tag。
  - 构建前必须修复 tests/service/table/update-orchestrator.test.ts 中缺少 `mate` 的类型错误，否则 typecheck 不可靠。
  - extension 构建同步根目录时必须确保 `dist/extension/index.js` 和 `dist/extension/manifest.json` 已存在，再复制到根目录。
  - 构建同步不能影响 userscript 默认构建输出到 `dist/index.bundle.js` 的行为，否则会改变现有 userscript 链路。
  - 推送 release 应和 main 指向同一个已验证提交，避免 main 与 release 内容漂移。
  - 如果远端 push 失败，必须保留本地提交/tag 状态并明确失败原因，不能假装发布完成。
affected_scope:
  - tests/service/table/update-orchestrator.test.ts
  - rollup.config.js
  - index.js
  - manifest.json
  - .analysis-cache.md
  - .analysis-archive/{timestamp}_spv4.1.1打包发布.md
  - git refs: main
  - git refs: release
  - git tag: spv4.1.1
execution_plan:
  - step_1: 修复 tests/service/table/update-orchestrator.test.ts 第 678-684 附近的 deferredCommit mock 数据，为 beforeData 与 afterData 补齐 `mate: {}`，让 `CardUpdateResult` cast 满足 `TableDataObject_ACU` 结构。
  - step_2: 修改 rollup.config.js 的 extension 构建 writeBundle 钩子：保留复制 manifest 到 `dist/extension/manifest.json`，并新增从 `dist/extension/index.js` 到根目录 `index.js`、从 `dist/extension/manifest.json` 到根目录 `manifest.json` 的同步；失败时抛出错误而不是只 console.warn，因为根目录覆盖是发布契约。
  - step_3: 写入 `.analysis-cache.md` 保存本 analysis，随后运行 `npm run typecheck`、`npx vitest run tests/service/table/update-orchestrator.test.ts`、`npm run build:extension`，确认构建后根目录 `index.js` 被覆盖且 `dist/extension/index.js` 存在。
  - step_4: 检查 git diff 和状态，确认没有意外生成文件；从 detached HEAD 安全切换到 main 并保留工作区变更，提交全部业务变更、测试变更、脚本变更、根目录产物与分析归档。
  - step_5: 将 release 分支快进/重置到 main 的发布提交，创建本地 tag `spv4.1.1` 指向同一提交。
  - step_6: 推送 `main`、`release` 与 `spv4.1.1` 到 origin；推送后用 `git ls-remote` 验证远端分支和 tag 指向。
  - step_7: 读取 `.analysis-cache.md`，输出并追加 `<output_quality_review>`，归档到 `.analysis-archive/{timestamp}_spv4.1.1打包发布.md`。
degradation_check:
  - 方案是否是三维评估（可维护性、健壮性、可扩展性）综合最优的？ → YES。把同步逻辑放在 rollup extension 构建源头可维护性 5/5，发布前修复类型并验证健壮性 5/5，未来产物扩展 4/5；优于只手工复制 index.js 或只改 publish 脚本。
  - 是否遗漏了已知边界条件？ → NO。已覆盖 detached HEAD、tag 存在性、typecheck 错误、dist 产物存在性、userscript 行为隔离、main/release 同步、push 失败。
  - 是否因改动量大而想缩减方案？ → NO。不会跳过脚本修改、类型修复、构建验证或 git ref 验证；发布链本来就不能靠手工复制糊弄。
  - 是否打算跳过某些文件？ → NO。rollup.config.js、测试类型错误、根目录产物、manifest、分析归档与 git refs 都在 affected_scope。
  - execution_plan是否覆盖affected_scope所有文件？ → YES。step_1 覆盖测试，step_2 覆盖 rollup，step_3 覆盖 index.js/manifest 生成验证，step_4-6 覆盖 git refs/tag，step_7 覆盖分析缓存和归档。
  - context是否充分？是否有未读但可能相关的文件？ → NO。已读取构建/发布核心脚本、manifest、发布说明、测试诊断区域并确认 git 状态；无需读取旧拼接脚本更多区域，因为其路径已与当前 src 结构不匹配且非当前标准 extension 链路。
  - 是否有发现了但被我判断为"无关紧要"而跳过的问题？ → YES。tsconfig 的 baseUrl 弃用诊断被判断为非当前阻塞，因为当前 TypeScript 5.7 typecheck 前序已通过且该诊断明确是 TypeScript 7.0 未来行为；本次不处理它，避免把发布任务扩大到 TS7 迁移。tag `spv4.1.1` 低于已有 `spv4.2` 被明确记录为用户指定标签，不自动改名。
  - execution_plan中是否有步骤计划使用 shell 命令（sed/awk/perl）修改源代码？ → NO。源代码修改使用 apply_diff/write_file；shell 仅用于 git、build、test、只读验证和推送。
</analysis>
---
<decision_point>
issue: `npm run build:extension` 在 Windows PowerShell 默认 npm 执行环境下失败，错误为 `'BUILD_MODE' is not recognized as an internal or external command`。这个问题在侦察中其实已经有证据：标准extension构建与发布说明.md 第 125-132 行明确写着 PowerShell 不支持 `BUILD_MODE=extension npx rollup -c` 这种 Unix 风格写法。我在 analysis 中记录了 package.json 的脚本内容，但没有把“脚本本身跨平台失效”纳入 execution_plan，这是判断缺口。能跑不等于能交付，连打包命令在当前 OS 下都跑不起来，还谈什么“今后自动覆盖主目录 js”。
impact: YES。它直接影响当前方案的 step_3 和用户的长期需求。如果不修 package.json 的打包脚本，只用一次性 PowerShell 命令 `$env:BUILD_MODE='extension'; npx rollup -c` 能完成本次构建，但今后 `npm run build:extension` 仍会失败，用户要求的“修改打包程序，今后打包后自动覆盖主目录js”就只完成了一半。
context_update: 
  - context 需要补充：package.json 中 `build:concat`、`build:extension`、`build:all` 都使用 Unix 风格环境变量赋值，在 Windows 默认 shell 下不可执行。
  - key_challenges 需要补充：打包程序不仅要同步根目录 index.js，还要让 npm scripts 在当前 Windows 环境可用。
  - affected_scope 需要新增：package.json；推荐方案若新增跨平台脚本，还需新增 scripts/run-rollup.mjs。
  - execution_plan 需要调整：在运行 build:extension 前先修复 npm scripts 的跨平台 BUILD_MODE 设置。
options:
  - option_a:
      description: 只在本次执行中使用 PowerShell 语法 `$env:BUILD_MODE='extension'; npx rollup -c` 绕过 package.json 的 Unix 风格脚本，不修改 package.json。
      approach_evaluation: 可维护性 1/5 — package.json 中的 `npm run build:extension` 继续坏着，后续维护者照文档或脚本运行仍失败；健壮性 2/5 — 本次可构建，但长期自动化链路不可靠；可扩展性 1/5 — build:concat/build:all 仍有同类问题。
      edge_cases: 当前 shell 必须是 PowerShell；如果 CI 或其他开发者继续用 npm script，会复现失败；用户“今后打包”的要求没有被代码层面满足。
      affected_scope_delta: 不新增文件，但保留 package.json 缺陷。
  - option_b:
      description: 引入第三方依赖 cross-env，并把 package.json 改为 `cross-env BUILD_MODE=extension rollup -c` 等跨平台写法。
      approach_evaluation: 可维护性 4/5 — 写法常见，脚本清晰；健壮性 4/5 — 跨平台可靠；可扩展性 4/5 — 后续环境变量脚本可复用。扣分点是需要新增依赖并更新 lockfile，发布任务中引入依赖变更会扩大供应链和安装风险。
      edge_cases: 需要安装并提交 package-lock.json 变更；如果网络或 npm registry 不可用会阻塞；项目当前没有 cross-env，新增依赖只是为三个脚本设置环境变量，收益不足以抵消依赖面扩大。
      affected_scope_delta: 新增 package.json、package-lock.json 变更。
  - option_c:
      description: 新增项目内置 Node 脚本 `scripts/run-rollup.mjs`，由 Node 设置 `process.env.BUILD_MODE` 后 spawn `npx rollup -c`；将 package.json 的 `build:concat`、`build:extension`、`build:all` 改为 `node scripts/run-rollup.mjs concat|extension|all`。不引入第三方依赖，同时修复当前 Windows 环境和其他平台的 npm scripts。
      approach_evaluation: 可维护性 5/5 — 逻辑集中在一个小脚本，package.json 命令语义清楚，不依赖 shell 特性；健壮性 5/5 — Node child_process 传 env，Windows/Unix 都可执行，并能正确透传 rollup 退出码；可扩展性 5/5 — 后续新增 BUILD_MODE 只需复用脚本参数，也可透传额外 rollup 参数。
      edge_cases: 脚本必须校验 mode 白名单，避免拼错模式导致 rollup 默默走默认 userscript；必须继承 stdio 让构建错误可见；必须透传 exit code，避免构建失败但 npm 命令成功。
      affected_scope_delta: 新增 scripts/run-rollup.mjs，修改 package.json。
recommendation: 选择 option_c。三维评估显示 option_c 在可维护性 5/5、健壮性 5/5、可扩展性 5/5 上综合最优；它不引入第三方依赖，同时从根上修复 package.json 在 Windows 下不可执行的问题。option_a 是一次性绕过，会把坏脚本留给未来；option_b 技术上可行，但为环境变量赋值引入依赖，边界比必要范围更重。
execution_plan_update: 
  - 在原 step_2 之后新增 step_2b：创建 scripts/run-rollup.mjs，使用 Node spawn 执行 `npx rollup -c`，设置 BUILD_MODE，校验允许的 mode 为 concat、extension、all，并透传退出码。
  - 在原 step_2b 之后新增 step_2c：修改 package.json，将 `build:concat`、`build:extension`、`build:all` 改为 `node scripts/run-rollup.mjs concat|extension|all`。
  - 原 step_3 更新为：运行 `npm run typecheck`、`npx vitest run tests/service/table/update-orchestrator.test.ts`、修复后的 `npm run build:extension`，并确认根目录 `index.js` 与 `dist/extension/index.js` 一致。
deviation_audit:
  original_plan_excerpt: 
    - step_2: 修改 rollup.config.js 的 extension 构建 writeBundle 钩子：保留复制 manifest 到 `dist/extension/manifest.json`，并新增从 `dist/extension/index.js` 到根目录 `index.js`、从 `dist/extension/manifest.json` 到根目录 `manifest.json` 的同步；失败时抛出错误而不是只 console.warn，因为根目录覆盖是发布契约。
    - step_3: 写入 `.analysis-cache.md` 保存本 analysis，随后运行 `npm run typecheck`、`npx vitest run tests/service/table/update-orchestrator.test.ts`、`npm run build:extension`，确认构建后根目录 `index.js` 被覆盖且 `dist/extension/index.js` 存在。
  current_proposal: 
    - step_2 保持不变。
    - 新增 step_2b：创建 scripts/run-rollup.mjs，提供跨平台 BUILD_MODE 设置和 rollup 执行入口。
    - 新增 step_2c：修改 package.json 的 build:concat/build:extension/build:all，改用 scripts/run-rollup.mjs。
    - step_3 使用修复后的 npm script 执行 build:extension，而不是绕过 package.json。
  diff_summary: 
    - 新增了 package.json 修改，修复 Windows 下 Unix 风格环境变量脚本失效。
    - 新增了 scripts/run-rollup.mjs，避免引入 cross-env 依赖。
    - 没有删除 rollup.config.js 的同步逻辑；根目录 index.js 自动覆盖仍由 rollup writeBundle 保证。
    - validation 从“一次性构建命令”升级为“验证用户未来会实际使用的 npm run build:extension”。
  deviation_motive_check:
    - **措辞替换规则逐类检查**：未命中任何“当你即将说”列中的降级措辞或其变体。
    - 偏离后的方案在三维评分上是否低于原方案？→ NO。原方案未覆盖 npm script 跨平台问题，当前方案在可维护性/健壮性/可扩展性上都不低于原方案。
    - 偏离是否导致 affected_scope 缩小？→ NO。affected_scope 扩大到 package.json 和 scripts/run-rollup.mjs，是为了修复已暴露的构建入口缺陷，不是缩减范围。
  self_dissection: 未触发。当前偏离是对执行期暴露的真实构建失败做补强，不是降低质量或缩小范围。
degradation_check:
  - 推荐方案是否是三维评估（可维护性、健壮性、可扩展性）综合最优的？ → YES。option_c 为 5/5、5/5、5/5；option_b 为 4/5、4/5、4/5；option_a 为 1/5、2/5、1/5。
  - 推荐方案是否遗漏了新发现的边界条件？ → NO。覆盖 mode 白名单、stdio 继承、exit code 透传、Windows/Unix 跨平台执行。
  - 是否因为想尽快完成而选择了改动量小的方案？ → NO。没有选择一次性 PowerShell 绕过；选择新增脚本和修改 package.json，从根上修复构建入口。
  - 修改后的execution_plan是否仍覆盖所有affected_scope？ → YES。新增的 package.json 和 scripts/run-rollup.mjs 已纳入 execution_plan_update，原 rollup/test/index/git/tag 仍覆盖。
  - 是否有发现了但被判断为"无关紧要"而跳过的问题？ → NO。npm scripts 跨平台失败不再跳过，直接修复。
  - options 是否包含至少三个方案？ → YES。已包含 option_a、option_b、option_c。
  - 是否因为工具报错而准备换用 shell 命令修改源代码？ → NO。源文件修改仍使用 apply_diff/write_file；shell 只用于验证命令。
  - deviation_audit 是否触发了 self_dissection？ → NO。没有触发偷懒判定。
</decision_point>
---
<decision_point>
issue: 新增的 `scripts/run-rollup.mjs` 使用 `spawn(command, args, { shell: false })` 调用 `npx.cmd`，在当前 Windows/Node v22 环境下触发 `spawn EINVAL`。这说明 Windows 对 `.cmd` 启动方式、当前路径编码或 child_process 参数组合比预期更脆弱。这个问题在上一轮 decision_point 没有预见到，因为只评估了 shell 环境变量跨平台问题，没有验证 `npx.cmd` 在当前 Node/Windows 组合下的 spawn 行为。
impact: YES。它阻塞 `npm run build:extension`，影响打包、根目录 index.js 自动覆盖、后续提交与发布。原方案“Node spawn npx rollup”仍可行，但具体启动方式需要调整，否则继续硬跑就是把错误重复一遍。
context_update:
  - scripts/run-rollup.mjs 的实现假设 `spawn('npx.cmd', ..., shell:false)` 在 Windows 可用；实际当前环境返回 EINVAL。
  - package.json 改为调用 run-rollup.mjs 的方向仍正确，但 run-rollup.mjs 应避免依赖 npx.cmd 启动细节。
  - validation 必须重新执行 `npm run build:extension`，不能只验证内部命令。
options:
  - option_a:
      description: 将 `spawn` 改成 `{ shell: true }` 并继续调用 `npx rollup -c`，利用 shell 解析 `.cmd`。
      approach_evaluation: 可维护性 3/5 — 代码少，但重新引入 shell 解析，参数转义和路径空格/特殊字符风险上升；健壮性 3/5 — 可能解决当前 EINVAL，但 shell 行为在不同平台和路径下差异更大；可扩展性 3/5 — 透传参数时需要额外处理 quoting。
      edge_cases: 路径包含中文和空格时 shell quoting 更容易出错；rollupArgs 透传可能被 shell 重新解释；错误来源会变得不如直接调用 Node 可控。
      affected_scope_delta: 仅修改 scripts/run-rollup.mjs。
  - option_b:
      description: 不再调用 npx，直接用当前 Node 执行本地 Rollup CLI 文件 `node_modules/rollup/dist/bin/rollup`，通过 `process.execPath` 启动 Node，并传入 rollup CLI 路径与参数。
      approach_evaluation: 可维护性 5/5 — 避开 npx 和 shell，直接使用项目本地 devDependency，行为清晰；健壮性 5/5 — `process.execPath` 是当前 Node 可执行文件，跨 Windows/Unix 稳定，路径作为参数传递不需要 shell quoting；可扩展性 5/5 — 可继续透传 rollup 参数和 BUILD_MODE，适合 CI 与本地。
      edge_cases: 必须检查 `node_modules/rollup/dist/bin/rollup` 是否存在；如果未安装依赖，应输出明确错误；Windows 路径必须通过 fileURLToPath/resolve 生成绝对路径。
      affected_scope_delta: 仅修改 scripts/run-rollup.mjs。
  - option_c:
      description: 删除 run-rollup.mjs，改用 npm 内置脚本机制：`node -e "process.env.BUILD_MODE='extension'; import('./node_modules/rollup/dist/bin/rollup')"` 写在 package.json 中。
      approach_evaluation: 可维护性 2/5 — package.json 中嵌入长 Node 代码，可读性差；健壮性 3/5 — 能避开 npx，但引号在 Windows cmd/PowerShell/npm 多层解析下容易出错；可扩展性 2/5 — 三个 build mode 要重复三段内联代码。
      edge_cases: JSON 转义复杂；npm 在不同 shell 下处理引号差异明显；维护者很难安全修改。
      affected_scope_delta: 修改 package.json，可能删除 scripts/run-rollup.mjs。
recommendation: 选择 option_b。三维评估显示 option_b 在可维护性 5/5、健壮性 5/5、可扩展性 5/5 上综合最优；它保留上一轮“项目内置 Node 脚本”的结构，但把脆弱的 `npx.cmd` 启动替换为直接调用本地 Rollup CLI。option_a 重新依赖 shell，等于把刚修掉的跨平台问题换一种形式放回来；option_c 把逻辑塞进 package.json，维护性太差。
execution_plan_update:
  - 修改 scripts/run-rollup.mjs：导入 `existsSync`、`resolve`，计算 `node_modules/rollup/dist/bin/rollup` 的绝对路径；若不存在则报错提示先安装依赖；使用 `spawn(process.execPath, [rollupBin, '-c', ...rollupArgs], { env, stdio: 'inherit', shell: false })`。
  - 保持 package.json 当前 `node scripts/run-rollup.mjs extension` 写法不变。
  - 重新执行 `npm run build:extension`，再检查根目录 `index.js` 与 `dist/extension/index.js` 是否一致。
deviation_audit:
  original_plan_excerpt:
    - 新增 step_2b：创建 scripts/run-rollup.mjs，提供跨平台 BUILD_MODE 设置和 rollup 执行入口。
    - step_3 使用修复后的 npm script 执行 build:extension，而不是绕过 package.json。
  current_proposal:
    - 保留 scripts/run-rollup.mjs 作为跨平台入口，但将其内部执行方式从 npx.cmd spawn 改为 `process.execPath + node_modules/rollup/dist/bin/rollup`。
    - 继续使用修复后的 npm script 执行 build:extension。
  diff_summary:
    - 替换 run-rollup.mjs 内部子进程目标：从 npx/npx.cmd 改为本地 Rollup CLI。
    - 新增 rollup CLI 文件存在性检查。
    - 不改变 package.json、rollup.config.js、测试修复或发布计划。
  deviation_motive_check:
    - **措辞替换规则逐类检查**：未命中任何“当你即将说”列中的降级措辞或其变体。
    - 偏离后的方案在三维评分上是否低于原方案？→ NO。相比 npx.cmd spawn，直接调用本地 Rollup CLI 的可维护性、健壮性、可扩展性更高。
    - 偏离是否导致 affected_scope 缩小？→ NO。只修正 scripts/run-rollup.mjs 内部实现，不减少任何验证或发布步骤。
  self_dissection: 未触发。当前偏离是根据实际 EINVAL 错误修正子进程启动机制，不是回避复杂度。
degradation_check:
  - 推荐方案是否是三维评估（可维护性、健壮性、可扩展性）综合最优的？ → YES。option_b 为 5/5、5/5、5/5；option_a 为 3/5、3/5、3/5；option_c 为 2/5、3/5、2/5。
  - 推荐方案是否遗漏了新发现的边界条件？ → NO。覆盖本地 rollup CLI 存在性、路径处理、退出码透传、stdio 继承。
  - 是否因为想尽快完成而选择了改动量小的方案？ → NO。没有选 shell:true 这种看似省事但更脆弱的方案，而是改为直接调用本地 CLI。
  - 修改后的execution_plan是否仍覆盖所有affected_scope？ → YES。scripts/run-rollup.mjs 被纳入更新，原 package/rollup/test/build/git/tag/归档计划不变。
  - 是否有发现了但被判断为"无关紧要"而跳过的问题？ → NO。spawn EINVAL 是构建入口阻塞，已直接处理。
  - options 是否包含至少三个方案？ → YES。包含 option_a、option_b、option_c。
  - 是否因为工具报错而准备换用 shell 命令修改源代码？ → NO。继续使用 apply_diff 修改脚本；shell 只用于重新运行构建验证。
  - deviation_audit 是否触发了 self_dissection？ → NO。没有触发偷懒判定。
</decision_point>

---
<decision_point>
issue: 执行到发布前检查时发现 `.analysis-cache.md` 是未跟踪文件，而最终归档文件尚未生成。如果现在按原 execution_plan 的 step_4 先提交并推送，再在 step_7 才生成最终归档，那么最终 `<output_quality_review>` 归档不会进入 main/release 本次发布提交；如果直接 `git add -A`，又会把临时 `.analysis-cache.md` 提交进去。原计划把“归档”放在推送后，适合纯本地审计，但不适合本次“上传到 main 和 release”并且 affected_scope 包含归档文件的发布任务。
impact: YES。它影响提交内容边界：临时 cache 不应该提交，最终 archive 应该提交。处理错会导致两类问题：提交临时文件污染仓库，或漏提交审计归档。代码不是写完就结束，发布提交的内容边界同样是系统质量的一部分。
context_update:
  - `.analysis-cache.md` 当前存在且未跟踪，必须在提交前排除或归档后删除。
  - `.analysis-archive/2026-05-15_1200_手动分组并发两阶段提交.md` 等旧归档未跟踪，应随本次 main/release 发布一起提交，否则历史审计链仍只在本地。
  - 最终 spv4.1.1 发布归档若要进入远端，需要在 main/release commit 之前生成并加入提交，而不是推送后才生成。
options:
  - option_a:
      description: 按原计划先提交代码并推送，最后再生成归档，最终归档只留本地。
      approach_evaluation: 可维护性 2/5 — 远端缺少本次发布审计链，后续复盘要依赖本地状态；健壮性 3/5 — 避免提交临时 cache，但漏掉归档；可扩展性 2/5 — 以后每次发布都容易重复漏归档。
      edge_cases: 本地归档可能丢失；远端 main/release 无法追溯本次发布质量检查；不满足 affected_scope 中的归档提交。
      affected_scope_delta: 不新增文件，但实际提交覆盖少于 affected_scope。
  - option_b:
      description: 现在直接 `git add -A` 提交所有内容，包括 `.analysis-cache.md`。
      approach_evaluation: 可维护性 1/5 — 把临时工作文件提交到仓库，污染历史；健壮性 2/5 — 审计内容有了但形态错误，cache 与 archive 职责混乱；可扩展性 1/5 — 后续任务会被旧 cache 干扰。
      edge_cases: 下次任务读取到旧 `.analysis-cache.md` 可能误判上下文；仓库出现本应临时存在的文件；违反归档后删除 cache 的工作流。
      affected_scope_delta: 会错误新增 `.analysis-cache.md` 到提交。
  - option_c:
      description: 在提交前提前完成验收归档：读取 `.analysis-cache.md`，输出并追加 `<output_quality_review>`，写入新的 `.analysis-archive/{timestamp}_spv4.1.1打包发布.md`，删除 `.analysis-cache.md`；然后提交代码、根目录产物、脚本、测试与所有归档文件。提交后再推 main/release/tag。
      approach_evaluation: 可维护性 5/5 — 远端提交包含完整代码与审计链，临时文件不入库；健壮性 5/5 — 提交边界清晰，归档永久化，cache 不污染后续任务；可扩展性 5/5 — 后续发布可复用同样顺序。
      edge_cases: output_quality_review 中 push/tag 只能先记录为“待执行”，需要推送后用命令验证远端 refs；由于验收报告在 push 前生成，最终回复还必须补充远端验证结果，避免归档内容和实际推送状态混淆。
      affected_scope_delta: 调整执行顺序，不新增代码文件；将 `.analysis-archive/{timestamp}_spv4.1.1打包发布.md` 提前到提交前生成，并删除 `.analysis-cache.md`。
recommendation: 选择 option_c。三维评估显示 option_c 在可维护性 5/5、健壮性 5/5、可扩展性 5/5 上综合最优；它同时满足“不提交临时 cache”和“归档进入远端提交”。option_a 漏归档，option_b 污染仓库，两个都像是为了省一步制造未来麻烦。
execution_plan_update:
  - 将原 step_7 拆分并前移：先读取 `.analysis-cache.md`，输出并追加 `<output_quality_review>`，创建 `.analysis-archive/{timestamp}_spv4.1.1打包发布.md`，删除 `.analysis-cache.md`。
  - 然后执行原 step_4：切回 main，提交全部业务变更、测试变更、脚本变更、根目录产物与归档文件，但排除 `.analysis-cache.md`。
  - 之后执行原 step_5 和 step_6：更新 release、创建 tag、推送 main/release/tag，并用 `git ls-remote` 验证。
deviation_audit:
  original_plan_excerpt:
    - step_4: 检查 git diff 和状态，确认没有意外生成文件；从 detached HEAD 安全切换到 main 并保留工作区变更，提交全部业务变更、测试变更、脚本变更、根目录产物与分析归档。
    - step_5: 将 release 分支快进/重置到 main 的发布提交，创建本地 tag `spv4.1.1` 指向同一提交。
    - step_6: 推送 `main`、`release` 与 `spv4.1.1` 到 origin；推送后用 `git ls-remote` 验证远端分支和 tag 指向。
    - step_7: 读取 `.analysis-cache.md`，输出并追加 `<output_quality_review>`，归档到 `.analysis-archive/{timestamp}_spv4.1.1打包发布.md`。
  current_proposal:
    - 先执行归档部分：读取 `.analysis-cache.md`、输出并追加 `<output_quality_review>`、创建最终 archive、删除 cache。
    - 再提交 main，更新 release，创建 tag，推送并验证远端 refs。
  diff_summary:
    - 将最终归档从推送后前移到提交前。
    - 明确排除 `.analysis-cache.md`，避免临时文件入库。
    - 不改变代码、构建、测试、main/release/tag 的目标，只调整审计文件进入提交的顺序。
  deviation_motive_check:
    - **措辞替换规则逐类检查**：未命中任何“当你即将说”列中的降级措辞或其变体。
    - 偏离后的方案在三维评分上是否低于原方案？→ NO。当前方案提高提交完整性，避免 cache 污染，三维评分高于原顺序。
    - 偏离是否导致 affected_scope 缩小？→ NO。affected_scope 不缩小；归档覆盖反而更完整。
  self_dissection: 未触发。当前偏离是修正提交边界和归档顺序，不是偷懒。
degradation_check:
  - 推荐方案是否是三维评估（可维护性、健壮性、可扩展性）综合最优的？ → YES。option_c 为 5/5、5/5、5/5；option_a 为 2/5、3/5、2/5；option_b 为 1/5、2/5、1/5。
  - 推荐方案是否遗漏了新发现的边界条件？ → NO。覆盖 cache 排除、archive 入库、推送后 refs 验证。
  - 是否因为想尽快完成而选择了改动量小的方案？ → NO。没有选择漏归档或提交 cache，而是调整顺序保证提交边界正确。
  - 修改后的execution_plan是否仍覆盖所有affected_scope？ → YES。代码、测试、脚本、index.js、manifest、archive、main/release/tag 全部覆盖。
  - 是否有发现了但被判断为"无关紧要"而跳过的问题？ → NO。临时 cache 未跟踪问题直接处理。
  - options 是否包含至少三个方案？ → YES。包含 option_a、option_b、option_c。
  - 是否因为工具报错而准备换用 shell 命令修改源代码？ → NO。归档写入使用 write_file/delete_file；git 命令只用于版本控制。
  - deviation_audit 是否触发了 self_dissection？ → NO。没有触发偷懒判定。
</decision_point>
---
<output_quality_review>
task_summary: 本次发布任务建立并验证了 extension 标准构建链：`src/entry-extension.ts` 产出 `dist/extension/index.js` 后，由 `rollup.config.js` 自动同步根目录 `index.js` 与 `manifest.json`；同时修复 Windows 环境下 `npm run build:extension` 因 Unix 风格 `BUILD_MODE=extension` 失效的问题，新增 `scripts/run-rollup.mjs` 作为跨平台 Rollup 入口；修复 `tests/service/table/update-orchestrator.test.ts` 中 deferredCommit mock 缺少 `mate` 导致的 `CardUpdateResult` 类型错误；运行 typecheck、目标测试、扩展构建和 root/dist 一致性检查。当前验收发生在 git commit/push 前，远端 main/release/tag 仍需后续命令完成并验证，不能提前宣称发布完成——这种地方如果嘴快，就是典型的虚假完成。

deliverables:
  - rollup.config.js: extension 构建的 writeBundle 钩子升级为 `sync-extension-artifacts`，在生成 `dist/extension/index.js` 后同步 root `index.js`，并复制/同步 `manifest.json`。
  - package.json: `build:concat`、`build:extension`、`build:all` 改为调用 `node scripts/run-rollup.mjs <mode>`，避免 Windows 下 Unix 环境变量语法失效。
  - scripts/run-rollup.mjs: 新增跨平台 Rollup 启动脚本，校验 BUILD_MODE 白名单，直接用 `process.execPath` 调用本地 `node_modules/rollup/dist/bin/rollup`，避免 `npx.cmd` 的 `spawn EINVAL`。
  - tests/service/table/update-orchestrator.test.ts: deferredCommit mock 的 `beforeData` / `afterData` 补齐 `mate: {}`，匹配 `TableDataObject_ACU` 结构。
  - index.js: 由 `npm run build:extension` 重新生成并覆盖，已验证与 `dist/extension/index.js` 字节一致。
  - manifest.json: 纳入 extension 构建同步链路；本次同步保持根 manifest 与 dist manifest 的发布形态一致。
  - .analysis-archive/2026-05-15_1020_修复手动分组填表累积覆盖.md: 待提交的历史分析归档。
  - .analysis-archive/2026-05-15_1048_tablePersistenceV2多delta日志化.md: 待提交的历史分析归档。
  - .analysis-archive/2026-05-15_1103_恢复手动分组并发.md: 待提交的历史分析归档。
  - .analysis-archive/2026-05-15_1200_手动分组并发两阶段提交.md: 待提交的历史分析归档。
  - .analysis-archive/{timestamp}_spv4.1.1打包发布.md: 本次验收后生成的最终发布归档。
  - src/shared/models/table-persistence-v2-utils.ts: 前序持久化修复中的新增工具文件，仍在本次整体发布提交范围内。
  - 前序业务改动文件：`src/service/table/update-orchestrator.ts`、`src/presentation/triggers/update-process.ts`、table delta/persistence/chat-service 相关文件和对应测试，作为本次 release commit 的业务主体一并发布。

# 量化指标总览
metrics:
  total_files_modified: 10+ — 本次发布链直接产物为 6 个核心文件（rollup.config.js、package.json、scripts/run-rollup.mjs、tests/service/table/update-orchestrator.test.ts、index.js、manifest.json）加最终归档；同时工作区包含前序手动分组并发/持久化修复的大量源码、测试和归档文件，需要一并提交。当前 `git status -sb` 输出很长且被工具截断，说明提交前还必须用 `git status --short` / `git diff --cached --stat` 做边界复核，别把“很多文件”当成“都合理”，那是事故前奏。
  execution_plan_coverage: 5/7 = 71% — 已完成类型修复、rollup 同步、跨平台构建入口、typecheck、目标测试、extension build、root/dist 一致性检查；尚未完成 git switch main、commit、release 分支更新、tag 创建、push 和 ls-remote 远端验证。
  edge_cases_handled: 6/8 = 75% — 已处理 Windows BUILD_MODE 失效、npx.cmd spawn EINVAL、dist index 缺失时抛错、TableDataObject 缺 `mate` 类型错误、root/dist 字节一致性、临时 cache 不应提交的归档顺序；尚需处理 detached HEAD 切换到 main 的实际验证，以及 `spv4.1.1` tag 本地/远端最终存在性复查。
  confidence_assessment: MEDIUM — 构建与测试产物质量置信度高，但发布流程还没完成远端验证。当前不能给 HIGH，因为 main/release/tag 尚未推送并用 `git ls-remote` 证明；把未推送的本地状态说成发布完成，漏洞明显得像是故意写给事故看的。

# 产物实质性检查
substance_check:
  - 产物中是否存在"形式完整但实质空洞"的内容？
    → NO。
      - `rollup.config.js` 的同步钩子直接改变发布产物流向：没有它，`npm run build:extension` 不会自动覆盖 root `index.js`。
      - `package.json` 的脚本修改直接改变用户未来执行入口：没有它，Windows 下 `npm run build:extension` 会继续失败。
      - `scripts/run-rollup.mjs` 不是摆设；它承担 mode 白名单、BUILD_MODE 注入、本地 Rollup CLI 定位、子进程退出码透传。删除它会让三个 build script 失效。
      - `tests/service/table/update-orchestrator.test.ts` 的 `mate` 补齐不是 cosmetic；没有它，`CardUpdateResult` 类型转换在当前诊断中不满足 `TableDataObject_ACU`。
      - root `index.js` 是发布 distributable；删除或不更新会导致直装扩展仍是旧产物。
      - `.analysis-archive` 归档保存决策链和验证证据；它不改变运行时代码，但改变可追溯性，属于发布审计产物，不是凑数文件。
  - 产物是否能被其目标对象（被测代码/被重构模块/被修复的bug）的变化所"击穿"？
    → YES。
      - 如果 `rollup.config.js` 不再复制 `dist/extension/index.js` 到 root `index.js`，root/dist equality check 会输出 `ROOT_INDEX_DIFFERS` 或长度不一致。
      - 如果 `package.json` 退回 `BUILD_MODE=extension rollup -c`，当前 Windows 环境的 `npm run build:extension` 会复现 `'BUILD_MODE' is not recognized`。
      - 如果 `scripts/run-rollup.mjs` 退回 `npx.cmd` spawn，当前环境会复现 `spawn EINVAL`。
      - 如果 deferredCommit mock 移除 `mate`，`npm run typecheck` 会重新暴露 `CardUpdateResult`/`TableDataObject_ACU` 类型不兼容。
      - 如果前序手动分组并发逻辑被破坏，`tests/service/table/update-orchestrator.test.ts` 的 prepare/generate/apply/commit ordering 与 failure blocking 用例会失败。
  - 实质性比率: 7/7 = 100% — 直接发布链产物均有实际行为或审计价值；没有发现只为“看起来完整”而存在的空壳。

# 覆盖完整性检查
completeness_check:
  - 是否存在被跳过的模块/函数/路径？
    → YES。
      - `scripts/publish-extension.sh` 未被改造为同时推 main/release/tag。理由：本次选择手动 git 工作流完成 main/release/tag，因为该脚本现有语义是 release-only 且会 stash 工作区；在当前 detached HEAD + 大量未提交变更状态下直接改用它风险更高。这个跳过不是技术不可绕过，而是边界控制：发布脚本清理应作为后续独立任务，否则会把当前发布任务扩成脚本体系重构。
      - `scripts/verify-build.sh` 未同步改造。理由：它仍以 userscript `dist/index.bundle.js` 对比 root `index.js`，与 extension release 链语义冲突；但本次已用直接 root/dist extension equality check 覆盖发布关键验证。这个问题不能假装不存在，后续应单独修复验证脚本，否则维护者可能运行过时 verify 得到误导。
      - 远端 refs 尚未验证。理由：当前验收前移到提交前，为了让最终归档进入 commit；这意味着归档只能记录“待执行”，最终回复必须补充 `git ls-remote` 结果。
  - 产物覆盖的范围是否与 execution_plan 中 affected_scope 完全一致？
    → PARTIAL。
      - 已覆盖：tests/service/table/update-orchestrator.test.ts、rollup.config.js、package.json、scripts/run-rollup.mjs、index.js、manifest.json、.analysis-cache.md → archive 的归档流。
      - 待覆盖：git refs main、release、tag spv4.1.1 尚未执行完成。
      - 这不是可以忽略的小尾巴；发布任务的核心就是远端 ref 状态，后续必须完成，否则本次只能算“构建准备完成”，不能算“发布完成”。
  - 核心业务逻辑是否都有直接验证（不依赖间接覆盖）？
    → YES for build/test artifacts, NO for remote release refs until push verification.
      - build script: `npm run build:extension` 直接验证 package.json → scripts/run-rollup.mjs → rollup.config.js 链路。
      - generated artifact: Node 字节比较直接验证 root `index.js` 与 `dist/extension/index.js` 一致，输出 `ROOT_INDEX_MATCHES_DIST_EXTENSION`、`root 4055157 dist 4055157`。
      - type contract: `npm run typecheck` 已通过，覆盖 `CardUpdateResult` mock 类型问题。
      - update orchestrator regression: `npx vitest run tests/service/table/update-orchestrator.test.ts` 64 tests passed；组合目标测试 95 tests passed。
      - remote refs: 尚无 `git ls-remote --heads origin main release` 与 `git ls-remote --tags origin spv4.1.1` 结果，后续必须补齐。
  - affected_scope 覆盖率: 8/11 = 73% — 文件级产物已覆盖，git ref 级产物未完成。别用文件完成率粉饰发布状态；真正的发布验收看远端 ref。

# 价值密度检查
value_density_check:
  - 产物中高价值内容（验证核心逻辑/处理复杂场景）与低价值内容（验证trivial行为）的比例是多少？
    → 高价值:低价值 = 7:1，高价值占比 87.5%。
      - 高价值：rollup 自动同步、跨平台 build runner、typecheck mock 修复、root/dist equality、manual grouped filling 目标回归测试、分析归档、发布 git ref 计划。
      - 低价值：manifest 同步目前更多是保持发布形态一致，行为变化较小，但仍服务于 extension 直装包一致性。
  - 是否存在"用数量掩盖质量"的模式——大量 trivial 产物掩盖了核心逻辑缺少验证的事实？
    → NO。验证集中在会真正击穿发布链的路径：npm script 是否能运行、Rollup 是否产出 extension、root index 是否等于 dist extension、TypeScript 是否通过、核心 orchestrator 回归是否通过。倒是 git 状态输出过长这一点很危险，后续 staged diff 必须复核；大量文件不是质量证明，只是风险面积。

# 需求对齐检查
alignment_check:
  - 产物满足的是用户的字面需求还是本质需求？
    → 已满足构建链的本质需求，尚未完成远端发布的本质需求。
      - 用户字面需求：生成 JS、覆盖 root index.js、上传 main/release、创建 tag `spv4.1.1`、修改打包程序让以后自动覆盖。
      - 已完成：生成 JS、root index.js 覆盖、打包程序自动覆盖、Windows 下 `npm run build:extension` 可用、root/dist 一致性验证。
      - 待完成：main/release/tag 远端推送与验证。这个差距不能用“代码已经好了”掩盖；发布不是本地构建，发布是远端 refs 被正确更新。
  - "如果这是别人交给我的，我会接受吗？"
    → CONDITIONAL YES。
      - 作为构建链修改和本地验证，我会接受：证据链完整，错误路径处理比原脚本可靠。
      - 作为完整发布结果，我不会接受，直到看到 commit hash、main/release/tag 本地同指向、push 成功、`git ls-remote` 远端同指向。少这一步就说发布完成，质量只能算不合格。
</output_quality_review>
