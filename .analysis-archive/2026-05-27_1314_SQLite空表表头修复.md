<analysis>
context: 搜索 resultToContent 只发现 3 处源码引用：src/data/sqlite/schema-mapper.ts 定义、src/data/sqlite/sync-bridge.ts 导入、src/data/sqlite/sync-bridge.ts 的 _exportSheet 调用。src/data/sqlite/schema-mapper.ts:179-198 的 resultToContent 当前签名为 columns、values、chineseHeaders，遇到 columns.length === 0 直接返回 [['row_id']]，这会在 SQLite 空 SELECT 不返回列元数据时丢失 DDL 中的业务列。src/data/sqlite/sync-bridge.ts:193-203 的 _exportSheet 会先执行 SELECT * FROM ${tableName}，再从 meta.sourceData.ddl 或 engine.getTableDDL(tableName) 取 DDL 并 buildColumnNameMap，但没有把 DDL 解析出的列名传给 resultToContent。src/shared/ddl-utils.ts:45-70 已有 parseDDLColumnNames，可从 DDL 中按顺序解析列名并跳过表级约束；schema-mapper.ts 已 re-export parseDDLColumnNames，sync-bridge.ts 当前也从 schema-mapper.ts 导入其它 DDL 工具，继续从该入口导入 parseDDLColumnNames 能保持 data/sqlite 层既有依赖风格。tests/data/sqlite/schema-mapper.test.ts:309-312 已覆盖 columns=[] 时返回 [['row_id']] 的旧行为，但没有覆盖 fallback columns。tests/data/sqlite/sync-bridge.test.ts:230-245 覆盖有数据导出中文表头还原，但没有覆盖只有表头、无数据行的 SQLite 空表导出。
needs: 用户的本质需求不是加载那段运行时字符串替换补丁，而是源码级修复“填表结果为空/SQLite 空表导出后表头被降级成只有 row_id”的数据完整性问题，并补充回归测试，保证后续重构不会再次把空表表头丢掉。修复必须只作用于 SQLite SQL→content 转换链路，不破坏已有 columns 非空时的中文表头映射和 null/blob 值转换。
key_challenges: 核心难点是区别“查询结果没有数据行但仍有列信息”和“底层 queryResult.columns 为空导致没有列信息”两类空结果；前者现有 resultToContent(['col1','col2'], []) 已能保留表头，后者需要从 DDL fallback 恢复列名。另一个难点是 fallback columns 应该使用 SQL 列名再经过 chineseHeaders 映射，而不是直接用中文表头，否则会破坏 resultToContent 的单一职责。第三个难点是保留旧 API 兼容性：已有调用 resultToContent(columns, values, chineseHeaders) 不能被迫修改。
confidence: HIGH
  - HIGH: 已定位唯一源码定义和唯一业务调用点；已确认 DDL 解析函数存在且可复用；已定位直接单测与 sync bridge 组合测试位置；修复可以通过可选参数保持兼容，边界条件明确。
approach: 在 resultToContent 增加第四个可选参数 fallbackColumns?: string[]，仅当 columns 缺失或长度为 0 且 fallbackColumns 为非空数组时使用 fallbackColumns 作为列名来源；随后仍走同一套 headerRow 构建逻辑，让 sqlToChinese 映射把 item_name 映射为“物品名称”。在 sync-bridge 的 _exportSheet 中，从 ddl 解析 fallbackColumns 并传入 resultToContent。补两个层级测试：schema-mapper 单测锁住 fallbackColumns 行为和无 fallback 时的旧行为；sync-bridge 组合测试用 content 只有表头无数据行的 sheet 加载后导出，验证导出 content[0] 仍是 DDL 注释映射后的完整中文表头。
  三维评分（每个维度 1-5 分，5 为最优）：
  - 可维护性: 5/5 — 改动集中在 SQL 结果转换函数和唯一导出调用点，不引入运行时 monkey patch，不新增无用抽象；参数名 fallbackColumns 明确表达用途，调用方从 DDL 解析列名的逻辑放在拥有 DDL 上下文的 _exportSheet。
  - 健壮性: 5/5 — 处理 columns 为 undefined/null 的防御性输入、columns=[] 且 fallbackColumns 非空、fallbackColumns 为空、columns 非空时不覆盖真实查询列信息；中文表头映射仍复用原逻辑。
  - 可扩展性: 4/5 — 以后其它 SQL 查询导出若也遇到列元数据缺失，可复用 resultToContent 的 fallbackColumns 参数；没有过度抽象成策略对象，合理控制复杂度。
edge_cases: 
  - resultToContent 接收 columns=[]、values=[]、fallbackColumns=['row_id','item_name','quantity'] 时，应返回只有完整表头的一行，而不是 [['row_id']]。
  - resultToContent 接收 columns=[]、fallbackColumns 非空且 chineseHeaders 包含 item_name→物品名称 时，应返回 ['row_id','物品名称',...]，证明 fallback 仍走中文映射。
  - resultToContent 接收 columns 非空时，即使 fallbackColumns 非空也必须优先使用 columns，避免真实 SELECT 指定列被 DDL 全量列覆盖。
  - resultToContent 接收 columns=[] 且 fallbackColumns 缺失或空数组时，继续返回 [['row_id']]，保持旧调用兼容。
  - SyncBridge 导出只有表头无数据行的 SQLite 表时，queryResult.values 为空且 queryResult.columns 可能为空，导出的 sheet.content[0] 必须保留 DDL 定义的完整中文表头。
affected_scope: 
  - src/data/sqlite/schema-mapper.ts
  - src/data/sqlite/sync-bridge.ts
  - tests/data/sqlite/schema-mapper.test.ts
  - tests/data/sqlite/sync-bridge.test.ts
execution_plan:
  - step_1: 修改 src/data/sqlite/schema-mapper.ts 的 resultToContent 函数签名，增加 fallbackColumns?: string[]；在构建 headerRow 前增加有效列解析逻辑：如果 columns 不是非空数组且 fallbackColumns 是非空数组，则使用 fallbackColumns；如果最终仍无列，则返回 [['row_id']]；后续 headerRow 和 dataRows 逻辑使用解析后的 effectiveColumns。
  - step_2: 修改 src/data/sqlite/sync-bridge.ts 的 import，从 schema-mapper 增加 parseDDLColumnNames；在 _exportSheet 中基于 ddl 计算 fallbackColumns，并把它作为第四参传给 resultToContent。
  - step_3: 修改 tests/data/sqlite/schema-mapper.test.ts 的 resultToContent describe，新增 fallbackColumns 回归测试，并补一条 columns 非空时不被 fallbackColumns 覆盖的测试。
  - step_4: 修改 tests/data/sqlite/sync-bridge.test.ts 的 exportToTableData describe，新增“空 SQLite 用户表导出保留 DDL 表头”的组合测试：用 makeSheet({ content: [['row_id','物品名称','数量','描述']] }) 加载，无 INSERT；导出后断言 content 等于完整表头。
  - step_5: 运行 npx vitest run tests/data/sqlite/schema-mapper.test.ts tests/data/sqlite/sync-bridge.test.ts，确认定向回归通过；再运行 npx tsc --noEmit 确认类型兼容。
  - step_6: 调用验收专家复查本次修复，若发现阻塞问题则按反馈修复并再次验证。
degradation_check:
  - 方案是否是三维评估（可维护性、健壮性、可扩展性）综合最优的？ → YES。可维护性 5/5，因为修复落在数据转换函数和唯一导出调用点；健壮性 5/5，因为覆盖缺列元数据、无 fallback、真实 columns 优先、中文映射等边界；可扩展性 4/5，因为可复用可选参数但没有制造多余抽象。
  - 是否遗漏了已知边界条件？ → NO。已列出 fallback 非空、fallback 缺失、columns 非空优先、中文映射、SyncBridge 端到端空表导出五类边界。
  - 是否因改动量大而想缩减方案？ → NO。改动量小但必须同时改源码和测试；不会只评价用户补丁而不落地修复。
  - 是否打算跳过某些文件？ → NO。affected_scope 中四个文件都在 execution_plan 覆盖范围内。
  - execution_plan是否覆盖affected_scope所有文件？ → YES。step_1 覆盖 schema-mapper.ts，step_2 覆盖 sync-bridge.ts，step_3 覆盖 schema-mapper.test.ts，step_4 覆盖 sync-bridge.test.ts。
  - context是否充分？是否有未读但可能相关的文件？ → NO。已搜索唯一 resultToContent 调用点和 DDL 解析工具；当前修复不需要读取手动/自动填表 orchestrator，因为问题落在 SQLite 导出 SQL→content 转换链路。
  - 是否有发现了但被我判断为"无关紧要"而跳过的问题？ → NO。用户提供的远程 bundle monkey patch 被判断为不应采用，原因是它绕过源码、版本和构建验证；对应能力会在源码中实现。
  - execution_plan中是否有步骤计划使用 shell 命令（sed/awk/perl）修改源代码？ → NO。源码修改使用 apply_diff，shell 只用于测试和类型检查。
</analysis>---
<output_quality_review>
task_summary: 本次任务将用户提供的运行时 bundle 字符串替换补丁转化为源码级修复：SQLite 空表导出或运行时清空后导出时，若 SQL 查询结果没有返回列名，则从 DDL 解析列名作为 fallback，保留完整表头，并通过 DDL 注释映射回中文表头。
deliverables:
  - src/data/sqlite/schema-mapper.ts：resultToContent 增加 fallbackColumns?: string[]，columns 非空时优先使用真实 columns，columns 为空时使用 fallbackColumns 恢复表头，两者都为空时保持旧行为 [['row_id']]。
  - src/data/sqlite/sync-bridge.ts：_exportSheet 从 DDL 解析 parseDDLColumnNames(ddl)，并传给 resultToContent 作为 fallbackColumns。
  - tests/data/sqlite/schema-mapper.test.ts：新增 columns=[] + fallbackColumns 恢复完整中文表头测试；新增 columns 非空时真实 columns 优先测试。
  - tests/data/sqlite/sync-bridge.test.ts：新增初始空 SQLite 用户表导出保留完整表头测试；新增运行时 DELETE 清空后导出仍保留完整表头测试。

# 量化指标总览
metrics:
  total_files_modified: 4 — 修改源码 2 个文件，测试 2 个文件。
  execution_plan_coverage: 6/6 = 100% — 已完成 resultToContent 修复、SyncBridge 调用修复、schema-mapper 单测、sync-bridge 组合测试、定向测试与 tsc 验证、验收专家复查；并根据验收建议补充 DELETE 清空真实链路测试后完成二次验收。
  edge_cases_handled: 5/5 = 100% — 已覆盖 fallback 非空恢复表头、中文映射、真实 columns 优先、无 fallback 保持旧行为、SyncBridge 空表导出保留 DDL 表头；额外覆盖运行时 DELETE 清空后导出。
  confidence_assessment: HIGH — 定向测试 58/58 passed，tsc --noEmit exitCode 0，验收专家二次复查允许最终放行。
    - HIGH: 核心链路已有源码级修复、单元测试、组合测试和独立验收；剩余 PRAGMA live schema fallback / ALTER TABLE schema 漂移属于后续增强，不阻塞当前空表表头丢失修复。

# 产物实质性检查
substance_check:
  - 产物中是否存在"形式完整但实质空洞"的内容？
    → NO。src/data/sqlite/schema-mapper.ts 的 resultToContent 行为实际改变了 columns=[] 时的表头恢复结果；src/data/sqlite/sync-bridge.ts 实际把 DDL 解析出的 fallbackColumns 接入导出链路；schema-mapper 测试会在 fallback 逻辑失效时失败；sync-bridge 测试会在空表/DELETE 清空导出丢表头时失败。
  - 产物是否能被其目标对象（被测代码/被重构模块/被修复的bug）的变化所"击穿"？
    → YES。如果移除 resultToContent 的 fallbackColumns 逻辑，schema-mapper 的 columns=[] + fallbackColumns 测试会失败；如果 _exportSheet 不传 fallbackColumns，sync-bridge 的空表导出与 DELETE 清空导出测试会失败；如果真实 columns 优先逻辑被破坏，对应单测会失败。
  - 实质性比率: 4/4 = 100%

# 覆盖完整性检查
completeness_check:
  - 是否存在被跳过的模块/函数/路径？
    → NO。搜索确认 resultToContent 只有 schema-mapper 定义和 sync-bridge 导出调用这一条业务链路；本轮修改覆盖了定义、调用方和对应测试。没有采用用户提供的远程 bundle monkey patch，因为那会绕过源码、构建和版本控制。
  - 产物覆盖的范围是否与 execution_plan 中 affected_scope 完全一致？
    → YES。affected_scope 的四个文件全部已修改或验证：src/data/sqlite/schema-mapper.ts、src/data/sqlite/sync-bridge.ts、tests/data/sqlite/schema-mapper.test.ts、tests/data/sqlite/sync-bridge.test.ts。
  - 核心业务逻辑是否都有直接验证（不依赖间接覆盖）？
    → YES。resultToContent fallback 分支由 tests/data/sqlite/schema-mapper.test.ts 直接验证；SyncBridge 初始空表导出和运行时 DELETE 清空导出由 tests/data/sqlite/sync-bridge.test.ts 使用真实 SqliteEngine + SyncBridge 组合验证；类型兼容由 npx tsc --noEmit 验证。
  - affected_scope 覆盖率: 4/4 = 100%

# 价值密度检查
value_density_check:
  - 产物中高价值内容（验证核心逻辑/处理复杂场景）与低价值内容（验证trivial行为）的比例是多少？
    → 高价值:低价值 = 4:0，高价值占比 100%。四项改动都直接服务于空表表头恢复、真实列优先、导出链路接入和用户反馈真实场景验证，没有用 trivial 断言凑数量。
  - 是否存在"用数量掩盖质量"的模式——大量 trivial 产物掩盖了核心逻辑缺少验证的事实？
    → NO。测试数量不多，但分别锁住纯函数 fallback 分支、真实 columns 优先分支、初始空表导出和运行时 DELETE 清空导出，覆盖的是问题根因链路。

# 需求对齐检查
alignment_check:
  - 产物满足的是用户的字面需求还是本质需求？
    → 满足本质需求。用户给出的补丁指出“SQLite 空表 SELECT 返回 columns=[] 时不要降级成只有 row_id，并在导出单张表时用 DDL 列名 fallback”。本次没有照搬运行时远程 bundle 替换，而是在源码中实现同等能力，并补了回归测试和二次验收，避免下一次构建或发布把修复丢掉。
  - "如果这是别人交给我的，我会接受吗？"
    → YES。当前实现改动小、位置准确、兼容旧调用、测试能击穿核心缺陷，且经过 58/58 定向测试、tsc 类型检查和验收专家二次放行。保留的 PRAGMA live schema fallback / ALTER TABLE schema 漂移风险已明确为后续增强，不是本轮空表表头丢失修复的阻塞项。
</output_quality_review>
