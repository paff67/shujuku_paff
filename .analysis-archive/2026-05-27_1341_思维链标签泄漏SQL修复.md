<analysis>
context: 用户报告合并写入把 AI 思维链（<thought>...</thought>）文本混入 SQL 语句。侦察确认：src/service/ai/prompt-builder/table-edit-parser.ts:15-24 的 normalizeAiResponseForTableEditParsing_ACU 不剥离 <thought>/<thinking> 标签，只做了引号拼接修复和转义处理。src/service/table/sql-table-service.ts:252-257 的 applyEdits 只剥离 HTML 注释标记（<!--/-->），不剥离思维链标签。src/service/vector/summary-vector-index-runtime.ts:90-92 已有剥离 <thinking>/<thought> 的正则模式，但仅在该模块内生效，未被复用到表格编辑解析链路。提取链路是 extractEditsFromAiResponse_ACU → extractTableEditInner_ACU → normalizeAiResponseForTableEditParsing_ACU，提取后 mergeAiEditContents_ACU 合并，再 applyMergedEdits_ACU → provider.applyEdits 执行 SQL。
needs: 确保从 AI 响应中提取编辑内容时，思维链标签在预处理阶段就被剥离，防止思维链文本泄漏进 SQL 执行层。同时需要在 SQL 执行层添加防御性清洗，即使提取失败也能阻止思维链文本被执行。
key_challenges: 核心难点是思维链标签可能有属性（如 <thinking type="deep">）和未闭合的孤立标签，需要同时处理完整块和孤立标签两种情况。另一个难点是不能误伤 SQL 字符串值中偶然出现的 <thought> 文本（实际不可能，因为 SQL 字符串值在提取阶段已经被正确处理）。
confidence: HIGH
  - HIGH: 已定位两个需要修复的精确位置；已有可复用的正则模式（summary-vector-index-runtime.ts）；修复范围小且不涉及接口变更。
approach: 在 normalizeAiResponseForTableEditParsing_ACU 的预处理阶段添加 <thinking>/<thought> 块和孤立标签剥离，使用与 summary-vector-index-runtime.ts 相同的正则模式。在 SqlTableService.applyEdits 的清洗阶段添加同样的剥离作为纵深防御。
  三维评分（每个维度 1-5 分，5 为最优）：
  - 可维护性: 5/5 — 复用已有正则模式，不引入新抽象，修改点明确。
  - 健壮性: 5/5 — 双层防御（提取前剥离 + SQL 执行前剥离），处理完整块和孤立标签两种情况。
  - 可扩展性: 4/5 — 如果未来有新的 AI 推理标签格式，只需在同一个位置添加正则。
edge_cases: 
  - AI 响应包含 <thought>...</thought> 完整块时，应在提取前被完整剥离。
  - AI 响应包含 <thinking type="deep">...</thinking> 带属性块时，应被正确匹配并剥离。
  - AI 响应包含未闭合的 <thought> 或 </thinking> 孤立标签时，应被剥离。
  - 正常 SQL 语句不受影响，因为剥离发生在 SQL 字符串进入执行层之前。
  - 原生模式（非 SQL）的 parseAndApplyTableEdits_ACU 也通过 normalizeAiResponseForTableEditParsing_ACU 获得保护。
affected_scope: 
  - src/service/ai/prompt-builder/table-edit-parser.ts
  - src/service/table/sql-table-service.ts
  - tests/service/ai/prompt-builder/table-edit-parser.test.ts（如果存在）
  - tests/service/table/sql-table-service.test.ts
execution_plan:
  - step_1: 修改 src/service/ai/prompt-builder/table-edit-parser.ts 的 normalizeAiResponseForTableEditParsing_ACU，在现有清洗逻辑之前添加 <thinking>/<thought> 块和孤立标签的剥离。
  - step_2: 修改 src/service/table/sql-table-service.ts 的 applyEdits，在 HTML 注释剥离之前添加 <thinking>/<thought> 块和孤立标签的防御性剥离。
  - step_3: 搜索并确认 table-edit-parser 的测试文件位置，补充思维链剥离的回归测试。
  - step_4: 运行定向测试、类型检查和构建验证。
  - step_5: 验收专家复查。
degradation_check:
  - 方案是否是三维评估（可维护性、健壮性、可扩展性）综合最优的？ → YES。可维护性 5/5，复用已有模式；健壮性 5/5，双层防御；可扩展性 4/5，添加新标签只需追加正则。
  - 是否遗漏了已知边界条件？ → NO。已列出完整块、带属性块、孤立标签、SQL 安全性、原生模式兼容五类边界。
  - 是否因改动量大而想缩减方案？ → NO。两个文件各加 3 行正则，改动量极小。
  - 是否打算跳过某些文件？ → NO。affected_scope 中所有文件都在 execution_plan 覆盖范围内。
  - execution_plan是否覆盖affected_scope所有文件？ → YES。step_1 覆盖 table-edit-parser.ts，step_2 覆盖 sql-table-service.ts，step_3 覆盖测试。
  - context是否充分？是否有未读但可能相关的文件？ → NO。已读取 normalizeAiResponseForTableEditParsing_ACU、extractTableEditInner_ACU、applyEdits、splitSqlStatements、runBatch 的完整实现。
  - 是否有发现了但被我判断为"无关紧要"而跳过的问题？ → NO。
  - execution_plan中是否有步骤计划使用 shell 命令（sed/awk/perl）修改源代码？ → NO。使用 apply_diff。
</analysis>