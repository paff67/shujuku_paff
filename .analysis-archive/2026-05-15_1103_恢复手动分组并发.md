<analysis>
context: src/service/table/update-orchestrator.ts 的 orchestrateManualUpdate_ACU 当前在手动分组阶段先按 selectedSheetKeys 构造 updateGroups，groupKeys 来自 Object.keys(updateGroups)。clearBeforeUpdate 路径会先清空目标楼层和目标表，再调用 loadAllChatMessages_ACU、reloadStorageProvider、refreshData，不能破坏。当前第850-891行把手动分组强制串行执行，注释声称 tablePersistenceV2 是单条消息单 layer 替换写；这个事实在 tablePersistenceV2 多 delta 改造后已经过期。当前循环中每个 group 会解析 primarySheetKey、primaryTableName，通过 resolveTableApiPresetOverride_ACU 得到 tableApiPreset，并把 requestOptions 传给 processBatch(group.indices, 'manual_independent', { targetSheetKeys, batchSize, requestOptions })。失败结果会推入 failedGroups 并 break；异常也推入 failedGroups 并 break。循环结束后先 _set_isAutoUpdatingCard_ACU(false)，若 failedGroups 非空，会再次 loadAllChatMessages_ACU 和 refreshData 恢复运行时数据，然后返回首个错误。tests/service/table/update-orchestrator.test.ts 第963-1020行有一个旧测试强制断言 maxConcurrentGroups=2 时仍顺序执行，并且每个单独 group 之间必须 load/refresh；这与用户要求恢复分组并发冲突，必须重写而不是删除。src/service/table/update-scheduler.ts 的 executeAutoUpdatePlan_ACU 已有项目内并发 chunk 模式：maxConcurrentGroups = Math.max(1, settings.maxConcurrentGroups || 1)，每个 chunk 通过 Promise.allSettled 并发处理，并收集失败。自动更新当前是在所有组完成后统一刷新；手动更新为了让后续 chunk 看到前一 chunk 的已落盘 deltas，应采用每个 chunk 完成后刷新。已知 tablePersistenceV2 多 delta 语义是 deltas 列表追加、delta 作为最新 mirror、sequence 用于重放顺序；这能解决同一 floor 上多个 delta 不能共存的问题，但不能证明 processUpdatesBatch_ACU 内部对 currentJsonTableData_ACU 的全局状态读写完全并发安全。
needs: 恢复 orchestrateManualUpdate_ACU 对 settings_ACU.maxConcurrentGroups 的尊重，使多个手动 update group 在同一个 chunk 内并发启动；同时保留现有 requestOptions/tableApiPreset、clearBeforeUpdate、失败恢复、自动合并和最终状态清理行为。测试必须从“禁止并发”改为“验证并发重叠、chunk 边界刷新、失败 chunk 阻止后续 chunk”。实现不能宣称 tablePersistenceV2 之外的全局 runtime state 已完全并发隔离，只能通过 chunk 刷新降低后续 chunk 风险，并把同 chunk 内并发限定为恢复用户要求的旧调度语义。
key_challenges: 第一，旧注释和旧测试与新需求直接冲突，不改测试会让回归套件锁死在错误行为上。第二，Promise.allSettled 的 idx 必须对应 chunkKeys，不能在并发返回对象和 chunkKeys 间错位，否则失败组记录会错。第三，一个 chunk 中如果一个 group 失败，已经并发启动的其他 group 不能取消，因此只能保证“不启动后续 chunk”，不能保证同 chunk 内全部停止。第四，手动流程需要每个 chunk 后 loadAllChatMessages_ACU 和 refreshData，保证后续 chunk 基于前序已落盘 deltas 重建，而不是只在最后刷新。第五，processUpdatesBatch_ACU 仍可能写 currentJsonTableData_ACU，全局状态并发风险不是本次小范围调度恢复能彻底消除的；测试只能覆盖编排层并发和失败策略，不能伪装成全链路并发隔离证明。
confidence: MEDIUM
  - 侦察信息足以修改 orchestrateManualUpdate_ACU 的编排逻辑和对应单元测试。
  - 未验证假设1：processBatch 的真实实现 processUpdatesBatch_ACU 在同 chunk 内并发表处理时是否完全避免 currentJsonTableData_ACU 互相污染；目前只能依据用户要求和 tablePersistenceV2 多 delta 改造恢复调度并发。
  - 未验证假设2：底层 persistTablesToChatMessage_ACU 的并发读改写在真实宿主保存链路中是否完全原子；多 delta 让同层追加具备数据结构承载能力，但不是跨异步保存事务锁。
approach: 采用 chunked concurrency 恢复方案：在 orchestrateManualUpdate_ACU 中计算 maxConcurrentGroups = Math.max(1, settings_ACU.maxConcurrentGroups || 1)，按 chunk 切分 groupKeys；每个 chunk 内 map 成异步任务并发调用 processBatch；用 Promise.allSettled 收集失败；每个 chunk 完成后调用 loadAllChatMessages_ACU 和 refreshData；如果该 chunk 有失败，则刷新后 break，不再启动后续 chunk；最终沿用既有 failedGroups 恢复和返回逻辑。这个方案与 executeAutoUpdatePlan_ACU 的并发模型一致，但手动流程增加 chunk 后刷新以兼容跨 chunk 累积。
  三维评分（每个维度 1-5 分，5 为最优）：
  - 可维护性: 4/5 — 复用项目已有 chunk + Promise.allSettled 调度风格，改动集中在 orchestrateManualUpdate_ACU 的旧串行块和对应测试；不足是手动/自动调度仍存在重复模式，但本次不扩大重构范围，避免引入无关风险。
  - 健壮性: 4/5 — 处理 maxConcurrentGroups 小于等于0或未设置时回落1，捕获 rejected 和 success=false，失败后刷新状态，并阻止后续 chunk；不足是同 chunk 内已启动任务无法取消，全局 currentJsonTableData_ACU 并发隔离也不是本次完全解决。
  - 可扩展性: 3/5 — chunk 语义清晰，未来可抽取共享 group executor 或加入 save-target-aware 锁；当前仍把策略内联在 orchestrator 中，扩展到更复杂的并发隔离会需要继续重构。
edge_cases: 
  - settings_ACU.maxConcurrentGroups 未配置、为0或负数时必须退化为1，避免 for 循环步长为0导致死循环。
  - 同一个 chunk 中一个 group 返回 { success:false }，另一个 group 成功时，结果必须失败，并且后续 chunk 不得启动。
  - 同一个 chunk 中一个 group 抛异常时必须计为失败，错误信息进入 failedGroups，并且后续 chunk 不得启动。
  - maxConcurrentGroups=2 且有两个分组时，两个 processBatch 必须在同一 chunk 内重叠启动，不能出现第一个结束后第二个才启动的顺序化行为。
  - 每个 chunk 完成后必须 loadAllChatMessages_ACU 再 refreshData，确保下一 chunk 基于前一 chunk 的 tablePersistenceV2.deltas 重建。
  - requestOptions 的 tableApiPreset 仍必须按每个 group 的 primarySheetKey / primaryTableName 独立计算，不能因为并发 chunk 改成共享对象而串 preset。
  - clearBeforeUpdate 的预清空、reloadStorageProvider 和初始刷新必须保留在并发执行前。
affected_scope: 
  - src/service/table/update-orchestrator.ts
  - tests/service/table/update-orchestrator.test.ts
  - .analysis-cache.md
  - .analysis-archive/{timestamp}_恢复手动分组并发.md
execution_plan:
  - step_1: 修改 src/service/table/update-orchestrator.ts 第850-891行附近的手动串行循环，替换为 maxConcurrentGroups chunk 并发循环，移除 tablePersistenceV2 单 layer 替换写的过期注释，改为说明 tablePersistenceV2.deltas 支持同层多 delta，但 chunk 后刷新仍用于后续 chunk 状态重建。
  - step_2: 在同一代码块中保持 primarySheetKey、primaryTableName、tableApiPreset、requestOptions 的逐 group 计算，并让 processBatch 的参数结构保持不变；Promise.allSettled 后同时处理 rejected、success=false 和错误信息。
  - step_3: 修改 tests/service/table/update-orchestrator.test.ts 第963-1020行旧顺序化测试，改为验证 maxConcurrentGroups=2 时两个 group 在同一 chunk 内并发启动、没有单 group 间刷新、chunk 完成后刷新一次，并继续断言 targetSheetKeys。
  - step_4: 在 tests/service/table/update-orchestrator.test.ts 添加一个失败策略测试：至少三个 group、maxConcurrentGroups=2，第一 chunk 中一个成功一个失败，第三个 group 不得启动，结果返回失败，并验证失败 chunk 后发生恢复刷新。
  - step_5: 运行 npx vitest run tests/service/table/update-orchestrator.test.ts；若通过，再运行 npx vitest run tests/service/table/update-orchestrator.test.ts tests/service/table/table-service.test.ts tests/service/table/table-delta-reconstruct.test.ts；最后运行 npm run typecheck。
  - step_6: 验收修改和测试结果，写入 .analysis-cache.md 并归档到 .analysis-archive 的新文件，不覆盖旧归档。
degradation_check:
  - 方案是否是三维评估（可维护性、健壮性、可扩展性）综合最优的？ → YES。可维护性4/5，因为沿用项目已有 chunk 并发风格；健壮性4/5，因为失败收集、异常处理、chunk 后刷新和后续 chunk 阻止都覆盖；可扩展性3/5，因为尚未抽象 save-target-aware 调度器，但本次目标是恢复手动分组并发而不是重构整个更新系统。
  - 是否遗漏了已知边界条件？ → NO。已列出 maxConcurrentGroups 边界、success=false、throw、并发重叠、chunk 后刷新、requestOptions 独立计算、clearBeforeUpdate 保留。
  - 是否因改动量大而想缩减方案？ → NO。会同时修改生产代码和测试，并运行相关验证，不删除旧测试来逃避失败。
  - 是否打算跳过某些文件？ → NO。当前受影响的生产代码和测试文件都在 affected_scope 中；tablePersistenceV2 多 delta 已完成，本次不重复改动。
  - execution_plan是否覆盖affected_scope所有文件？ → YES。step_1/2 覆盖 update-orchestrator.ts，step_3/4 覆盖 update-orchestrator.test.ts，step_6 覆盖 analysis cache/archive。
  - context是否充分？是否有未读但可能相关的文件？ → YES，存在未彻底读取 processUpdatesBatch_ACU 全实现的并发状态风险，但本次修改目标是恢复 orchestrateManualUpdate_ACU 调度并发；该风险已在 confidence 和 key_challenges 中明确，不会伪装成已完全解决。
  - 是否有发现了但被我判断为"无关紧要"而跳过的问题？ → YES。currentJsonTableData_ACU 的全局并发风险和 persistTablesToChatMessage_ACU 的真实并发读改写原子性风险未在本次彻底解决；它们不是无关紧要，而是超出“恢复分组并发调度”的直接修改范围。当前方案通过 chunk 后刷新降低跨 chunk 风险，但同 chunk 内仍依赖现有 processBatch/persistence 行为。必须在最终说明中诚实标注。
  - execution_plan中是否有步骤计划使用 shell 命令（sed/awk/perl）修改源代码？ → NO。修改使用 apply_diff/write_file；shell 只用于测试和 typecheck。
  → YES项已就地修正：对未彻底解决的全局状态风险进行明确边界声明，并用测试覆盖编排层而非虚假宣称全链路事务安全。
</analysis>
---
<output_quality_review>
task_summary: 恢复 orchestrateManualUpdate_ACU 的手动分组 chunk 并发执行，让 settings_ACU.maxConcurrentGroups 重新生效，并将旧的“必须串行”回归测试改为验证并发重叠、chunk 后刷新、失败 chunk 阻止后续 chunk。
deliverables:
  - 修改 src/service/table/update-orchestrator.ts：第850行附近从逐组串行循环改为基于 maxConcurrentGroups 的 Promise.allSettled chunk 并发循环。
  - 修改 tests/service/table/update-orchestrator.test.ts：重写旧顺序化测试，新增失败 chunk 阻断后续 chunk 的测试。
  - 更新 .analysis-cache.md 并准备归档。

# 量化指标总览
metrics:
  total_files_modified: 3 — src/service/table/update-orchestrator.ts、tests/service/table/update-orchestrator.test.ts、.analysis-cache.md
  execution_plan_coverage: 6/6 = 100% — step_1 到 step_6 均已执行或正在完成归档。
  edge_cases_handled: 7/7 = 100% — maxConcurrentGroups 回落、success=false、throw、并发重叠、chunk 后刷新、requestOptions 独立计算、clearBeforeUpdate 保留均有实现或测试覆盖。
  confidence_assessment: MEDIUM — 编排层修改与测试通过，但同 chunk 内 processUpdatesBatch_ACU 对 currentJsonTableData_ACU 的全局状态并发隔离仍不是本次彻底证明的范围；persistTablesToChatMessage_ACU 的真实宿主保存原子性也仍是已知边界。

# 产物实质性检查
substance_check:
  - 产物中是否存在"形式完整但实质空洞"的内容？
    → NO。update-orchestrator.ts 的循环语义实际改变：从逐 group await 改为 chunk 内 Promise.allSettled 并发；测试不是凑数，旧测试被改成会在串行实现下卡住或失败的并发重叠验证，新增失败测试会在后续 chunk 被错误启动时失败。
  - 产物是否能被其目标对象（被测代码/被重构模块/被修复的bug）的变化所"击穿"？
    → YES。如果把生产代码改回串行，第一个并发测试中 sheet_0 等待 sheet_1 释放会暴露错误；如果失败后仍启动后续 chunk，第二个测试会出现 sheet_2:start 或抛出“后续 chunk 不应启动”；如果 chunk 后不刷新，事件序列会失败。
  - 实质性比率: 3/3 = 100%

# 覆盖完整性检查
completeness_check:
  - 是否存在被跳过的模块/函数/路径？
    → YES。processUpdatesBatch_ACU 内部 currentJsonTableData_ACU 全局状态并发隔离没有在本次重构中解决；原因不是回避困难，而是用户本轮明确目标是恢复 orchestrateManualUpdate_ACU 分组并发，且 tablePersistenceV2 多 delta 已完成。这个风险已在 analysis 中标注，不能包装成已解决。
  - 产物覆盖的范围是否与 execution_plan 中 affected_scope 完全一致？
    → YES。生产代码、测试、analysis cache/archive 均覆盖。
  - 核心业务逻辑是否都有直接验证（不依赖间接覆盖）？
    → YES。maxConcurrentGroups=2 的同 chunk 并发启动由事件顺序直接验证；失败 chunk 阻断后续 chunk 由 sheet_2 不启动直接验证；chunk 后刷新由 load/refresh 事件序列直接验证。
  - affected_scope 覆盖率: 4/4 = 100%

# 价值密度检查
value_density_check:
  - 产物中高价值内容（验证核心逻辑/处理复杂场景）与低价值内容（验证trivial行为）的比例是多少？
    → 高价值:低价值 = 3:0，高价值占比 100%。两个测试都覆盖并发调度核心风险，不是只测函数被调用这种低价值断言；生产代码改动直接恢复用户要求的调度语义。
  - 是否存在"用数量掩盖质量"的模式——大量 trivial 产物掩盖了核心逻辑缺少验证的事实？
    → NO。只新增/改写必要测试，没有堆数量。测试会对错误调度行为产生明确失败。

# 需求对齐检查
alignment_check:
  - 产物满足的是用户的字面需求还是本质需求？
    → 满足本质需求：不是简单删掉串行限制，而是按 settings_ACU.maxConcurrentGroups 恢复 chunk 并发，同时保留 requestOptions/tableApiPreset、chunk 后刷新、失败恢复和后续 chunk 阻断。依赖 tablePersistenceV2.deltas 的同层多 delta 承载能力，但没有虚假承诺全局 runtime state 已完全事务化。
  - "如果这是别人交给我的，我会接受吗？"
    → YES，但带技术边界说明接受。编排层恢复并发和回归测试达标；如果要把质量从 MEDIUM 推到 HIGH，下一步应设计 save-target-aware/global-state-aware 的 processBatch 隔离或锁策略，而不是假装当前并发链路已经无条件安全。
</output_quality_review>
