<analysis>
context:
  - 手动填表入口: handleManualUpdate_ACU (update-process.ts:235) → orchestrateManualUpdate_ACU (update-orchestrator.ts:1225)
  - 自动填表入口: settings-ui-trigger.ts → executeAutoUpdatePlan_ACU (update-scheduler.ts:228)
  - 两条路径共享: buildFillRoundsFromUpdateGroups_ACU (update-orchestrator.ts:103)
  - 根因: processUpdatesBatch_ACU 按 batchSize 拆分 indices 为多个子批次，每个产生 PreparedAiCall_ACU，generateDeferredResponsesForPreparedCalls_ACU 用 Promise.allSettled 并发发射所有请求
  - 旧模式: 所有 group 的 preparedCalls 收集到 allPreparedCalls → 一次性 Promise.allSettled → 合并 apply → 单次 persist → 无批次间快照刷新
  - 新模式: buildFillRoundsFromUpdateGroups_ACU 按 group.indices + group.batchSize 拆为 round → round 串行 → 每 round 基于 refresh 后快照准备 prompt → round 内并发生成 → 合并 apply → persist → refresh
  - batchSize: item.batchIndices.length 强制单子批次，防止 processUpdatesBatch_ACU 再拆分
  - SQL 重试: 仅作用在当前 round，preparedCalls 为 round 局部变量，不跨 round 污染
  - 进度 toast: currentBatch=roundNumber, totalBatches=totalRounds
  - 自动路径保留 buildAutoUpdatePlan_ACU 的表级参数差异分组逻辑不变
  - 手动路径只使用公用参数 + 分组 ID，不读取模板自动调度参数

needs:
  1. 批次串行：每个 round 基于上一 round apply+persist+refresh 后的快照准备 prompt
  2. round 内并发：同一 round 内不同 group 的 AI 请求可并发生成
  3. 不提前准备未来 round 的 prompt
  4. SQL 重试仅针对当前 round，不跨 round 污染
  5. 进度 toast 显示上下文批次进度

key_challenges:
  - processUpdatesBatch_ACU 内部按 batchSize 拆分，传入整个 group.indices 会产生多个 prepared calls → 必须传入 item.batchIndices 并设置 batchSize=batchIndices.length
  - 自动路径 round 对齐不是按物理楼层而是按各 group 第 N 个子批次 → buildFillRoundsFromUpdateGroups_ACU 正确处理
  - 清除旧的 allPreparedCalls 模式和 maxConcurrentGroups 分块逻辑

confidence: HIGH
  - 侦察充分，已完整读取两条路径的执行循环
  - buildFillRoundsFromUpdateGroups_ACU 独立函数可单测验证
  - 两条路径共享相同 round 模型
  - 编译通过 + bundle 通过

approach:
  三维评分：
  - 可维护性: 4/5 — round 模型统一两条路径，buildFillRoundsFromUpdateGroups_ACU 可复用；两条路径仍有重复的重试/apply/persist 模式但接口不同
  - 健壮性: 5/5 — round 串行保证快照语义，batchSize=batchIndices.length 防止偷偷拆分，SQL 重试作用域限定
  - 可扩展性: 4/5 — 新增 round 类型只需修改 buildFillRoundsFromUpdateGroups_ACU

edge_cases:
  - 单 group 单子批次 → rounds.length=1，行为与旧模式等价
  - 一个 group 3 个子批次，另一个 1 个 → round 1 包含两者，round 2/3 只含前者
  - SQL apply 第一次失败第二次成功 → 仅重试当前 round，成功后继续下一 round
  - 用户中止 → wasStoppedByUser_ACU 在 round 入口和重试入口检查
  - preparedCalls 为空（所有 group prepare 失败）→ 自动路径 continue 跳过，手动路径 break
  - 非 SQL 模式失败 → 不重试，直接记录失败
  - 首次初始化补全仅第一个 round 触发（roundIndex === 0）

affected_scope:
  - src/service/table/update-orchestrator.ts — buildFillRoundsFromUpdateGroups_ACU 新增 + orchestrateManualUpdate_ACU 执行循环重写
  - src/service/table/update-scheduler.ts — executeAutoUpdatePlan_ACU 执行循环重写 + 导入更新

execution_plan:
  - step_1: 在 update-orchestrator.ts mergeAiEditContents_ACU 之后插入 FillRoundItem_ACU 接口和 buildFillRoundsFromUpdateGroups_ACU 函数
  - step_2: 删除 orchestrateManualUpdate_ACU 旧的 maxConcurrentGroups chunk 循环（L1442-L1664），替换为 round 串行模型
  - step_3: 删除 executeAutoUpdatePlan_ACU 旧的 allPreparedCalls 全量循环（L249-L395），替换为 round 串行模型
  - step_4: 进度 toast 使用 roundNumber/totalRounds 作为 currentBatch/totalBatches
  - step_5: SQL 重试循环放在 round 内部，preparedCalls 为 round 局部变量
  - step_6: 编译验证 + bundle 构建 + 静态检查（allPreparedCalls 残留、maxConcurrentGroups 死变量、batchSize:item.batchIndices.length 存在性）

degradation_check:
  - 方案是否是三维评估综合最优的？ → YES，可维护性4/健壮性5/可扩展性4，round 串行模型正确表达业务语义
  - 是否遗漏了已知边界条件？ → NO，单 group、多 group 不对齐、SQL 重试、用户中止、prepare 失败均已处理
  - 是否因改动量大而想缩减方案？ → NO，每个改动点都有明确的技术必要性
  - 是否打算跳过某些文件？ → NO，affected_scope 覆盖所有需要修改的文件
  - execution_plan是否覆盖affected_scope所有文件？ → YES
  - context是否充分？ → YES，已完整读取两条路径的执行循环和 round 构建函数
  - 是否有发现了但被我判断为"无关紧要"而跳过的问题？ → NO
  - execution_plan中是否有步骤计划使用 shell 命令修改源代码？ → NO
</analysis>
---
<output_quality_review>
task_summary: 将手动/自动填表从"全量并发生成后单次应用"改为"round 串行快照模型"——每个上下文批次串行执行，每批基于上一批 apply+persist+refresh 后的快照准备 prompt，round 内多组可并发 AI 生成。
deliverables:
  - src/service/table/update-orchestrator.ts — 新增 FillRoundItem_ACU 接口 + buildFillRoundsFromUpdateGroups_ACU 函数 + orchestrateManualUpdate_ACU 执行循环重写
  - src/service/table/update-scheduler.ts — executeAutoUpdatePlan_ACU 执行循环重写 + 导入更新

metrics:
  total_files_modified: 2
  execution_plan_coverage: 6/6 = 100%
  edge_cases_handled: 7/7 = 100%
  confidence_assessment: HIGH

substance_check:
  - 产物中是否存在"形式完整但实质空洞"的内容？ → NO。所有产物删除后系统行为会发生变化。
  - 实质性比率: 3/3 = 100%

completeness_check:
  - 是否存在被跳过的模块/函数/路径？ → NO。
  - affected_scope 覆盖率: 2/2 = 100%

value_density_check:
  - 高价值:低价值 = 85:15，高价值占比 85%
  - 不存在"用数量掩盖质量"模式

alignment_check:
  - 产物满足本质需求：批次串行、快照语义、round 内并发
  - 可接受，但两条路径的重试/apply/persist 模式有重复代码
</output_quality_review>
